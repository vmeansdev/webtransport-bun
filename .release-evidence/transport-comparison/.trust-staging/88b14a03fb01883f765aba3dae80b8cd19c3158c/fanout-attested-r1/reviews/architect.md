APPROVED

# Exact-stage Architect review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b), second stage

Reviewer: Claude workflow subagent, Architect pass, 2026-09-09 (~03:25–03:40Z). Pre-traffic review of exact bytes. Git read-only (`git status --porcelain -uno | wc -l` → 0 before and after; HEAD `88b14a03…` throughout). Nothing on the rig modified: ssh as `hermes-admin` ran only `ls`, `sha256sum`, `stat`, `cat`, `ss`, `ps`, `find`, `df`, `date` and `sudo -n -u _wtcompare` presence probes. The frozen command was not run, the controller was not pointed at the staged pair, the preflight was not run. The only file written under the repo is this artifact; harnesses live in the session scratchpad. Both first-stage reviews (`../../1b9e8092…/fanout-attested-r1/reviews/{architect,critic}.md`, both CHANGES REQUIRED) were read first and treated as untrusted; every carried claim below was re-executed at this candidate.

## Bindings

- Stage receipt SHA-256: `588f8736e0d6fced1c4f910a372fa5ca7610796c1e52573f7bf11957ddb90fe4`
- Upcoming run command SHA-256: `20ac06c4044794982f7c863165e3df49cc45615f33fa59b9908cc621199b3189`
- Candidate HEAD: `88b14a03fb01883f765aba3dae80b8cd19c3158c`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

Recomputed with `shasum -a 256` and equal to the receipt: plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073` (R6), approval `docs/superpowers/plans/approvals/2026-09-08-physical-budget-amendment.md` → `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`; deviations `2026-09-09-ticker-250-retired-by-preflight.md` → `ccc0964e…`, `2026-09-09-b6-verify-external-trust-bound.md` → `5c23bce514922fb76d5ac4be670e22ee49624717c47227a262ec3d71b3571714`.

## Verdict

The first stage's blocker is fixed at the root and the fix is sound: the external trust bound now has one encoder (`cross-supervisor-protocol.ts` `externalTrustBoundSha256`), the frozen 9.7 verifier recomputes it from the index, the two staged leaves and the stage receipt before any seal is opened, and the quarantine lifts `EXTERNAL_TRUST_BOUND_UNVALIDATED` only for a recomputation that equals the bound. I executed the verifier's recomputation on THIS stage's real receipt, real leaves and the receipt's own anchors (`ok:true`, digest `cac9d358…`), every single-input mutation refuses with `EXTERNAL_TRUST_BOUND_MISMATCH`, and the quarantine over the two real B5 seals relabelled canonical/promotable returns `promotable:true, reasons:[]` with that proof. The staged bytes are the candidate tree by blob identity, the run command is byte-identical to `buildFrozenRunCommand("9.7")` from HEAD, every receipt leaf recomputes, both keys are present with the lease armed, and every carried finding from the first review holds at this candidate. Section 4 lists what remains non-blocking and what the operator must satisfy at launch.

## 1. The fix (diff `1b9e8092..88b14a03`, read in full)

`git -c diff.external= diff --no-ext-diff --stat 1b9e8092 88b14a03` → 10 files, +1284/−42: `verify-campaign-index.ts` (+189), `verify-campaign-index.canonical.test.ts` (+518, new), `verify-campaign-index.test.ts` (+308), `output-policy.ts` (+44/−7), `output-policy.test.ts` (+81), `cross-supervisor-protocol.ts` (+49), `stage-live-campaign.ts` (+30/−35), `stage-live-campaign.test.ts` (+21), `frozen-run-wrapper.fragment.sh` (+5), the deviation note (+52). `88b14a03^` = `1b9e8092` (exactly one commit). Nothing under `crates/`, `scenarios/`, `server.ts`, `fanout-role.ts`, `compare-controller.ts`, `cohort-protocol.ts`, `verify-artifact.ts`, `server-observation-artifact.ts`, `render-campaign-report.ts` — the claim "no relay, transport, server, worker, controller or supervisor code changed" holds by the diff itself; the receipt agrees (`macSupervisorSha256 501f5d2a…`, `linuxSupervisorSha256 84caa017…`, both `bun` digests, `serverEntrypointSha256 aeef5d1b…`, `fanoutRoleEntrypointSha256 d9c3f971…` all equal the B5 pilot receipt's, field-by-field `bun -e` diff of the two receipts).

**1.1 The encoder reproduces the live bounds.** Scratch `encoder.ts` imports `externalTrustBoundSha256` and `EXTERNAL_TRUST_BOUND_PREIMAGE_FIELDS` from HEAD and feeds each receipt's own ten fields: this stage → `cac9d3582efa390dae3ee0a6b63180734b6d919be01037270b9db725d3dd9807` MATCH; the abandoned first B6 stage (`1b9e8092/fanout-attested-r1`) → `161e27a3…` MATCH; the B5 pilot (`1b9e8092/fanout-pilot-r1`) → `63ea97fa…` MATCH; the A5 receipt (`efaed8bb/busyms-attested-focused-r1`) → `34c2261e…` MATCH. For each receipt, perturbing any one of the ten preimage fields changes the digest (10/10). The encoder is the only spelling of the preimage: `runMint` and `buildMinimalStageReceipt` both call it (diff), and the field order is the `canonicalJson` of `{schema, ...fields}` — identical bytes to the old inline literal, which is why the pre-fix receipts reproduce.

**1.2 The verifier refuses on any single field, before opening a seal.** `validateExternalTrustBound` (`verify-campaign-index.ts:533-646`): lstat → regular file → strict JSON → `canonicalJson(value)+"\n"` byte-equality → schema `live-stage-receipt/v1` → every preimage field plus `externalTrustBoundSha256` well-formed → receipt bound equals the command's → the six held fields (index candidate/campaignId/stagedCapabilitySha256/sourceArchiveSha256, the two leaf digests computed from the raw 32-byte leaves the verifier opened) must equal the receipt's copies → recompute over held + receipt's authority/lock/two directory identities → equal the command's bound. It is called at `:852-870`, after `readCampaignIndexV2` and `proveRegisteredTopology` and before `indexedSealed`/the entry loop — no seal is read before it (the canonical test asserts `opened.seals` unchanged on refusal; I re-read the code between `:800` and `:880`). Without `--stage-receipt`, a canonical index with any `promotable:true` entry is refused `EXTERNAL_TRUST_BOUND_UNVALIDATED` at the same point, except under `--integrity-only` (`:862-869`); `--stage-receipt` without both leaves is `TRUST_PROTOCOL` (`:787-792`).

Executed on the real staged material (scratch `real-validate.ts`): the on-disk receipt IS `canonicalJson+"\n"` (true); `publicKeySha256` of the two staged leaf files equals the receipt's leaf digests (true/true); `validateExternalTrustBound({receipt, bound cac9d358…, index anchors = receipt's candidate/campaignId/capability/archive, leaves})` → `ok:true`, `recomputedSha256 == sha256 == cac9d358…`. Mutations, each → `EXTERNAL_TRUST_BOUND_MISMATCH`: bound flipped ("minted for bound cac9d358…"), index candidate = `1b9e8092`, campaignId = `fanout-pilot-r1`, capability = the pilot's `c2224a9a…`, archive = the pilot's `e4b5c7c5…`, mac leaf = the pilot's `4a85c3bc…`, rig leaf = the pilot's `5492aac5…`, receipt = the first B6 stage's file ("minted for bound 161e27a3…"). The index anchors the verifier holds are what the controller writes: the real B5 index (`.release-evidence/…/1b9e8092…/fanout-pilot-r1/campaign-index.json`) carries `candidate 1b9e8092…`, `stagedCapabilitySha256 c2224a9a…`, `sourceArchiveSha256 e4b5c7c5…` = that stage's receipt `capabilitySha256`/`archiveSha256`; the controller is unchanged in this diff.

**1.3 The quarantine.** `output-policy.ts:728-758`: no bound → `EXTERNAL_TRUST_BOUND_MISSING`; bound but no validation, or a validation missing any of `sha256`/`recomputedSha256`/`method` → `EXTERNAL_TRUST_BOUND_UNVALIDATED`; validation present but `sha256` not 64-hex, or `sha256 ≠ recomputedSha256`, or `sha256 ≠ bound` → new `EXTERNAL_TRUST_BOUND_MISMATCH`; otherwise no reason. Executed over the real B5 `ws` and `wt` seals (`reps/ticker-fanout_rate-100/{ws,wt}/rep-1.sealed.json`, 173 KB each, originally `promotable:false, pilot, fanout-pilot-r1`) relabelled `promotable:true, canonical, fanout-attested-r1`, bound `cac9d358…`, `expectedComparisonId fanout-attested-r1`: with the verifier's real validation object → `{"promotable":true,"reasons":[]}` for both arms; with no validation → `["EXTERNAL_TRUST_BOUND_UNVALIDATED"]`; with the validation but a flipped bound → `["EXTERNAL_TRUST_BOUND_MISMATCH"]`. So the real seal shape clears every other quarantine rule (sidecar digests, toolchain observation, source identity, artifactKind, comparisonId) — the placeholder was the only reason left, as the first review found, and it is now lifted only by the recomputation. Callers: `verify-campaign-index.ts:1045` (frozen path, passes the validation), `run-campaign.ts:1981/2057`, `bin/verify-artifact.ts:118`, `render-report.ts:375` pass none and keep the fail-closed answer (grep). `render-campaign-report.ts` (the promoted render at command line 623) carries no quarantine call (grep 0).

**1.4 The frozen wrapper.** The success verification (command lines 596-618, wrapper fragment `:565-577`) now passes `--stage-receipt="$MAC_TRUST/stage-receipt.json"` beside both leaves; the integrity-only attempt (lines 505-510) passes bound + `--integrity-only` only, which the verifier accepts without a receipt (`:862` guard; test `a_canonical_index_claiming_promotion_without_the_receipt…` asserts the integrity-only answer is not `EXTERNAL_TRUST_BOUND_UNVALIDATED`). `grep -n stage-receipt` on the frozen file: 143 (recovery-record path), 215/223 (phase strings), 524 (verify-stage-approval), 526 and 617 — the two the lead named. The file is 680 lines by `wc -l` (the brief said 674; the first stage's was 675 and this diff adds 5). `stage-live-campaign.test.ts` "frozen run wrapper argv" asserts the receipt path is the leaves' parent `/stage-receipt.json` on the success argv and absent from the integrity argv.

**1.5 Tests — what they actually construct.** `bun test verify-campaign-index.canonical.test.ts verify-campaign-index.test.ts output-policy.test.ts stage-live-campaign.test.ts` → 172 pass / 0 fail / 639 expect (8.3 s). Plainly: the positive canonical test is a **stub-shaped root at the seal level and a real-shaped root at the index level**. It builds 60 seals from `fixtures/valid-ws-run.json` relabelled `measured/canonical/promotable:true/comparisonId fanout-attested-r1` with fresh non-sentinel sidecar digests, sealed by the real `sealRunArtifact`, 12 median flats, a real `campaign-index/v2` (six frozen cells read from the 9.7 fragment, 5 reps, `scheduledMeasuredArms 60`), two real Ed25519 leaves and a receipt minted by the real encoder; `verifyRunArtifact`, `reconstructCohortEvidenceOffline` (answering the cell's real `cohortCellCardinality` row so `checkExpectedTotals` runs for real) and `verifyArmAttestationEvidence` are replaced via `mock.module` and restored in `afterAll`. Through the frozen 9.7 argv (`--expect-canonical-fanout-complete`, 60/0/0/60/12/6/60, cells/arms/kinds/reps) it reaches `ok:true` with `promotedCells` = the six cells, `canonicalFanoutComplete:true`, `attestationsVerified:60`, and asserts all 60 seals went through all three delegated verifiers. Mutations: one demoted rep → `expectedPromotableCount` refusal, and without counters the completion gate names `5/6 paired promotions` + `PROMOTION_ENTRY_NOT_PROMOTABLE`, and a left-behind flat is `stale echo flat`; one missing flat → `expectedFlatCount … 11`; one unpaired cell → `expectedPairCount … 5`; wrong bound → `EXTERNAL_TRUST_BOUND_MISMATCH` before any seal opens; moved lock digest → `recomputed as`; no receipt → `EXTERNAL_TRUST_BOUND_UNVALIDATED`. The unit suite adds `validateExternalTrustBound` positive + each of the six held fields + the four receipt-only fields + absent/dir/pretty-printed/missing-field/wrong-schema receipts + the leaves rule + a supplied receipt being checked even on a `promotable:false` index. `output-policy.test.ts` pins the arbitrary-marker refusal (now with the code asserted) and the three mismatch shapes.

Can the real seal shape still fail? What the stubs skip is exactly what B5 exercised on real bytes under the same, unchanged code: `verifyRunArtifact` and the attestation graph passed the frozen 9.6 verifier with both leaves at 03:53 local (the pilot's `diagnostic-report.md` exists, which the wrapper renders only after `SUCCESS_RC=0`; `controller-terminal.json` PASS), and the first review executed the real reconstruction + `checkExpectedTotals` on the real rate-100 seals under canonical. The quarantine on the real shape is executed above (1.3). What no real bytes cover is the other five rows' `checkExpectedTotals` (only stubbed rows) and the completion branch on real seals — the same residual the first review named; it is a defect of the evidence available, not of the bytes, and every one of those paths refuses closed rather than open.

**1.6 Other checks run at HEAD.** `bun run typecheck` (tsc) → clean, 5.8 s. `bunx biome check --diagnostic-level=error` on the eight changed TS files → 0 errors; `biome format` → no fixes; the four production files carry 0 warnings. `bun tools/compare/check-official-io.ts` → `status=PASS failure-count=0`. `bun test r1-entrypoint-red r1-entrypoint-wiring fanout-promotion render-campaign-report mac-trust-access compare-controller` → 86/0 + 115/0 + 114/0. Local acceptance `bun test tools/compare/fanout-production-e2e.test.ts` → **19 pass / 0 fail / 393 expect, 197.26 s, exit 0**; afterwards `ps` shows no `comparison-supervisor`/`compare-controller`/`fanout-role`/`server.ts` process and `lsof` no listener on 4433.

## 2. The bytes (all recomputed here)

**2.1 Source identity.** `git rev-parse HEAD` → `88b14a03…`; `HEAD^{tree}` → `767b4209085f868f8794104e31939e2ad4b0a6f0` = receipt `candidateTreeOid`; porcelain 0. `source.tar` → `09390c3062f5e23452f1715a74b14eb4332e9ef1397e5ce67546a085c9b740ce`, 54,353,920 B, `tar -tf | wc -l` 2,072, inventory file → `0f4964d3…` = `archiveMemberInventorySha256`. Extracted to the scratchpad: 1,778 regular files, 0 symlinks; `git hash-object --stdin-paths` over every file joined with its path, `diff`ed against `git ls-tree -r 767b4209` (1,778 blobs: 1,767 × 100644 + 11 × 100755, no 120000/160000) → **zero differences**. The archive is the candidate tree by blob identity.

**2.2 Receipt leaves.** `shasum -a 256`: receipt `588f8736…`; authority `09a88873…` (= `authority-digest.bin` raw bytes, `xxd`); lock `0aa9bf4c…`; manifest `414c8032…`; capability `a2e21409…`; TLS cert `49a792b6…`; lease snapshot `a52dacbf…`; mac leaf `42038271…`; rig leaf `371a4f90…`; launch records ws/bulk `4329cfd4…`, ws/fanout `5fd69957…`, wt/bulk `54d05a9c…`, wt/fanout `10e96cd5…`; roles `server.ts` `aeef5d1b…`, `fanout-role.ts` `d9c3f971…`, `stage-live-campaign.ts` `2e3506d9…` (= `stageToolEntrypointSha256`); observation `directoryIdentitySha256 33709190…` = `linuxDirectoryIdentitySha256`, its `roleRootPath /tmp/ws-wt-linux-build.BgjyJo/tools/compare`. All equal the receipt. `incoming/` is `cmp`-identical to the staged originals (receipt, authority, digest, lock, manifest, capability, both leaves, TLS pair). `bin/` and `prebuilds/` empty; `linuxAddonManifestSha256` = `sha256("")`, `macAddonManifestSha256 7a4a58d6…` the recorded placeholder family.

**2.3 Role leaves vs the pilot.** `roles/server.ts` and `roles/fanout-role.ts` are byte-identical to `1b9e8092/fanout-pilot-r1/roles/` (`diff -r`: the only differing role file is `stage-live-campaign.ts`, and its diff is exactly the encoder hunks of this commit). On the rig, `/tmp/ws-wt-linux-build.BgjyJo/tools/compare/{server.ts,bin/fanout-role.ts}` → `aeef5d1b…`/`d9c3f971…`. The eight D4 preflight receipts under `preflight/1b9e8092…/` (`ws|wt × ticker-fanout_rate-100 × 1x|2x`, `ws|wt × chat-fanout_subscribers-1000 × 1x|2x`) all read `verdict PASS`, candidate `1b9e8092…`; since the diff touches no relay/transport/server/worker/controller/supervisor file and the staged role leaves and both supervisor binaries are the pilot's bytes, they describe this candidate's physics.

**2.4 Frozen run command.** `buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, receipt, paths from the preamble})` from HEAD (scratch `rebuild.ts`) → 32,378 B, `cmp` **byte-identical**, SHA `20ac06c4…`. `executionPurposeToSection("canonical")` → `{9.7, 16_200_000}` (`stage-live-campaign.ts:1097-1098`). Lines 666-680 `diff` against `frozen-run-section-9.7.fragment.sh` → identical; against the 9.6 fragment only the literals differ. Preamble (1-33): 13 `unset`s, `CANDIDATE 88b14a03…`, `RUN_TIMEOUT_MS=16200000`, `STAGE_NOT_AFTER_MS=1788995999123`, both leaf digests, capability/lock/archive/bound = receipt, keys at `…/keys/$CANDIDATE/$CAMPAIGN_ID.{mac,rig}.pk8`. Admission gates (519-528) run before the traps (532-535): leaf digests vs preamble, `STAGE_NOT_AFTER_MS − now > 16,200,000 + 5,400,000 = 21,600,000`, the sole `verify-stage-approval` over `$MAC_TRUST/exact-stage-approval.json` (absent now — correct). Section literals: the six cells in ladder order, `REPS=5`, `PURPOSE=canonical`, `CAMPAIGN_TIMEOUT_MS=14400000`, 60/60/12/6/60, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted` — 9.7 and D5 (16.2 M − 14.4 M = 1.8 M pairing). Controller argv (550-560) and the three post-render counts (623-635; the flat filter equals `RUN_CONTROL_FILENAMES`, unchanged in this diff) as the first review recorded.

Time budget: `Date.now()` 2026-09-09T03:30:55Z → remaining 71,343,769 ms > 21,600,000; the admission gate closes at **2026-09-09T17:19:59Z**; `notAfterMs` = 23:19:59Z. Rig `date +%s` 1788924521 vs Mac 1788924527 taken seconds apart — no skew of note.

**2.5 Hosts, keys, lease.** Mac runtime `/usr/local/libexec/webtransport-bun/comparison/88b14a03…/fanout-attested-r1/{comparison-supervisor,bun}` → `501f5d2a…` / `e0c90ec1…` = receipt (= `~/.bun/bin/bun`). `sudo -n -u _wtcompare` probe: `keys/88b14a03…/fanout-attested-r1.mac.pk8` **PRESENT** (0400, 83 B; `.pub` → `42038271…`; recovery pair beside it). `OUT` `.release-evidence/transport-comparison/88b14a03…/fanout-attested-r1` exists and is empty. Rig (`hermes-admin@10.99.0.2`, `~/.ssh/ubuntu-vm-hermes`): stage dir inode 8265806 / 8 links / 0755 / uid 1001 = the observation's `directoryIdentity`; `bin/comparison-supervisor` `84caa017…`, `observe-directory-identity` `fdd18545…`, `~/.bun/bin/bun` `9fd36f87…`; rig copies of receipt (`588f8736…`), authority, lock, manifest, capability equal; `keys/88b14a03…/fanout-attested-r1.rig.pk8` **PRESENT** (0400 `_wtcompare`, 83 B; `.pub` → `371a4f90…`); lease `leases/88b14a03…/fanout-attested-r1.lease.json` → `a52dacbf…` = staged snapshot, `state "armed"`, `notAfterMs 1788995999123`, `privateKeyPath` this key; no `.status.json`. `ss -ltnup`/`-lunp` → nothing on 4433; only the two long-lived `rig-min-*-echo-server` processes; `replay/{mac,rig}-records` empty on both hosts; `find $RIG_STAGE -name 'execution-*'` → 0; Mac `campaign-root/` holds exactly lock, reservation (`RESERVED`), manifest, r1-red bundle, source-archive receipt, ssh-host receipt; `/tmp` tmpfs 2.2 G free; rig load 0.29.

## 3. Carry-over from the first review, re-checked at this candidate

- **Wall clock.** Every constant the first review summed lives in `cohort-protocol.ts` / `compare-controller.ts`, neither in this diff: 76 s ticker / 156 s chat per execution (+30 s server-start), 72 executions → 2.92 h worst < 14,400 s; measured B5 records 15.9–16.4 s per ticker execution → ≈34 min expected. Verifier + render on 60 seals is seconds inside the 1,800 s pairing.
- **Un-preflighted rows ≤ 0.2.** The eight receipts' main-thread means are unchanged (rate-100 0.257/0.392, 0.263/0.405; chat-1k 0.177/0.576 with warmup 0.332, 0.179/0.601 with warmup 0.358); rate-25/50 and chat-250/500 interpolate to ≤ 0.14 and ≤ 0.18 at 1× on the same code.
- **72 O_EXCL execution records.** The Mac supervisor binary is the pilot's bytes (`501f5d2a…`), whose B5 root holds `execution-000001…000004`; B6 reaches 72 sequential indices; both campaign roots hold none now.
- **One FAIL rep fails closed.** Controller unchanged (`compare-controller.ts` not in the diff); at the verifier the canonical test now executes it: one demoted rep → `expectedPromotableCount` refusal / `5/6 paired promotions`; the wrapper then writes the integrity-only record and destroys both keys — no retry on this stage.
- **Older keys unreachable.** Key paths are `$CANDIDATE`-scoped (lines 39-45), the admission gate digest-checks this stage's leaves, and the bound now binds by recomputation: the first B6 stage's receipt refuses (executed, "minted for bound 161e27a3…"), as do the pilot's leaves and archive (1.2). The first B6 stage's keys at `1b9e8092…` are still PRESENT on both hosts (probe), plus the pilot keys at `efaed8bb…`/`ab6126a3…`/`b3c71552…` — housekeeping `abandon`, unreachable from this run.

## 4. Non-blocking findings and launch preconditions

1. **The quarantine's proof is structural, not cryptographic.** `checkPromotionQuarantine` accepts any well-formed `{sha256, recomputedSha256, method}` triple equal to the bound; a caller asserting `method:"marker"` with the bound copied twice is accepted (executed). Its only producer is `validateExternalTrustBound`, called once in the verifier — acceptable as an internal seam; worth a comment that the type is a claim the verifier alone may mint.
2. `promotionEligible` from `reconstructCohortEvidenceOffline` still has no production reader (`grep` → writers in `verify-artifact.ts` only); the hard conservation + `EXPECTED_TOTALS_MISMATCH` carry the enforced predicate, as the first review traced.
3. Wrapper `FAILURES` (command line 281) still lacks `DELIVERY_CONTEXT_MISMATCH` (`cross-supervisor-protocol.ts:166`) — a FAIL under a coarser code, never a PASS.
4. The controller's positional render leaves `report.md` beside the wrapper's `campaign-report.md`; harmless to every count.
5. **Mac load.** `sysctl vm.loadavg` read 4.27 (during my acceptance run) and 4.58 with no comparison process alive — above the preflight's 4.0 bound; the frozen command carries no load gate. Launch only with 1-min load < 4, no foreign `ps` storms, rig < 0.5 (0.29 now), and before 17:19:59Z.
6. The brief's "674 lines" is 680 by `wc -l`; the two `--stage-receipt` lines are 526 and 617 as stated.

## 5. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file after writing; the five bindings print with the values above (recorded in the review transcript).
