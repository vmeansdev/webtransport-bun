APPROVED

# Exact-stage Critic review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b), second stage

Reviewer: Claude workflow subagent, Critic pass, 2026-09-09 (03:35–03:55Z). Pre-traffic review of exact bytes. Git read-only (`git status --porcelain -uno | wc -l` → 0 before and after; `git rev-parse HEAD` → `88b14a03…` throughout). Nothing on the rig modified: ssh as `hermes-admin` ran `stat`, `sha256sum`, `ls`, `find`, `df`, `cat /proc/loadavg`, `date`, `ps`, `ss`, `systemctl list-unit-files` and `sudo -n -u _wtcompare` presence probes only. The frozen command was not run, the controller was not pointed at the staged pair, the preflight was not run. The only file written under the repo is this artifact; harnesses live in the session scratchpad (`x/encoder.ts`, `x/real-validate.ts`, `x/rebuild.ts`, `x/e/critic-real-seals.test.ts`). The Architect artifact (`reviews/architect.md`, SHA-256 `c2f6a6da8a60316819a2668eec7969c96ba2556461a7ef7cb10e729e70712f9c`) and both first-stage reviews under `../../1b9e8092…/fanout-attested-r1/reviews/` were read first and treated as untrusted; every claim below was re-executed here and the proving command is cited.

## Bindings

- Stage receipt SHA-256: `588f8736e0d6fced1c4f910a372fa5ca7610796c1e52573f7bf11957ddb90fe4`
- Upcoming run command SHA-256: `20ac06c4044794982f7c863165e3df49cc45615f33fa59b9908cc621199b3189`
- Candidate HEAD: `88b14a03fb01883f765aba3dae80b8cd19c3158c`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

`shasum -a 256` here, equal to the receipt: plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073`; approval `docs/superpowers/plans/approvals/2026-09-08-physical-budget-amendment.md` → `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`; deviations `2026-09-09-ticker-250-retired-by-preflight.md` → `ccc0964e…`, `2026-09-09-b6-verify-external-trust-bound.md` → `5c23bce5…`.

## Verdict

I tried to refute the fix and could not. The external trust bound now has one encoder; I reproduced all four live bounds with it (this stage `cac9d358…`, the abandoned first B6 stage `161e27a3…`, the B5 pilot `63ea97fa…`, the A5 receipt `34c2261e…`), the frozen 9.7 verifier recomputes the bound from the campaign index, the two staged leaves and the stage receipt before it opens a seal, every one of the ten preimage fields refuses on its own, and — the thing the first stage lacked — I drove the REAL verifier through the frozen 9.7 argv shape over the pilot's REAL cohort seals with the REAL cohort reconstruction, the REAL `checkExpectedTotals`, the REAL quarantine and a REAL receipt/leaf pair, and it answered `ok:true, 10/10, 2 flats, 1 pair`; the promoted render then produced both attested arms. The staged bytes are the candidate tree by blob identity, the run command is byte-identical to `buildFrozenRunCommand("9.7")` from HEAD, both keys are present with the lease armed, and every carried finding from the first review holds at this candidate. Section 4 has what remains non-blocking and the launch preconditions the operator must meet — the Mac's load (5.4–8.1 during this review, with no comparison process alive) is the one thing standing between these bytes and a launch.

## 1. The fix, attacked

**1.1 The diff is what the brief says.** `git -c diff.external= diff --no-ext-diff --stat 1b9e8092 88b14a03` → 10 files, +1284/−42, exactly the ten named; `git rev-parse 88b14a03^` → `1b9e8092…`. I read the production hunks in full (`cross-supervisor-protocol.ts` +49: a schema constant, an interface, a field list and `externalTrustBoundSha256` — purely additive; `output-policy.ts`: the quarantine's three-way branch; `verify-campaign-index.ts`: `validateExternalTrustBound` at `:533-646`, the call at `:852-870`, the quarantine hand-off at `:1045-1049`, the flag at `:1439`/`:1565`; `stage-live-campaign.ts`: `runMint` and `buildMinimalStageReceipt` now call the encoder; the wrapper fragment +5). Nothing under `crates/`, `scenarios/`, `server.ts`, `fanout-role.ts`, `compare-controller.ts`, `cohort-protocol.ts`, `verify-artifact.ts`.

**1.2 The encoder is order-independent and reproduces the live bounds.** `canonical.ts` sorts object keys (`Object.keys(record).sort()`, lines 23 and 36-37 of `canonicalizeValue`), so the encoder's insertion order cannot matter and the pre-fix inline literal produced the same bytes. Scratch `x/encoder.ts` (imports `externalTrustBoundSha256` and `EXTERNAL_TRUST_BOUND_PREIMAGE_FIELDS` from HEAD, feeds each receipt's own ten fields): this stage MATCH `cac9d358`, first B6 stage MATCH `161e27a3`, B5 pilot MATCH `63ea97fa`, A5 MATCH `34c2261e`; a one-nibble perturbation of any field changes the digest, 10/10 for each receipt (40/40).

**1.3 What binds the receipt, and what the recomputation does and does not bind.** Scratch `x/real-validate.ts` on the REAL staged receipt, REAL leaves (`sha256` of the raw 32-byte `.pub` files equals the receipt's two leaf digests: true/true) and the receipt's own anchors:

- real → `ok`, `recomputed cac9d358 == sha cac9d358`; the on-disk receipt IS `canonicalJson(value)+"\n"` (true).
- bound flipped / index candidate=`1b9e8092` / campaignId=`fanout-pilot-r1` / capability flipped / archive flipped / mac leaf flipped / rig leaf flipped / receipt = first B6 stage's / receipt = pilot's → all `EXTERNAL_TRUST_BOUND_MISMATCH`, each naming the field or the minted bound.
- receipt re-canonicalised with `authoritySha256`, `lockSha256`, `macDirectoryIdentitySha256` or `linuxDirectoryIdentitySha256` edited → `EXTERNAL_TRUST_BOUND_MISMATCH` ("recomputed as …"): these four fields come only from the receipt, and a substituted value would have to produce a SHA-256 preimage of the frozen bound.
- pretty-printed copy → "not the stage tool's canonical bytes"; symlink → "not a regular file"; absent; directory → all refuse.
- **receipt re-canonicalised with a NON-preimage field edited (`notAfterMs` +1 day, or `macSupervisorSha256` flipped) → `ok`.** The recomputation binds exactly the ten preimage fields, not the receipt as a whole. That is by design and it is safe here because (a) the admission gate binds the whole file: `verify-stage-approval` (`stage-live-campaign.ts:495-503`) hashes `$MAC_TRUST/stage-receipt.json` and refuses `EXACT_STAGE_APPROVAL_RECEIPT_DIGEST_MISMATCH` unless it equals the approval's `stageReceiptSha256`, and the verifier reads the SAME path (command lines 526 and 617 are both `$MAC_TRUST/stage-receipt.json`); (b) the verifier consumes no non-preimage receipt field; (c) the controller reads the receipt at start (`compare-controller.ts:1493-1518`) and writes `stagedCapabilitySha256`/`sourceArchiveSha256` into the index from it, which is what the verifier then holds the receipt to. Between admission and verification the file is `0444` operator-owned; an operator editing a non-preimage field mid-run changes nothing the verifier claims. Recorded as 4.1, non-blocking.
- Two stagings cannot collide: the preimage carries the candidate, campaign id, archive digest and both fresh leaves; the verifier takes candidate/campaignId/capability/archive from the index it is proving and the two leaf digests from the raw leaves it opened the signature graph with, and only then recomputes.

**1.4 The wrapper passes a path that exists at run time.** `MAC_TRUST` (line 21) is this staged directory; `$MAC_TRUST/stage-receipt.json` exists (`-r--r--r--`, 3,343 B, SHA `588f8736…`). Line 617 is the success verifier's `--stage-receipt`; line 526 is the admission gate's; `grep -n stage-receipt` also finds 143 (recovery record) and 215/223 (phase strings) — none other. The integrity-only verifier (lines 413-418) passes bound + `--integrity-only` and no receipt; `verify-campaign-index.ts:862-869` guards the `UNVALIDATED` refusal with `!integrityOnly`, and the unit suite pins it. The file is 680 lines (`wc -l`), not 674.

**1.5 The quarantine.** `bun -e` over the real B5 `ws` seal relabelled canonical/promotable/`fanout-attested-r1` with bound `cac9d358…`: no validation → `[EXTERNAL_TRUST_BOUND_UNVALIDATED]`; `recomputedSha256` differing → `[EXTERNAL_TRUST_BOUND_MISMATCH]`; `sha256` differing from the bound → `MISMATCH`; empty `method` → `UNVALIDATED`; a `{sha256:bound, recomputedSha256:bound, method:"marker"}` triple → `promotable:true` (4.1: structural). Callers (grep, non-test): `verify-campaign-index.ts:1045` passes the validation; `run-campaign.ts:1981/2057`, `bin/verify-artifact.ts:118`, `render-report.ts:375` pass none and keep the fail-closed answer.

## 2. A canonical seal passes on real bytes (the first stage's open proof)

Scratch `x/e/critic-real-seals.test.ts`: a one-cell root built the way the controller leaves one — the pilot's REAL `reps/ticker-fanout_rate-100/{ws,wt}/rep-1.sealed.json` re-sealed by the real `sealRunArtifact` with only `executionPurpose:"canonical"`, `promotable:true` and the campaign id changed, as reps 1–5 per arm, the rep-3 bytes as the two flats, a `campaign-index/v2` (canonical, one cell, `scheduledMeasuredArms 10`, anchors = the receipt's candidate/capability/archive), a `controller-terminal.json`; `parseVerifyCampaignIndexArgs` fed the frozen success argv (command lines 611-628 verbatim, one-cell counts 10/0/0/10/2/1/10, cells/arms/kinds/reps).

**E1 — the pilot's real trust, minimal stubs.** Trust = the REAL B5 stage receipt and REAL B5 staged leaves, campaign id `fanout-pilot-r1` (so the real bound `63ea97fa…` recomputes). With ONLY `verifyRunArtifact` replaced (`mock.module`; re-sealed bytes cannot pass their internal signatures) the REAL attestation graph refused: `CROSS_SUPERVISOR_MISMATCH execution executionPurpose: observed pilot, expected canonical` — the signatures verified and the relabel was caught, which is the honest reason it must be stubbed, and it means the quarantine, the real reconstruction and `checkExpectedTotals` had already passed for that seal (they precede the attestation in the loop, `:1045`, `:1090-1120`). With `verifyRunArtifact` and `verifyArmAttestationEvidence` replaced and `reconstructCohortEvidenceOffline` REAL under the staged leaves:

`{"ok":true,"passCount":10,"failCount":0,"refusedCount":0,"promotableCount":10,"sealedCount":10,"flatCount":2,"pairCount":1,"promotedCells":[],"canonicalFanoutComplete":false,"attestationsVerified":10}`

Mutations through the same argv: `--stage-receipt` dropped → `EXTERNAL_TRUST_BOUND_UNVALIDATED` with zero seals opened; bound flipped → `EXTERNAL_TRUST_BOUND_MISMATCH` ("minted for bound 63ea97fa…"), zero seals opened; one rep demoted (`promotable:false` re-sealed + index) → `expectedPromotableCount mismatch`.

**E2 — this stage's real trust.** Same root, campaign id `fanout-attested-r1`, anchors = THIS receipt's candidate `88b14a03…`/capability `a2e21409…`/archive `09390c30…`, the REAL staged leaves and the REAL `stage-receipt.json`, bound `cac9d358…`; the canonical test's three stubs (the B5-signed cohort cannot reconstruct under B6 leaves) → the same `ok:true` 10/10/2/1 answer; the same three mutations refuse identically with the B6 bound named.

**Render.** On the clean E1 root, `bun tools/compare/bin/render-campaign-report.ts --candidate=… --campaign-id=… --campaign-root=… --output=…/campaign-report.md` (the wrapper's promoted argv, lines 623-626) → rc 0; `^### (WS|WT) attested arm` → 2; per arm the topology line (`1 publisher / 8 workers / 100 subscribers / 101 sessions`), the totals line (`offered 1000, accepted 1000, relay writes 100000, delivered 100000, bytes 10000000, drain 0`), the three attested figures (busyMs 1932/1511 ms, main-thread 2680/2660 ms, process CPU 4650/10820 ms) and the claim-boundary sentence. The wrapper's own two counts on that root: 10 sealed, 2 flats.

**What remains unproven before 72 executions, stated plainly:** the other five rows' `checkExpectedTotals` and the six-cell `--expect-canonical-fanout-complete` branch have positive execution only on the tree's stub-shaped seals (`verify-campaign-index.canonical.test.ts`, 60/12/6 → `ok:true`, plus its mutation set); no real seal exists for rate-25/50 or chat-250/500/1000 in the promotable shape. Every one of those paths refuses closed (executed in the suite and in E1/E2), so the exposure is a wasted stage, never a false PASS. The controller, relay, roles and supervisors are the pilot's bytes (2.3 below), and the pilot's real seals cleared the whole seal-level graph under the frozen 9.6 verifier at 01:53Z.

## 3. The bytes, recomputed here

**3.1 Source identity.** `HEAD^{tree}` → `767b4209085f868f8794104e31939e2ad4b0a6f0` = `candidateTreeOid`. `source.tar` → `09390c30…`, 54,353,920 B, `tar -tf | wc -l` 2,072; inventory → `0f4964d3…`. Extracted to the scratchpad: 1,778 regular files, 0 symlinks; `git hash-object --stdin-paths` per path `diff`ed against `git ls-tree -r 767b4209` (1,778 blobs: 1,767×100644, 11×100755) → **zero differences**. `roles/{server.ts,fanout-role.ts,stage-live-campaign.ts}` → `aeef5d1b…`/`d9c3f971…`/`2e3506d9…` = `tools/compare/{server.ts,bin/fanout-role.ts,bin/stage-live-campaign.ts}` at HEAD = the receipt.

**3.2 Receipt leaves.** `shasum -a 256`: receipt `588f8736…`; command `20ac06c4…`; authority.json `09a88873…` = `xxd -p authority-digest.bin`; lock `0aa9bf4c…`; manifest `414c8032…`; capability `a2e21409…`; TLS cert `49a792b6…`; lease snapshot `a52dacbf…` (`state armed`, `notAfterMs 1788995999123`, `privateKeyPath` this key); mac leaf `42038271…`; rig leaf `371a4f90…`; launch records `4329cfd4…`/`5fd69957…`/`54d05a9c…`/`10e96cd5…`; observation `directoryIdentitySha256 33709190…`, `roleRootPath /tmp/ws-wt-linux-build.BgjyJo/tools/compare`. `incoming/` is `cmp`-identical to the staged originals (receipt, authority, digest, capability, both leaves, TLS cert and key). `bin/`, `prebuilds/`, `replay/{mac,rig}-records` empty; `campaign-root/` holds lock, reservation, manifest, r1-red bundle, source-archive receipt, ssh-host receipt — no execution records.

**3.3 Frozen run command.** Scratch `x/rebuild.ts`: `buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, receipt, paths from the preamble})` from HEAD → 32,378 B, `cmp` **byte-identical**, SHA `20ac06c4…`; `executionPurposeToSection("canonical")` → `{9.7, 16_200_000}` (`:1096-1097`); lines 666-680 `diff` against `frozen-run-section-9.7.fragment.sh` → identical (six cells in ladder order, `REPS=5`, `CAMPAIGN_TIMEOUT_MS=14400000`, 60/60/12/6/60, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted`). Preamble: 13 `unset`s, `RUN_TIMEOUT_MS=16200000`, `STAGE_NOT_AFTER_MS=1788995999123`, both leaf digests, capability/lock/archive/bound = receipt, `$CANDIDATE`-scoped key paths (lines 39-45). Admission gates (519-528) before the traps (532-535).

**3.4 Hosts, keys, lease.** Mac runtime `…/88b14a03…/fanout-attested-r1/{comparison-supervisor,bun}` → `501f5d2a…`/`e0c90ec1…` = receipt (= `~/.bun/bin/bun`). `sudo -n -u _wtcompare` probes: `keys/88b14a03…/fanout-attested-r1.mac.pk8` **PRESENT** (0400, 83 B), `.pub` → `42038271…`, recovery pair present; 46 candidate key dirs. `OUT` exists and is empty; `exact-stage-approval.json` absent (correct before approval). Rig: stage dir `inode 8265806 / 8 links / 0755 / uid 1001` = the observation; `bin/comparison-supervisor` `84caa017…`, `observe-directory-identity` `fdd18545…`, `~/.bun/bin/bun` `9fd36f87…`; rig copies of receipt/authority/lock/manifest/capability equal; role root `server.ts`/`fanout-role.ts` → `aeef5d1b…`/`d9c3f971…`; `keys/88b14a03…/fanout-attested-r1.rig.pk8` **PRESENT** (0400 `_wtcompare`, 83 B), `.pub` → `371a4f90…`; lease → `a52dacbf…` = snapshot, no `.status.json`; 43 key dirs; `ss -ltnup`/`-lunp` → nothing on 4433; `find $RIG_STAGE -name 'execution-*'` → 0; `replay/` → 0 files; `/tmp` tmpfs 2.2 G free with the 1,003 M role root resident; rig `date +%s` 1788925230 = Mac 1788925230 (no skew); rig load 0.00.

## 4. Carry-over from the first review, re-checked at this candidate

- **Wall clock.** `cohort-protocol.ts` and `compare-controller.ts` are not in the diff, so the first review's sums stand: 76 s ticker / 156 s chat per execution (+30 s server-start), 72 executions → 2.92 h worst < 14,400 s; B5 measured 15.9–16.4 s per ticker execution → ≈34 min expected; verifier + render on 60 seals is well inside the 1,800 s the 9.7 pairing leaves (E1 verified 10 cohort seals with real reconstruction in ~0.3 s).
- **Validity at a launch today.** `date +%s` 1788925439 (03:43:59Z) → remaining 70,560,123 ms > 21,600,000; the admission gate (`STAGE_NOT_AFTER_MS − now > RUN_TIMEOUT_MS + 5,400,000`) closes at **2026-09-09T17:19:59Z**; `notAfterMs` = 23:19:59Z.
- **Un-preflighted rows ≤ 0.2.** The eight D4 receipts under `preflight/1b9e8092…/` are unchanged and describe these bytes: role leaves, both supervisors and the controller are byte-identical to the pilot's (3.1, 3.4, the diff); the interpolation to rate-25/50 (≈0.09/0.14) and chat-250/500 (≈0.05/0.09) holds unchanged.
- **72 O_EXCL execution records.** Mac supervisor is the pilot's binary (`501f5d2a…`); both campaign roots hold none now.
- **One FAIL rep fails closed, no retry on this stage.** Controller unchanged; E1 executes the verifier's refusal on a demoted rep; the wrapper then writes the integrity-only record and destroys both keys.
- **Older keys unreachable.** Key paths are `$CANDIDATE`-scoped; the admission gate digest-checks this stage's leaves; the bound is now recomputed, and the first B6 stage's receipt and the pilot's receipt/leaves/archive each refuse by name (1.3). Still PRESENT: `keys/1b9e8092…/fanout-attested-r1.{mac,rig}.pk8` on both hosts and `keys/efaed8bb…/fanout-pilot-r1.mac.pk8` on the Mac — housekeeping `abandon`, unreachable from this run.
- **Mac ports.** `net.inet.ip.portrange` 49152–65535, `tcp.msl 15000`, 17 TIME_WAIT at review — unchanged from the first review's bound (peak ≤ 2,020 ≪ 16,384).

## 5. Non-blocking findings and launch preconditions

1. **The quarantine's proof is structural** (`output-policy.ts:728-758`): a well-formed `{sha256, recomputedSha256, method}` triple equal to the bound is accepted whoever minted it (executed with `method:"marker"`), and the recomputation binds the ten preimage fields, not the receipt file (1.3). The only producer is `validateExternalTrustBound`, called once, and the whole file is bound at admission by the approval digest — acceptable as an internal seam; a comment on the type saying the verifier alone may mint it would close the reading.
2. `promotionEligible` from `reconstructCohortEvidenceOffline` still has no production reader (`grep -rn promotionEligible` → `verify-artifact.ts` writers only).
3. Wrapper `FAILURES` (now command line 286) still lacks `DELIVERY_CONTEXT_MISMATCH` (`cross-supervisor-protocol.ts:135/166`; `grep -c` on the frozen file → 0) — a FAIL under a coarser code, never a PASS.
4. The lease names `janitorUnit wtcompare-rig-key-janitor@88b14a03…-fanout-attested-r1.service`; `systemctl list-unit-files 'wtcompare-rig-key-janitor@*'` on the rig lists none. Nothing on the run path consumes the name (grep of the wrapper, controller, remote-supervisor and `crates/native/src` → 0); the pilot's lease carried the same and passed. Informational.
5. The controller's positional render leaves `report.md` beside `campaign-report.md`; invisible to every count.
6. **Mac load — the operator gate.** `sysctl vm.loadavg` read 8.02 / 8.10 / 6.90 / 5.42 across this review, the last with no test of mine running and no comparison process alive (`ps` → 0, `lsof` 4433 → 0). No `ps axww` storm; the consumers are `WindowServer` 44 %, `Codex (Service)` 30 % + `Codex (Renderer)` 15 % (the ChatGPT app), three `xpcproxy_sim`, and the Claude helpers. The frozen command carries no load gate; the D4 receipts were taken at 3.0–3.5 against a 4.0 bound. Launch only when 1-min load < 4 with the ChatGPT/Codex app and simulators quiet, rig < 0.5 (0.00 now), no acceptance run in flight, before 17:19:59Z.

## 6. Executed here

- `bun test tools/compare/fanout-production-e2e.test.ts` twice: **19 pass / 0 fail / 393 expect, 196.88 s and 196.39 s, exit 0 both**; afterwards no supervisor/controller/role/server process and no 4433 listener.
- `bun test verify-campaign-index.canonical verify-campaign-index output-policy stage-live-campaign` → 172/0 (8.2 s); `r1-entrypoint-red r1-entrypoint-wiring fanout-promotion` → 111/0; `render-campaign-report mac-trust-access` → 27/0; `compare-controller` → 100/0. `bun run typecheck` → clean. `bunx biome check --diagnostic-level=error` on the eight changed TS files → clean, no fixes. `bun tools/compare/check-official-io.ts` → `failure-count=0`.
- Scratch harnesses: `x/encoder.ts` (1.2), `x/real-validate.ts` (1.3), `x/rebuild.ts` (3.3), `x/e/critic-real-seals.test.ts` in three modes (E1 minimal, E1 with attestation stubbed, E2) including the render, the archive extraction + blob diff, and the `bun -e` quarantine probe (1.5).
- Not executed: `cargo test`, the full `bun test` (no Rust file is in the diff; the two B5 reviews report the full batches at the parent commit).

## 7. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file after writing; the five bindings print with the values in the bindings block (recorded in the review transcript).
