APPROVED

# Architect exact-staged-artifact review — B5 pilot (base plan B5 / §9.6, amendment slice 7)

Reviewer: Claude (Architect pass), 2026-09-08. Pre-traffic review of exact bytes; git read-only; nothing modified on either host; the frozen command was not run and the controller was never pointed at the real staged directory.

## Bindings

- Stage receipt SHA-256: `3470ce63d685fd93452a7202088e27d22131805e642054e304e12f46740bc4fb`
- Upcoming run command SHA-256: `a6959bb32b89003bb66fce96c0940ec1a1ae320a86fc0388f8dbae77afda3f42`
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-pilot-r1`

## 1. Identity of the staged bytes (all recomputed by execution)

- `git rev-parse HEAD` = efaed8bb…41c5; `git status --porcelain -uno | wc -l` = 0; `git rev-parse HEAD^{tree}` = 8eb3c2769400e12b51bf190ace9d79bb8235e85b. The receipt's `candidate`, `candidateHead` and `candidateTreeOid` equal these three values.
- `shasum -a 256` over the staged files reproduced every receipt digest: stage-receipt.json 3470ce63…, upcoming-run-command.sh a6959bb3… (675 lines, 31,865 bytes — the brief said 669; the digest is what binds), source.tar 1f737d61… (53,504,000 bytes, `stat -f %z`), archive-member-inventory.txt 14378a0e… (2045 lines), authority.json 1a4009b3…, staging-root/mac-supervisor-ed25519.pub 3c253ffc…, staging-root/rig-supervisor-ed25519.pub b50822d4…, rig-signing-key-lease.armed.json 3eee9502…, staged-capability.json ad97b522…, campaign-root/campaign-lock.json aafc0452…, campaign-root/manifest.json 1c70ac09…, staged-server-tls.crt c175df18…, the four launch records 528eb0aa… / 62d82535… / dcdfe852… / 96d86bbd…, roles/server.ts 36a8fcd0…, roles/fanout-role.ts 37b6a5e8…, roles/stage-live-campaign.ts 7efc3286….
- Plan and approval: `shasum -a 256 docs/superpowers/plans/2026-09-05-cohort-completion-amendment.md` = 90d469cb… (receipt `approvedPlanSha256`); `docs/superpowers/plans/approvals/2026-09-05-cohort-completion-amendment.md` = 6e66236a… (receipt `approvalRecordSha256`; the record itself binds plan digest 90d469cb…). authority.json carries both under `approval`.
- Archive = commit by object identity: extracted source.tar to the scratchpad (outside the repo), ran `git hash-object` on all 1751 regular files and diffed the (oid, path) list against `git ls-tree -r HEAD`: 1751 vs 1751, `diff` exit 0. Member count 2045 = 1751 files + 294 directories; 0 symlinks. File modes also match (`tar -tvf` x-bit vs git 100755/100644: 11 executables, diff exit 0).
- Role leaves: worktree `tools/compare/server.ts` 36a8fcd0…, `tools/compare/bin/fanout-role.ts` 37b6a5e8… (the brief's `roles/fanout-role.ts` path is the staged leaf name; the source lives at `bin/`), `tools/compare/bin/stage-live-campaign.ts` 7efc3286… — identical to the staged leaves and to the archive (archive == HEAD). On the rig (`sha256sum` over ssh, read-only): `/tmp/ws-wt-linux-build.03pyUO/tools/compare/server.ts` 36a8fcd0…, `bin/fanout-role.ts` 37b6a5e8…, `bin/stage-live-campaign.ts` 7efc3286…, and the rig stage `roles/*` copies hash the same. Rig stage dir inode 8265612 uid 1001 mode 755 ext4 matches `linuxDirectoryIdentity`/authority `linux-staging`; Mac campaign-root inode 381644901 mode 700 and staging-root inode 381644905 match authority `mac-campaign`/`mac-staging`.
- Cross-record binding: manifest.json, campaign-lock.json, staged-capability.json, authority.json, source-archive-receipt.json (archive 1f737d61…, tree 8eb3c276…), ssh-host-receipt.json (10.99.0.2:22, linuxSupervisorSha256 c742c9cb…), linux-stage-observation.json and the rig lease all name candidate efaed8bb… and this campaign id; lock ↔ manifest ↔ capability ↔ authority digests cross-reference (lockSha256 aafc0452…, capabilitySha256 ad97b522…, authoritySha256 1a4009b3…, r1RedApprovalBundleSha256 ff4644a7…, sourceArchiveReceiptSha256 4997de06…). The rig stage copies of manifest/lock/capability/authority/authority-digest.bin hash identically to the Mac's.

## 2. The run command is the plan's §9.6 and is built from HEAD

- Rebuilt with `buildFrozenRunCommand({section:"9.6", executionPurpose:"pilot", stageReceipt:<staged receipt>, …})` from HEAD (`tools/compare/bin/stage-live-campaign.ts`, wrapper fragment 80848ca5… + `frozen-run-section-9.6.fragment.sh` 113be81d…) and `cmp`'d against the staged file: BYTE_IDENTICAL, digest a6959bb3….
- Read in full. Admission gates (lines 519–528: both public leaf digests vs the pinned values, `STAGE_NOT_AFTER_MS - now > RUN_TIMEOUT_MS + 5,400,000`, and the sole `verify-stage-approval` call over receipt + command + `exact-stage-approval.json`) precede the first trap (lines 532–535), so a refusal there leaves both campaign keys intact. Margin now: 70,514,872 ms remaining vs 7,500,000 required.
- Section values are the plan's §9.6: `CELLS=ticker-fanout/rate-10000`, `REPS=1`, `PURPOSE=pilot`, `CAMPAIGN_TIMEOUT_MS=1800000`, `EXPECTED_PASS=2`, `EXPECTED_PROMOTABLE=0`, `EXPECTED_FLATS=0`, `EXPECTED_PAIRED_PROMOTIONS=0`, `RENDER_MODE=diagnostic`, `RUN_TIMEOUT_MS=2100000`, `EXECUTION_PURPOSE=pilot`; the two additions the plan block lacks (`EXPECTED_SEALED=2`, `EXPECT_CANONICAL_FANOUT_COMPLETE=0`) are recorded in `deviations/2026-09-02-wrapper-expected-counts.md` §3–4. Controller argv (lines 550–560) carries `--stage=full --staged-dir="$MAC_TRUST" --arm-kinds=primary --campaign-timeout-ms=1800000 --write-terminal-record`. Success-path verifier argv (607–624) carries both staged public leaves, `--expected-*` counts, and the registered topology flags `--expect-cells/--expect-arms=ws,wt/--expect-arm-kinds=primary/--expect-measured-repetitions=1` (deviation 2026-09-06-verify-campaign-index-registered-topology.md); no `--expect-canonical-fanout-complete` (pilot). Diagnostic render (640–644) is `--source=sealed-index --allow-non-promotable`; render-campaign-report.ts:235 prints `NON-PROMOTABLE PILOT EVIDENCE` for purpose `pilot`. Traps INT/TERM/HUP/EXIT, integrity-only fallback (`--integrity-only`, no expectations, no keys), and `cleanup_signing_keys` destroying exactly the two campaign PKCS#8 files with owner-run absence probes are the §9.8 shape. The 13 ambient names cleared at the top match `AMBIENT_ENV_CLEARED_BY_FROZEN_RUN` (includes `COMPARISON_MAC_SUPERVISOR_UID_SEAM`).

## 3. Cell → cohort grant (executed)

- `evidence.ts cohortCellForArm({cellId:"ticker-fanout/rate-10000", armKind:"primary"})` → `"ticker 10k"` (read-path → null). `cohort-protocol.ts cohortCellCardinality("ticker 10k")` → `{publisherCount:1, workerCount:8, subscriberCount:100, sessionCount:101, measuredIngress:100000, expandedDeliveries:10000000}`; `COHORT_WORKER_COUNT` = 8; `COHORT_CELL_GRANT_PARAMETERS` row → `measuredDurationMs:10000, messageBytes:100`.
- Controller derivation: compare-controller.ts:4617–4640 builds the role-plan preimage from those two tables (publisherRatePerSecond = 100000/1/10 = 10000, payloadBytes 100, warmup 10 msgs @ 500 ms); :663–670 declares `fanout-expanded-deliveries` = 10,000,000. The signed `cohort-grant/v1` is minted by the Mac supervisor binary from the Rust cell table (crates/native/src/secure_fs.rs:19573–19586: ticker-fanout/rate-10000 → 1/8/100/101/100_000/10_000_000/10_000 ms/100 B/30 s readiness; mint at :21790–21826 sets `workerCount` = SUBSCRIBER_SHARD_MODULUS, `expectedProcessCount` = 1+8, `expectedSessionCount` = 1+100, `expectedOfferedIngress`/`expectedExpandedDeliveries` from the row). The `expectedOfferedIngress: 100` sites in comparison-supervisor.rs:4241/4982 are inside `#[cfg(all(test, not(windows)))] mod resident_admission_tests` (:3160–3161).
- CohortGrantV1 schema (cohort-protocol.ts:701–740) carries exactly these fields plus the frozen literals (500 conn/s, 200 in flight, 5000 ms warmup, 1000 ms windows, 10000 ms drain).

## 4. What the frozen verifier PROVES vs what it only reports (the A5 finding, re-examined for B5)

- The transport/armKind/repetitionKind blindness is closed by `proveRegisteredTopology` (deviation 2026-09-06): the pilot index must contain exactly one measured PASS per (cell, ws|wt, primary) with repetitionTotal 1, non-promotable.
- Topology cardinalities ARE proved by the verifier over the sealed artifacts, not asserted by argv: verify-campaign-index.ts:772–802 calls `verifyRunArtifact` with both staged public keys; verify-artifact.ts:4216 → `reconstructCohortEvidenceOffline`, which refuses under closed codes when the signed grant is not the frozen row (`COHORT_CARDINALITY_MISMATCH` :3704–3713 for publisher/subscriber/worker=8/sessions; `COHORT_GRANT_DECLARATION_INVALID` :3715–3722 for expectedOfferedIngress 100000 / expectedExpandedDeliveries 10000000), when the process proof is not 9 processes / 100 subscribers (:3808–3817), and when capacity or the Linux observation is not 101 sessions accepted / 101 active peak / 1 publisher / 100 subscribers (:3976–3990, :3992–3999). Any of these turns the seal non-PASS and the campaign verifier returns TRUST_PROTOCOL.
- The OBSERVED totals are NOT a refusal for a pilot: verify-artifact.ts:4141–4149 compares `offeredIngressTotal`, `serverAcceptedIngressTotal` (=100,000) and `deliveredTotal` (=10,000,000) — and per-window conservation (:4124–4140) — only to set `promotionEligible = false`. A pilot seal never claims promotion, so a run that delivered fewer than 10,000,000 records still verifies PASS. The diagnostic render (render-campaign-report.ts:163–277) prints the banner, campaign/candidate/purpose/stage, a seal-count sentence and a per-arm table (cell, arm, transport, status, promotable, attestation, sealed path, p50) — it does not print the topology or the ingress/delivery totals, so §9.6's "prints the exact ticker topology/cardinalities" is not met by the render and there is no deviation record for it.
- Consequence for the B5 stop gate: the "1 publisher / 8 workers / 100 subscribers / 101 Linux sessions" clauses are proved by the frozen verifier; the "100,000 offered/accepted ingress and 10,000,000 Linux-written/worker-delivered records for both arms" clauses must be evaluated by the lead AFTER the run by executing `reconstructCohortEvidenceOffline` over both sealed artifacts with the staged public leaves (exactly what fanout-production-e2e.test.ts:1083–1136 does: `conservation.offeredIngressTotal`, `serverAcceptedIngressTotal`, `deliveredTotal`, `linuxObservation.sessionsAccepted`, `processProof.*`) and recording the numbers with the stop-gate decision. This is a post-run evaluation obligation, not a launch condition: the seals carry the evidence in full, the render is a pure function of index+seals and can be regenerated later without touching sealed bytes, and a render-only source change would force a new candidate, a fresh A5 and a fresh B5 stage for no change in what the campaign measures. A deviation record should be written for the render gap.

## 5. Launch records, runtime binaries, keys, roots

- The four launch records (ws/wt × bulk-source/fanout-cohort) are `server.ts --transport=<t> --mode=<m> --stage-profile=phase-b --bind=10.99.0.2`, bind 10.99.0.2:4433, bun 9fd36f87…, serverEntrypointSha256 36a8fcd0…, TLS cert c175df18… / key digest 751453d8… (the staged key on the rig is 0400 `_wtcompare`), env PATH=/usr/bin:/bin. `stageReceiptSha256` is the constant zero placeholder by construction (server.ts:218) because the receipt is minted after the records and binds them (`stagedServerLaunchRecordSha256ByLaunch`); the rig reads only transport/argv/bindPort/TLS/env off the record (secure_fs.rs:16125–16145) and re-hashes `argv[0]` through the pinned role-root handle against `serverEntrypointSha256` before any fork (comparison-supervisor.rs:1920–1945). The receipt's `rigRoleRootPath` is opened by the controller as the rig's fd 10 `cohort-role-root` (compare-controller.ts:3080–3084) and the stage receipt refuses a non-absolute path (:4293–4296).
- Role children (1 publisher + 8 subscriber workers, 101 sessions to the Linux server) are spawned on the Mac by `createMacFanoutRoleChildHost` (compare-controller.ts:6643–6656) on `tools/compare/bin/fanout-role.ts`, admitted only when its bytes hash to the receipt's `fanoutRoleEntrypointSha256` (`executableRoleEntrypoint`, :6858–6872) and the staged `roles/fanout-role.ts` leaf is digest-checked at stage verification (:4446–4455). Working-tree bytes = 37b6a5e8… as required.
- Mac runtime `/usr/local/libexec/webtransport-bun/comparison/efaed8bb…/fanout-pilot-r1/{bun,comparison-supervisor}` = e0c90ec1… / c669b3fd… (receipt macBun/macSupervisor); `MAC_BUN=/Users/vmeansdev/.bun/bin/bun` is the same bytes (e0c90ec1…, 1.3.14). Rig stage `bin/comparison-supervisor` c742c9cb…, `bin/observe-directory-identity` e2e91ae8… (receipt/observation).
- Keys: `sudo -n -u _wtcompare test -e` → PRESENT `/var/db/webtransport-bun/comparison/keys/efaed8bb…/fanout-pilot-r1.mac.pk8` (Mac; plus the durable `.mac-recovery.pk8`) and PRESENT `/var/lib/webtransport-bun/comparison/keys/efaed8bb…/fanout-pilot-r1.rig.pk8` (rig); rig lease at `/var/lib/…/leases/efaed8bb…/fanout-pilot-r1.lease.json` is byte-identical to the staged `rig-signing-key-lease.armed.json` (state `armed`, notAfterMs 1788927447872, rigPublicKeySha256 b50822d4…). A5's campaign keys under the same candidate are ABSENT on both hosts (destroyed by its run); its lease file remains, under a different campaign id.
- Abandoned roots: every other candidate directory on both hosts (Mac `/usr/local/libexec/webtransport-bun/comparison/*`, keys dir, rig `~/ws-wt-stage/*`) is keyed by a different candidate or campaign id; the run pins both key paths, both public leaf digests, the authority digest and the staged dir by candidate+campaign, and the verifier opens the signature graph only with the two staged leaves, so none of them can reach this campaign.
- Rig state (read-only): load 0.05, 8 vcpu, 6.7 GB available, `/tmp` tmpfs 62% (2.4 GB free); port 4433 free (`ss` shows only the unrelated Aug-29 echo servers on 4446/4447). `_wtcompare` soft nofile 1024 / hard 524288; no code raises RLIMIT_NOFILE (grep over supervisor/secure_fs/fanout-role/controller/server: none). Budget: the rig runs the supervisor + one server child holding 101 sessions (WS: ~101 sockets; WT: one UDP endpoint) — well inside 1024; the Mac hosts the nine role children (≤13 sessions each). The role root and the addon it contains are readable by `_wtcompare` (`sudo -n -u _wtcompare test -r`).

## 6. Refusal of a nonexistent staged directory (executed)

`bun tools/compare/bin/compare-controller.ts --cells=ticker-fanout/rate-10000 --reps=1 --execution-purpose=pilot --candidate=efaed8bb… --campaign=architect-probe-nonexistent --stage=full --staged-dir=<scratchpad>/probe/does-not-exist --arm-kinds=primary --campaign-timeout-ms=1800000 --write-terminal-record=<scratchpad>/probe/controller-terminal.json` → stderr `controller real-run: REFUSED/STALE_OR_INVALID_STAGING: staged-dir verify failed (STAGE_VERIFY_FAILED): staged trust bootstrap missing …/does-not-exist/authority.json`, exit 4; terminal record `{"campaignStatus":"REFUSED","refusalCode":"STALE_OR_INVALID_STAGING","failureCode":null,"terminalKind":"REFUSED","trafficStarted":false,"controllerExitCode":4}`; no evidence directory was created under the repo; `git status --porcelain -uno` still 0. `realRun` performs this verify before any rig contact (compare-controller.ts:2900–2913).

## 7. Local real-process acceptance (executed)

`bun test tools/compare/fanout-production-e2e.test.ts` → 19 pass / 0 fail, 393 expect() calls, 197.62 s, on a Mac at load ≈ 5–7 (an unrelated build was not running; this test itself is the load). The positive four-execution run is chat-fanout/subscribers-1000 (2 warmup + 2 measured, 18 processes, 1010 sessions, 300,000 deliveries per arm, both issuer graphs closed under the staged keys, `verifyCampaignIndex` with expected 2/0/0/0/2). The pilot cell itself appears only in the negative test (`a_role_child_that_never_reaches_readiness…`, a ticker-10k dispatch with a role child stopped, expecting `READY_DEADLINE_EXCEEDED`); the `CHILD_LIFECYCLE: server teardown` and `accept_uni … delivery failed` lines at the end of the log belong to that deliberate teardown. So: the phase-b machinery is complete and reachable end to end over real supervisors and real role children on loopback for a larger session count than the pilot; a sustained 10,000 msg/s ticker window has never been driven positively on either loopback or the rig at this HEAD.

## 8. Findings (none blocking)

1. Render gap vs §9.6 (section 4 above): the diagnostic report does not print topology/cardinalities; the ingress/delivery totals gate only promotion. Post-run obligation on the lead: evaluate those stop-gate clauses by offline reconstruction over both seals and record a deviation. Do not read the verifier's `2 PASS` as proof of 100,000/10,000,000.
2. Rig key janitor is record-only: the lease names `wtcompare-rig-key-janitor@efaed8bb…-fanout-pilot-r1.service`, but no such unit, template, timer or `_wtcompare` user-manager job exists on the rig (`systemctl list-unit-files "wtcompare*"` → 0; unit status "could not be found"; no lingering user manager), and no code arms one (`janitor` occurs only where the string is minted, stage-live-campaign.ts:1327; nothing in the Rust binaries). Plan §9's "deletes the rig key at expiry even with no controller" is not implemented; for this campaign the run wrapper's `cleanup_signing_keys` (and `abandon` if not launched) is the only destruction path. Not a launch defect; a deviation is owed.
3. Addon digests bind nothing: `linuxAddonManifestSha256` = `addonSha256` in all four launch records = SHA-256 of the empty string, because `hashAddonManifest` (stage-live-campaign.ts:956–970) hashes an empty `prebuilds/` directory. The addon the rig will actually load is `/tmp/ws-wt-linux-build.03pyUO/crates/native/webtransport-native.linux-x64-gnu.node` = b89de22b1152ec769b7de28a90f8b95efff4203d45c0df5368939d6d074718fa (built at 08:21:42Z from the same archive), and the Mac role children load the worktree's `crates/native/webtransport-native.darwin-arm64.node` = 6e546c2f19e15d31bc1cfa08b9dca29d32fc4729e00b2d4a4ac308a4119e526d; `macAddonManifestSha256` (cd8028a4…) was computed over a transient build dir that stage-only deletes (:3425–3427) and cannot be reproduced by review. Nothing at run time consumes these digests (the rig reads three fields off the launch record). Identical state to the A5 receipt that passed WT traffic on the rig today; recorded here so the campaign can be tied to those two addon digests post hoc. Placeholder-evidence family; a deviation is owed.
4. Warmup-deadline sensitivity to Mac load: the deadline is `WARMUP_DURATION_MS 5000 + warmupDrainMs 6000` (cohort-protocol.ts:115, compare-controller.ts:7427) and the role children run on the Mac. The lead's one loaded-Mac miss was on this path. Launch only on an idle Mac (no builds, tests or stage in progress); the wrapper's 35-minute budget has no retry.

## 9. Verdict

The staged bytes are the B5 pilot the approved plan and amendment describe: the archive is the candidate commit by object identity, every digest in the receipt and the cross-record graph recomputes, the run command is byte-for-byte HEAD's §9.6 with admission before traps, the grant the run will sign derives to 1/8/100/101/100,000/10,000,000/10 s/100 B from both the TS and Rust tables, the verifier proves the topology from the sealed cohort evidence under the staged keys, both campaign keys are present and the rig lease is armed, and the production entry refuses a nonexistent stage with a typed refusal. Approved for launch of these exact bytes, with the four non-blocking findings above carried to the post-run evaluation.
