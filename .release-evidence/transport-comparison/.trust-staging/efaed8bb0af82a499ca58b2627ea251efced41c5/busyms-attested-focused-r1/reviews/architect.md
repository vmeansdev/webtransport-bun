APPROVED

# Architect review: exact staged artifacts, thirteenth stage (candidate efaed8bb)

Reviewed 2026-09-08 (review-time epoch 1788853740 s) by execution, pre-traffic, git read-only, nothing modified on either host. The frozen command was never launched and `compare-controller.ts` was never pointed at the real staged directory.

## Bindings

- Stage receipt SHA-256: `75f24efa81bdb56eb70e8b213daf5931cfc3d174847278e6acf8a9be68ad50e8`
- Upcoming run command SHA-256: `25e1f3cd291da5709a14c6beef76da160a34325f25e47ef5bb8d85b156795896`
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `busyms-attested-focused-r1`

## 1. The staged bytes are the candidate commit

- `git rev-parse HEAD` = `efaed8bb0af82a499ca58b2627ea251efced41c5`; `git rev-parse HEAD^{tree}` = `8eb3c2769400e12b51bf190ace9d79bb8235e85b`; `git status --porcelain -uno | wc -l` = 0. Receipt fields `candidate`, `candidateHead`, `candidateTreeOid` equal these (`cat stage-receipt.json`).
- `shasum -a 256` recomputed: stage-receipt.json 75f24efa…, upcoming-run-command.sh 25e1f3cd…, source.tar 1f737d61… (`stat -f %z` = 53504000; `tar -tf | wc -l` = 2045), archive-member-inventory.txt 14378a0e…, staging-root/mac-supervisor-ed25519.pub 46cda81f…, staging-root/rig-supervisor-ed25519.pub 3d9bbf7c…, authority.json b85a9bc4…, campaign-root/manifest.json b9f32f2b…, campaign-root/campaign-lock.json 1bedb210…, staging-root/staged-capability.json af9df8f7…, staging-root/rig-signing-key-lease.armed.json 72a9aca0…, tls/staged-server-tls.crt a4b54461…, roles/server.ts 36a8fcd0…, roles/stage-live-campaign.ts 7efc3286…, staged-server-launch-record.{ws,wt}.bulk-source.json b98e48f0… / 58e1c402…. Every value equals the receipt. `xxd -p authority-digest.bin` = b85a9bc4… (the authority digest as raw bytes). `incoming/` carries byte-identical copies of the receipt, leaves, authority, lock, manifest, capability and TLS material (same digests).
- Plan digests: `shasum -a 256 docs/superpowers/plans/2026-09-05-cohort-completion-amendment.md` = 90d469cb… = `approvedPlanSha256`; `docs/superpowers/plans/approvals/2026-09-05-cohort-completion-amendment.md` = 6e66236a… = `approvalRecordSha256`; the amendment's bound base plan `2026-08-30-busyMs-attested-fanout.md` hashes to 9374b747… as the amendment states. authority.json `approval.*` and campaign-root/r1-red-approval-bundle.json (`redHead` efaed8bb, `redSuiteSha256` 6e66236a…) carry the same two digests.
- Archive equals HEAD by object identity: extracted source.tar into the scratchpad (outside the repo), ran `git hash-object` on all 1751 regular files (0 symlinks), sorted, and `diff`ed against `git ls-tree -r HEAD --format='%(objectname) %(path)'`: empty diff, same 1751 paths. tools/compare/server.ts in the archive and in the worktree both hash to 36a8fcd0… = receipt `serverEntrypointSha256` = both launch records' `serverEntrypointSha256` = linux-stage-observation `serverEntrypointSha256` and `roleRootServerEntrypointSha256`.
- Derived digests recomputed with `canonicalJson` from tools/compare/canonical.ts: `macDirectoryIdentitySha256` from authority root `mac-staging` = 4c523780… (match); `linuxDirectoryIdentitySha256` from linux-stage-observation `directoryIdentity` = cd92d0ad… (match, and equals the observation's own field); `externalTrustBoundSha256` over `{schema:"external-trust-bound/v1", candidate, campaignId, authoritySha256, capabilitySha256, lockSha256, archiveSha256, macSigningPublicKeySha256, rigSigningPublicKeySha256, macDirectoryIdentitySha256, linuxDirectoryIdentitySha256}` (stage-live-campaign.ts:2434-2447) = 34c2261e… (match). All twelve staged JSON records are canonical bytes (receipt, observation, lease and launch records with one trailing LF; authority/manifest/lock/capability/receipts/bundle without).
- Cross-record binding: manifest ↔ lock ↔ capability ↔ authority all carry candidate efaed8bb, the same campaign id, `authoritySha256` b85a9bc4…, `lockSha256` 1bedb210…, `capabilitySha256` af9df8f7…; lock and capability carry `sourceArchiveSha256` 1f737d61…, `sourceArchiveReceiptSha256` 756a8612… (= sha of campaign-root/source-archive-receipt.json, whose `finalCandidateHead`/`finalCandidateTreeOid` are HEAD/tree), `r1RedApprovalBundleSha256` d8f6311c… (= sha of the bundle); capability `sshHostReceiptSha256` 69fdaee7… = sha of campaign-root/ssh-host-receipt.json = authority `topology.sshControlReceiptSha256`; ssh-host-receipt `linuxSupervisorSha256` c742c9cb… = receipt = authority `source.linuxSupervisorSha256`; authority `campaignReservationSha256` b614c46a… = sha of campaign-root/campaign-reservation.json; capability/authority `notAfter` 2026-09-09T03:30:39.331Z = receipt `notAfterMs` 1788924639331 = lease `notAfterMs`.
- Live directory identities: `stat -f` on the staged tree gives campaign-root inode 381618389 mode 700 links 8 (= authority `mac-campaign`), staging-root inode 381618393 mode 750 links 10 (= `mac-staging`), bin inode 381618388 mode 750 links 2 (= `mac-exec-parent`); `./target/release/observe-directory-identity staging-root` reproduces the `mac-staging` identity field for field. On the rig `stat -c "%i %u %g %a %h"` of the stage dir = `8265575 1001 1001 755 8` = authority `linux-staging` / observation `directoryIdentity`.

## 2. The frozen command is the plan's, and it executes what I verified

- Rebuilt with `buildFrozenRunCommand({section:"9.5", …})` from the worktree's `frozen-run-wrapper.fragment.sh` (sha 80848ca5…, blob 642fc892) and `frozen-run-section-9.5.fragment.sh` (sha 429b413e…, blob 8ed00191) with the receipt and the literal paths: `text === frozen` is true and its sha is 25e1f3cd…. So the frozen bytes are HEAD's fragments and preamble, nothing hand-edited.
- Read all 675 lines. Literals: REPO = this worktree; CANDIDATE efaed8bb; EXECUTION_PURPOSE focused; RUN_TIMEOUT_MS 4200000; OUT = `$REPO/.release-evidence/transport-comparison/efaed8bb…/busyms-attested-focused-r1` (exists, empty, 0700, created by `freeze-run-command` at stage-live-campaign.ts:2722); MAC_TRUST = the staged dir reviewed here; MAC_RUNTIME = `/usr/local/libexec/webtransport-bun/comparison/efaed8bb…/busyms-attested-focused-r1`; RIG hermes-admin@10.99.0.2; RIG_STAGE `/home/hermes-admin/ws-wt-stage/efaed8bb…/busyms-attested-focused-r1`; both public-leaf digests, STAGE_NOT_AFTER_MS, CAPABILITY/LOCK/ARCHIVE/EXTERNAL_TRUST_BOUND digests all equal the receipt; the 13 ambient env names of `AMBIENT_ENV_CLEARED_BY_FROZEN_RUN` are unset first. `grep -oE '[0-9a-f]{40}'` over the file yields only efaed8bb plus prefixes of the six digests above: no foreign candidate can be named by these bytes.
- Section fragment (lines 661-675) = base plan §9.5: `CELLS=bulk-one-way/physical`, `REPS=1`, `PURPOSE=focused`, `CAMPAIGN_TIMEOUT_MS=3600000`, `EXPECTED_PASS=2`, `EXPECTED_PROMOTABLE=0`, `EXPECTED_FLATS=0`, `EXPECTED_PAIRED_PROMOTIONS=0`, `RENDER_MODE=diagnostic`, plus `EXPECTED_SEALED=2` and `EXPECT_CANONICAL_FANOUT_COMPLETE=0` recorded by deviations 2026-09-02-wrapper-expected-counts and 2026-09-06-verify-campaign-index-registered-topology. `ARMS=ws,wt`, `ARM_KINDS=primary`. With `--expected-fail-count=0 --expected-refused-count=0` hard-coded, the success verifier encodes exactly the A5 stop gate: two measured primary PASS entries, zero promotable, zero flats, zero pairs, two sealed, cells/arms/arm-kinds/reps pinned, both staged public leaves passed so the signature graph is verified, not counted.
- Files it executes: after `cd "$REPO"` (line 663) it runs the worktree's `tools/compare/bin/compare-controller.ts` and `render-campaign-report.ts` (relative), and `"$REPO/tools/compare/bin/verify-campaign-index.ts"` and `"$REPO/tools/compare/bin/stage-live-campaign.ts"` (absolute); the inline `bun -e` parsers import `$REPO/tools/compare/{secure-fs,canonical}.ts`. Every one of those is HEAD by the object-identity diff above and `git status --porcelain -uno` = 0 lines. `verify-campaign-index.ts` and `render-campaign-report.ts` accept every flag the command passes (grep of each flag name in both files: all present). The Mac runtime binaries it exports are the receipt's: `shasum` comparison-supervisor c669b3fd…, bun e0c90ec1….
- Admission ordering, proved on the exact frozen bytes: lines 519-528 are the three read-only gates (both leaf re-hashes, validity window, the sole `verify-stage-approval` with `--stage-receipt/--upcoming-run-command/--exact-stage-approval` under `$MAC_TRUST`); the first `trap` is line 532 and `trap on_exit EXIT` line 535; `cleanup_signing_keys` is called only from `on_exit` (line 504). I executed a copy of the frozen file in the scratchpad in which only the six literal assignment lines and the `/usr/bin/sudo` path were rewritten (`diff` shows nothing else), with recording stubs for sudo/ssh/bun: (S1) `verify-stage-approval` exit 3 → wrapper exit 3, zero destroy calls, zero probes, no integrity-only dir, OUT untouched; (S3) an altered mac leaf → exit 1, zero bun/sudo/ssh calls at all; (S2) admitted, controller exit 3 with no record → exit 3, wrapper substitute written, integrity-only attempt written, Mac and rig destroy each called once and each absence probed; (S4) admitted, SIGTERM during the controller → exit 143, terminal-kind INTERRUPTED, integrity-only present, both destroys; (S5/S6) controller 0 without a terminal record → exit 68 fail-closed, both destroys; (S7) PASS record but success verifier fails → exit 1, integrity-only present, both destroys; (S8) full success → exit 0, `render-campaign-report.ts --source=sealed-index --allow-non-promotable … --output=$OUT/diagnostic-report.md` called once, no integrity-only, and both destroys still run last. The success verifier argv recorded in S8 is exactly the 16 flags listed in the command. `bun test tools/compare/bin/stage-live-campaign.test.ts -t "frozen run wrapper admission gates"`: 4 pass / 0 fail (it builds the command through `buildFrozenRunCommand`, i.e. the same fragment bytes).
- Validity window at review time: `date +%s` 1788853740 → remaining 70,899,331 ms > required 9,600,000 ms; the gate itself is re-evaluated at launch (line 521-523).

## 3. The wt seal defect is gone from what will execute

- `receiverGoneAfterFin` (server.ts:480-483) matches only a message whose first colon-delimited token is `E_STREAM_RESET`; `E_STOP_SENDING` and everything else rethrow. Its single call site (server.ts:538-542) is the `try` around `channel.end(...)` only, after the `for` loop over `chunkCount` chunks has completed; a `write` failure throws out of the loop before `end` is reached, so the tolerance cannot mask bytes outstanding. This is in the archived server.ts, the worktree server.ts, the rig's staged `roles/server.ts` and the rig's role root `/tmp/ws-wt-linux-build.fZdCNH/tools/compare/server.ts` (all `sha256sum` 36a8fcd0…), and the launch records pin that digest. The only change since the reviewed 80593b2b is one doc-comment sentence (`git diff 80593b2b efaed8bb -- tools/compare/server.ts`).
- `bun test tools/compare/bulk-source-peer.test.ts`: 6 pass / 0 fail, including "a receiver that vanishes after taking the whole stream has not failed the transfer" (E_STREAM_RESET at end → resolves with `bytesWritten === bytes`), "a receiver that refuses the stream still fails the transfer" (E_STOP_SENDING at end → rejects), "a reset with bytes still outstanding fails the transfer" (E_STREAM_RESET on the second write → rejects, end never reached).
- `bun test tools/compare/phase-a-capture-real-process.test.ts` three consecutive runs: 2 pass / 0 fail each (wt and ws real server child over the child pipe: no `child-pipe-refusal/v1`, answer sequence ready → measure-start-ack → capture-ack → stopped, 104,857,600 bytes delivered, 1600/1600 chunks, `channelEnded:true`, exit 0). Its header says what it does not cover (rig supervisor sequencing, the physical path, the campaign); that is honest and matches the ninth stage's evidence that the ws arm of this same topology sealed live.
- `compare-controller.ts` production entry, spawned against a nonexistent `--staged-dir` with the frozen argv shape and the seam env unset: exit 4, stderr `controller real-run: REFUSED/STALE_OR_INVALID_STAGING: staged-dir verify failed (STAGE_VERIFY_FAILED): staged trust bootstrap missing …/authority.json`, and it wrote a canonical `controller-terminal/v1` record with `terminalKind:"REFUSED"`, `refusalCode:"STALE_OR_INVALID_STAGING"`, `failureCode:null`, `trafficStarted:false`, `controllerExitCode:4` — the exact shape the wrapper's parser requires for REFUSED.

## 4. Hosts, keys, TLS, and the abandoned roots

- Rig (read-only ssh as hermes-admin): `sha256sum` `$RIG_STAGE/bin/comparison-supervisor` c742c9cb… (= receipt/authority/ssh-host-receipt), `/home/hermes-admin/.bun/bin/bun` 9fd36f87… (= receipt `linuxBunSha256` = launch records' `bunSha256`), staged stage-receipt.json 75f24efa…, both leaves 46cda81f…/3d9bbf7c…, authority/lock/manifest/capability/observation digests all equal the Mac copies, `roles/server.ts` 36a8fcd0…, `roles/stage-live-campaign.ts` 7efc3286…. As `_wtcompare` (`sudo -n -u _wtcompare`): campaign key `/var/lib/webtransport-bun/comparison/keys/efaed8bb…/busyms-attested-focused-r1.rig.pk8` PRESENT (0400, 83 bytes) with its `.pub`; the 80593b2b and c7fafa52 rig keys ABSENT (only a `.pub` remains under 80593b2b); lease `/var/lib/webtransport-bun/comparison/leases/efaed8bb…/busyms-attested-focused-r1.lease.json` sha 72a9aca0… = receipt/observation `rigSigningKeyLeaseSha256`, `state:"armed"`, no `.status.json`; `$RIG_STAGE/staged-server-tls.key` is 0400 `_wtcompare`, `test -r` as `_wtcompare` → READABLE, sha 91cf20e9… = both launch records' `tlsPrivateKeySha256`; staged-server-tls.crt a4b54461… = receipt `tlsCertificateSha256` = launch records. No comparison-supervisor or server.ts process is running (`pgrep`).
- Mac: as `_wtcompare`, `…/keys/efaed8bb…/busyms-attested-focused-r1.mac.pk8` PRESENT, `.mac-recovery.pk8` PRESENT, `.mac.pub` PRESENT; the 80593b2b and c7fafa52 `.mac.pk8` ABSENT (their `.pub` and recovery keys remain, which `cleanup_signing_keys` documents as out of its scope). `_wtcompare` (gid staff) can traverse `staging-root` and read both public leaves, the receipt, the run command and the certificate.
- Launch records: argv `server.ts --transport={ws,wt} --mode=bulk-source --stage-profile=phase-a --bind=10.99.0.2`, bind/advertised 10.99.0.2:4433, `tlsServerName wt-compare.local`, `allowedEnvironment` PATH only, `addonSha256` e3b0c442… (empty) with `linuxAddonManifestSha256` also empty, consistent with Phase A carrying no fanout role (`fanoutRoleEntrypointSha256: null`). The rig receives the record bytes in the cohort request and pins them against the receipt-digested `staged_launch_record_sha256` (comparison-supervisor.rs:1810-1817, 2079), and reads cert/key from its own staged leaves with digest checks (comparison-supervisor.rs:1861-1869).
- Nothing of the abandoned stages reaches this run: the frozen bytes name only efaed8bb paths; the 80593b2b Mac trust root, its `integrity-only/` evidence dir and the rig's stale `armed` lease for 80593b2b are inert (that key is ABSENT on both hosts, and the lease is addressed by the candidate-specific `leasePath`).

## 5. Observations, not blockers

1. `macObserverSha256` (43dd3767…) digests the archived clean build's observer, which no longer exists on disk; the pre-trap `verify-stage-approval` observes `staging-root` with the worktree's unpinned `target/release/observe-directory-identity` (a symlink into `~/.cache/wtb-target`, sha 9abd4791…). The controller never invokes the TS observer (`mint-live-trust-bootstrap.ts` is stage-time only; the Rust supervisor observes on its own), so the gate is a same-root inode/device/volume comparison with a binary the receipt does not pin. Same state as the sealed ninth stage; worth a future pin.
2. The lease's `janitorUnit` names a systemd unit that does not exist on the rig (`systemctl status …` "could not be found", no unit files or timers matching `wtcompare*`), and nothing in TS or Rust consumes the field (only stage-live-campaign.ts:1327 writes it). The frozen wrapper's cleanup is the only key-destruction path, which every post-admission scenario above exercised. Same state as c7fafa52.
3. Every launch record carries `stageReceiptSha256` of 64 zeros by construction (server.ts:218), as it did at c7fafa52; the binding to the receipt is the receipt's `stagedServerLaunchRecordSha256ByLaunch`, not the reverse.
4. The base plan's §9.5 sentence "the EXIT trap always destroys both private keys" is departed from for pre-admission refusals only, recorded in `deviations/2026-09-08-a5-admission-gates-before-traps.md`; the plan bytes are unchanged (digest matches), and post-admission behaviour is exactly the plan's.
5. Not executed here: the rig supervisor over the cable, the rig's Linux build (compared by digest only), and any campaign traffic.
