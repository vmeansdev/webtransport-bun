APPROVED

# Critic review: exact staged artifacts, thirteenth stage (candidate efaed8bb)

Reviewed 2026-09-08 (review-time epoch 1788853955 s .. 1788854942 s) by execution, independently of the Architect artifact (read first, re-verified second). Git read-only, pre-traffic. The frozen command was never launched; `compare-controller.ts` was never pointed at the real staged directory. Nothing on the Mac runtime or in the staged directory was modified except this file. One disclosure: to diff the two rig stage-directory listings I wrote two throwaway files `/tmp/.modes-<candidate>.txt` on the rig and deleted them in the same command; nothing under `/home/hermes-admin/ws-wt-stage`, `/tmp/ws-wt-linux-build.fZdCNH` or `/var/lib/webtransport-bun` was touched.

## Bindings

- Stage receipt SHA-256: `75f24efa81bdb56eb70e8b213daf5931cfc3d174847278e6acf8a9be68ad50e8`
- Upcoming run command SHA-256: `25e1f3cd291da5709a14c6beef76da160a34325f25e47ef5bb8d85b156795896`
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `busyms-attested-focused-r1`

## 1. Staged bytes are the candidate (recomputed, not read off the Architect)

- `git rev-parse HEAD` = efaed8bb…; `git rev-parse HEAD^{tree}` = 8eb3c276…; `git status --porcelain -uno | wc -l` = 0 (whole tree, re-checked at the end of the review). The receipt's `candidate`, `candidateHead`, `candidateTreeOid` equal these (`cat stage-receipt.json`).
- `shasum -a 256` over every regular file under the staged dir except the archive: receipt 75f24efa…, run command 25e1f3cd…, authority.json b85a9bc4…, campaign-root/manifest.json b9f32f2b…, campaign-root/campaign-lock.json 1bedb210…, staging-root/staged-capability.json af9df8f7…, staging-root/mac-supervisor-ed25519.pub 46cda81f…, staging-root/rig-supervisor-ed25519.pub 3d9bbf7c…, staging-root/rig-signing-key-lease.armed.json 72a9aca0…, tls/staged-server-tls.crt a4b54461…, roles/server.ts 36a8fcd0…, roles/stage-live-campaign.ts 7efc3286…, launch records ws b98e48f0… / wt 58e1c402…, source-archive-receipt 756a8612…, ssh-host-receipt 69fdaee7…, campaign-reservation b614c46a…, r1-red-approval-bundle d8f6311c…; `incoming/` copies byte-identical. All equal the receipt's fields. `stat -f %z source.tar` = 53504000, `shasum` 1f737d61…, `tar -tf | wc -l` = 2045.
- Plan bindings: `shasum -a 256 docs/superpowers/plans/2026-09-05-cohort-completion-amendment.md` = 90d469cb… = `approvedPlanSha256`; `docs/superpowers/plans/approvals/2026-09-05-cohort-completion-amendment.md` = 6e66236a… = `approvalRecordSha256`.
- Archive == HEAD by object identity: extracted source.tar into the scratchpad, `git hash-object` on all 1751 regular files (0 symlinks), sorted, `diff` against `git ls-tree -r HEAD --format='%(objectname) %(path)'`: empty diff, 1751 = 1751.
- Worktree files the command executes hash to their HEAD blobs by that same diff: tools/compare/bin/stage-live-campaign.ts 7efc3286… (= receipt `stageToolEntrypointSha256` = rig `roles/stage-live-campaign.ts`), tools/compare/server.ts 36a8fcd0… (= receipt/observation/launch records/rig role root), compare-controller.ts 67c196e7…, verify-campaign-index.ts b617dbf6…, render-campaign-report.ts 80863172…, secure-fs.ts 9611672e…, canonical.ts 32049dc1….
- Validity: `date +%s` 1788853955 at start → 70,684,331 ms remaining; 69,697,331 ms at the end of the review; required 9,600,000 ms (line 522-523 of the frozen command). The staged certificate (`openssl x509 -noout -dates -ext subjectAltName`) is valid 2026-09-08T07:28:23Z .. 2026-09-10T07:28:23Z, SAN `DNS:wt-compare.local, IP Address:10.99.0.2`; it outlives `notAfterMs` (2026-09-09T03:30:39Z).

## 2. The frozen command: rebuilt, read in full, and executed on every path

- Rebuilt with `buildFrozenRunCommand({section:"9.5", …})` from the worktree fragments (`frozen-run-wrapper.fragment.sh` 80848ca5…, `frozen-run-section-9.5.fragment.sh` 429b413e…) with the receipt and the literal paths: `text === frozen` is `true`, sha 25e1f3cd…. `grep -oE '[0-9a-f]{40}'` over the file yields efaed8bb plus prefixes of the six digest literals only: no other candidate can be named by these bytes.
- Read all 675 lines. The three admission gates are lines 519-528 (leaf re-hash ×2, validity window, the sole `verify-stage-approval` with `--stage-receipt/--upcoming-run-command/--exact-stage-approval` under `$MAC_TRUST`); the first `trap` is line 532 and `trap on_exit EXIT` is line 535; `cleanup_signing_keys` (line 78) is called only from `on_exit` (line 504). `set -euo pipefail` is line 1, so a failing gate exits before any trap exists. `git diff 80593b2b efaed8bb -- tools/compare/bin/frozen-run-wrapper.fragment.sh` shows exactly the trap relocation plus the two `"$REPO/tools/compare/bin/verify-campaign-index.ts"` absolutizations.
- Executed a copy of the frozen file in which only the `OUT`, `MAC_TRUST`, `MAC_RUNTIME`, `MAC_BUN` assignment lines and the two `/usr/bin/sudo` literals were rewritten (`diff` printed: shown in the log, nothing else differs), with recording stubs for `sudo`, `ssh`, and every `bun <script>` (inline `bun -e` passed through to the real bun so the wrapper's own record writers and parsers ran for real; the stub controller wrote real canonical `controller-terminal/v1` records):
  - S1 no `exact-stage-approval.json` under MAC_TRUST, REAL `stage-live-campaign.ts verify-stage-approval` executed: stderr `EXACT_STAGE_APPROVAL_MISSING`, exit 3, zero destroy calls, zero probes, no `integrity-only/`, OUT untouched. Also run directly against the real staged dir (read-only; the approval file is absent there today): exit 3 (`stage-live-campaign.ts:3517-3519`).
  - S3 one byte appended to the mac leaf copy: exit 1, zero bun/sudo/ssh calls. S3b `STAGE_NOT_AFTER_MS` rewritten to now: exit 1, zero calls.
  - S2 admitted, controller exit 3 without a record: exit 3; wrapper substitute `{terminalKind:"FAIL", failureCode:"CHILD_LIFECYCLE", controllerExitCode:3}`; `integrity-only/attempt-*` + published copies; Mac destroy, Mac probe, rig destroy, rig probe each once.
  - S5 controller exit 0 without a record: exit 68, substitute with `controllerExitCode:68`, both destroys.
  - S6 controller writes a `REFUSED/STALE_OR_INVALID_STAGING` record and exits 4: exit 4, integrity terminal-kind `REFUSED`, both destroys.
  - S7 PASS record but the success verifier exits 2: exit 2, integrity terminal-kind `FAIL`, both destroys; call order verify → verify(--integrity-only) → destroy/probe ×2.
  - S8 full success: exit 0, no `integrity-only/`, both destroys still run last. The success verifier argv recorded is exactly the 16 flags of lines 607-624 (`--expected-pass-count=2 --expected-fail-count=0 --expected-refused-count=0 --expected-promotable-count=0 --expected-flat-count=0 --expected-pair-count=0 --expected-sealed-count=2 --expect-cells=bulk-one-way/physical --expect-arms=ws,wt --expect-arm-kinds=primary --expect-measured-repetitions=1` with both staged leaves and the external trust bound); the render call is `--source=sealed-index --allow-non-promotable … --output=$OUT/diagnostic-report.md`.
  - S9 full success but `ssh` exits 255 with nothing on stdout: exit 70, `CLEANUP_FAILED mac_destroy=0 mac_absence=ABSENT rig_destroy=255 rig_absence=UNPROVEN`, and `$MAC_TRUST/recovery/{rig-disconnect-requirement.json, rig-signing-key-lease.status.intended.json, disconnect-requirement-sticky.json}` written by the real record writer with `phase:"post-stage-receipt"`, `stageReceiptSha256` 75f24efa…, `leaseSnapshotSha256` 72a9aca0…, `rigCleanupStatus:"unproven-unreachable"`.
  - S4 SIGTERM while the controller runs: exit 143, substitute `terminalKind:"INTERRUPTED"`, integrity terminal-kind `INTERRUPTED`, both destroys.
  - S10 a stale `integrity-only/terminal-kind.txt` pre-existing in OUT is superseded by this attempt's (`stale` → `FAIL`).
- `bun test tools/compare/bin/stage-live-campaign.test.ts -t "frozen run wrapper admission gates"`: 4 pass / 0 fail.
- Section fragment lines 661-675 pin `CELLS=bulk-one-way/physical REPS=1 PURPOSE=focused CAMPAIGN_TIMEOUT_MS=3600000 EXPECTED_PASS=2 EXPECTED_PROMOTABLE=0 EXPECTED_FLATS=0 EXPECTED_PAIRED_PROMOTIONS=0 EXPECTED_SEALED=2 EXPECT_CANONICAL_FANOUT_COMPLETE=0 RENDER_MODE=diagnostic`, `ARMS=ws,wt`, `ARM_KINDS=primary`. Controller argv order matters and is right: `--cells`, `--reps`, `--execution-purpose` precede `--stage=full`, so the `--stage=full` defaults (all cells, 5 reps, canonical) at `compare-controller.ts:3956-3961` do not apply.
- Can the expectations pass while the A5 gate is not met? Executed the REAL `verify-campaign-index.ts` with the wrapper's exact argv against the previous candidate's index (c7fafa52: entries ws PASS / wt FAIL, both non-promotable) using that candidate's own trust bound and leaves: `TRUST_PROTOCOL: bulk-one-way/physical wt primary is FAIL; every registered measured arm must PASS`, exit 3. With its own counts (pass=1 fail=1 sealed=1) it verifies with `attestationsVerified=1`, so the leaves open the signature graph rather than counting; with this stage's leaves substituted it refuses `MAC_SIGNING_KEY_MISMATCH … 46cda81fd539…`. `proveRegisteredTopology` (`verify-campaign-index.ts:303-440`) demands a measured PASS per (cell, ws|wt, primary) and entry count == scheduled, so two PASS entries of one wire cannot satisfy it.
- Ambient environment: the 13 names unset at lines 2-14 include `COMPARISON_MAC_SUPERVISOR_UID_SEAM`; every `process.env[...]` read in compare-controller.ts (lines 2942-3063: `MAC_SIGNING_KEY_ENV`, `MAC_SUPERVISOR_USER_ENV`, `MAC_SUPERVISOR_UID_SEAM_ENV`, `MAC_CAMPAIGN_SCRATCH_ROOT_ENV`, `RIG_SIGNING_KEY_ENV`, `RIG_BUN_PATH_ENV`) is either unset by the wrapper or exported by it at lines 35-42.

## 3. The wt seal defect on the live path

- Root cause matches the code: `runBulkSourcePeer` (server.ts:490-556) writes `chunkCount` chunks with `await channel.write(...)` and only then `try { await channel.end(...) } catch (e) { if (!receiverGoneAfterFin(e)) throw e; }` (538-542). `receiverGoneAfterFin` (480-483) accepts only a message whose first colon token is `E_STREAM_RESET`. Native mapping: `finish_send_stream` (crates/native/src/client_stream.rs:2885-2893) maps `StreamWriteError::Stopped` → `E_STOP_SENDING` and every other finish error (including ConnectionLost from the receiver's CONNECTION_CLOSE) → `E_STREAM_RESET`; the wt adapter's `endStream` (adapters/wt.ts:356-405) surfaces that message unchanged through `writable.end(cb)`. The rig role root's server.ts is the same bytes (`sha256sum` 36a8fcd0…), the launch records pin it, and `staged_entrypoint` (comparison-supervisor.rs:1920-1947) re-hashes `argv[0]` through the pinned role-root fd before the fork, so the live child is this code.
- The tolerance cannot hide a short transfer: a reset with bytes outstanding fails a `write` before `end` is reached (bulk-source-peer.test.ts "a reset with bytes still outstanding fails the transfer"), and the receiver is the independent authority — `executeBulkOneWay` (client.ts:849-940) throws on digest mismatch and on `deliveredBytes !== totalBytes` before any sample is sealed; `drain` (server.ts:2807-2845) also refuses `wrote N of M bytes`. `E_STOP_SENDING` still fails (test "a receiver that refuses the stream still fails the transfer").
- Other places a delivered wt transfer could still be reported as a lifecycle failure: the rig's `stop_and_capture` (comparison-supervisor.rs:2381-2413) only relays the child's own refusal via `child_site_detail!("server capture ack", …)`; the child's refusals on that path are `drain` (deadline / not ok / short), `finalBusyMs < baselineBusyMs`, `windowNs <= 0`, and a frame that is not `server-loop-utilization/v1` (server.ts:2565-2640). No code refuses `busyMs == 0` (grep over the supervisor, verifier, controller and tools/compare), so the loop-busy meter cannot break a seal; the send-path charge is exercised by `adapters/loop-busy.test.ts` 17/17.
- Executed: `bun test tools/compare/bulk-source-peer.test.ts` 6/6; `bun test tools/compare/phase-a-capture-real-process.test.ts` **13 consecutive runs, 26 pass / 0 fail** (real `server.ts` child, FD 3/4 pipes, staged argv, real client leg, production `server-stop-and-capture/v1`, 104,857,600 bytes, 1600/1600 chunks, `channelEnded:true`, exit 0 on both wires); `adapters/wt.test.ts` 40/40. What this does not prove is written at the bottom of that test file and is true: the rig supervisor's sequencing over the cable, the Linux build, and the campaign itself were not executed here; the ninth stage's live seal of the ws arm on this same topology is the only evidence for the rig half.

## 4. Hosts, keys, roots

- Rig (read-only ssh as hermes-admin): `sha256sum` `$RIG_STAGE/bin/comparison-supervisor` c742c9cb…, `/home/hermes-admin/.bun/bin/bun` 9fd36f87…, staged receipt 75f24efa…, leaves 46cda81f…/3d9bbf7c…, authority b85a9bc4…, lease 72a9aca0…, observation 8f0457ce…, roles/server.ts 36a8fcd0…, role-root server.ts 36a8fcd0…. `stat` of the stage dir = `8265575 1001 1001 755 8` = observation `directoryIdentity`. **Role root proved, not sampled:** `sha256sum` of all 1751 tracked paths under `/tmp/ws-wt-linux-build.fZdCNH` fed over ssh stdin, diffed against the extracted archive's `shasum -a 256`: 0 differing lines, 0 missing files. Every file under `tools/compare`, `packages/webtransport`, `crates/native` is other-readable and every directory other-traversable (`find ! -perm -o=r` / `! -perm -o=x` empty); as `_wtcompare`: server.ts, the Linux addon, `packages/webtransport/src/index.ts`, bun, the supervisor, the staged TLS key (0400 `_wtcompare`, sha 91cf20e9… = both launch records) all `test -r` READABLE. Top-level stage-dir modes/owners/names are identical to c7fafa52's (the sealed stage). Port 4433 is bound by nothing on UDP or TCP (`ss -lunp`/`-ltnp`). No supervisor or server child is running.
- Rig keys as `_wtcompare`: `keys/efaed8bb…/busyms-attested-focused-r1.rig.pk8` PRESENT (0400, 83 B) with `.pub` whose sha is 3d9bbf7c…; `keys/80593b2b…/` and `keys/c7fafa52…/` hold only a `.pub` — both campaign private keys ABSENT. Lease `leases/efaed8bb…/…lease.json` sha 72a9aca0…, `state:"armed"`, `privateKeyPath`/`rigPublicKeySha256`/`notAfterMs` as the receipt; no `.status.json`.
- Mac: `shasum` `$MAC_RUNTIME/comparison-supervisor` c669b3fd…, `$MAC_RUNTIME/bun` e0c90ec1… (both = receipt). `sudo -n -u _wtcompare test -e`: efaed8bb `.mac.pk8`, `.mac-recovery.pk8`, `.mac.pub` PRESENT; 80593b2b and c7fafa52 `.mac.pk8` ABSENT (their recovery keys and `.pub` remain, out of `cleanup_signing_keys`' scope by its own comment). `_wtcompare` can read the receipt, the run command, both leaves, the certificate, authority.json, the observation and the wt launch record under the staged dir; it cannot read `tls/staged-server-tls.key`, which the Mac side does not use.
- Abandoned roots cannot reach this run: OUT (`.release-evidence/transport-comparison/efaed8bb…/busyms-attested-focused-r1`) exists, 0700, empty; the 80593b2b evidence dir holds only `integrity-only/` and its Mac trust root holds the two CHANGES REQUIRED reviews — neither path is named by the frozen bytes, and `verify-campaign-index` reads only `$OUT`. The frozen command names no other candidate (§2). No `exact-stage-approval.json` exists yet under this stage's MAC_TRUST; `reviews/` held only `architect.md` (sha 6e7de8f2…) before this file.

## 5. Observations, not blockers

1. **Neither native addon is pinned by the receipt.** `linuxAddonManifestSha256` = e3b0c442… is `sha256("")` from `hashAddonManifest` over an empty `prebuilds/` (stage-live-campaign.ts:956-971) and the launch records carry the same value as `addonSha256`; the Linux addon actually loaded, `crates/native/webtransport-native.linux-x64-gnu.node` (sha 72dd03b4…, built 07:34 UTC today from the HEAD-identical role root), and the Mac addon `crates/native/webtransport-native.darwin-arm64.node` (git-ignored, sha 6e546c2f…, built 2026-09-06 20:17) are covered by nothing. The Mac addon predates 33fd4ce6/007a7ca9/39c26c54, but `git diff --stat 33fd4ce6^ HEAD -- crates/native/src ':!crates/native/src/bin' ':!crates/native/src/secure_fs.rs'` is empty and no lib module references `secure_fs::`, so the client data path is HEAD's. Same state as the sealed ninth stage (c7fafa52's receipt carries the identical empty digest). Placeholder-evidence family; worth a real pin.
2. `verifyExactStageApproval`'s dirty check is `git status --porcelain --untracked-files=no -- tools/compare` (stage-live-campaign.ts:585-595), not the whole tree; the whole tree is clean today by my own check, and nothing else in the run re-checks `packages/` or `crates/`.
3. Two idle bun processes from 2026-08-29 (`rig-min-wt-echo-server.js` on 10.99.0.2:4447/udp, `rig-min-echo-server.js` on :4446/tcp) are still running on the rig; load average 0.00; they were present during the ninth stage's sealed run.
4. The role root lives in `/tmp` under `q /tmp 1777 root root 10d`; every file's ctime is 07:34 UTC today, the next `systemd-tmpfiles-clean` tick is 17:52 UTC today, so nothing can be aged out before `notAfter`; a rig reboot would remove it and the run would refuse (`NotReady("staged server entrypoint")`), not mis-seal.
5. The rig lease for 80593b2b is still `armed` on disk; nothing consumes leases at run time (grep), and the efaed8bb lease is addressed by its own candidate path.
6. The cleanup `ssh` at line 91 carries `ConnectTimeout=10` but no `BatchMode=yes` (the probe at line 65 has it); S9 shows an unreachable rig still bounds to exit 70 with the recovery requirement written.
7. The real-process test asserts `finalBusyMs >= baselineBusyMs`, not `> 0`; since nothing refuses a zero, that is a statement about the number's meaning, not the seal.
8. As the Architect noted, the pre-trap `verify-stage-approval` observes `staging-root` with the unpinned `target/release/observe-directory-identity` (symlink into `~/.cache/wtb-target`, sha 9abd4791…); the receipt's `macObserverSha256` is the archived build's.
9. Not executed here: the rig supervisor over the cable, the Linux addon, the Mac supervisor binary's runtime behaviour (digest-compared only), and any traffic.
