CHANGES REQUIRED

# Exact-stage Architect review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b)

Reviewer: Claude workflow subagent, Architect pass. Review date 2026-09-09 (02:04–02:40Z). Pre-traffic review of exact bytes; git read-only; nothing on the rig modified; the frozen command, the controller and the physical preflight were not run against the staged pair; the only file written is this artifact (scratch harnesses live in the session scratchpad outside the repo; `git status --porcelain -uno` → 0 before and after). The B5 exact-stage reviews under `../fanout-pilot-r1/reviews/` were read first and treated as untrusted; everything below was re-verified by execution.

## Bindings

- Stage receipt SHA-256: `d9dc99a4299ba45e66e0b11ba26914ddcddfa998c85321a7a7e872b20dc404f7`
- Upcoming run command SHA-256: `0e94382e98a880f800547c9eabf62f95df897b22ec22a03d3c63913b37496c42`
- Candidate HEAD: `1b9e80923a290dd328550d684e5033c5c5e1441d`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

Approved plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073` (R6); approval record `docs/superpowers/plans/approvals/2026-09-08-physical-budget-amendment.md` → `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`; deviation `docs/superpowers/plans/deviations/2026-09-09-ticker-250-retired-by-preflight.md` → `ccc0964e73500cae5b394b1cc82929bc044dca413840da2d2d488b36a830be61`; base plan `2026-08-30-busyMs-attested-fanout.md` → `9374b747…`; cohort-completion amendment → `90d469cb…` (all `shasum -a 256`; the first two equal the receipt's `approvedPlanSha256` / `approvalRecordSha256`).

## Verdict

The stage is sound and the frozen command is exactly the B6 the amendment plus its deviation describe, but the candidate bytes cannot pass their own frozen verifier on a correct run: `tools/compare/bin/verify-campaign-index.ts:870` gates every `promotable:true` index entry on `checkPromotionQuarantine` (`tools/compare/output-policy.ts:686`), and that function returns `promotable:false` for every input by design (`EXTERNAL_TRUST_BOUND_UNVALIDATED` is added unconditionally whenever a bound is supplied, `EXTERNAL_TRUST_BOUND_MISSING` otherwise; its own tests pin this). Under canonical every measured PASS seal is `promotable:true`, so the first seal the verifier opens refuses with `TRUST_PROTOCOL: promotable entry fails quarantine … EXTERNAL_TRUST_BOUND_UNVALIDATED`, exit 3, after all 72 executions have run. Section 1 has the proof by execution and the consequence traced through the frozen bytes. This path never executed before: A5 and B5 carry only `promotable:false` entries, and no test in the tree returns `ok:true` from `verifyCampaignIndex` on a promotable entry outside `--integrity-only`. A fix changes the candidate, so B6 must be restaged from the fixed HEAD and reviewed afresh (base plan §9: any mutation abandons the reviewed root). Everything else I could verify is recorded so the re-review can be narrow.

## 1. Blocking finding — the verifier refuses every canonical promotable seal

**Code.** `verify-campaign-index.ts:866-882` (read in full):

```
if (entry.promotable) {
  const quarantine = checkPromotionQuarantine({ artifact: parsed, externalTrustBound: args.externalTrustBoundSha256, expectedComparisonId: index.campaignId });
  if (!quarantine.promotable) { … return { ok:false, code:"TRUST_PROTOCOL", message:`promotable entry fails quarantine for ${entry.sealedPath}: ${detail}` }; }
}
```

`output-policy.ts:686-792` (read in full): after the sidecar checks, `if (!isNonEmptyString(input.externalTrustBound)) addReason(…"EXTERNAL_TRUST_BOUND_MISSING"…) else addReason(…"EXTERNAL_TRUST_BOUND_UNVALIDATED", "externalTrustBound is opaque until the external run/lock schema validates it"…)`; the result is `promotable: reasons.length === 0`, so it is never true. `output-policy.test.ts:319-325` ("does not treat an arbitrary non-empty trust marker as validated" → `promotable false`) and `r1-entrypoint-red.test.ts:682-694` (`EXTERNAL_TRUST_BOUND_UNVALIDATED` expected on an otherwise verifying artifact) pin exactly that behaviour: the function is a fail-closed placeholder, not a validator.

**Executed (scratchpad `q1.ts`, importing `checkPromotionQuarantine` from HEAD).** Over the real B5 `ws` seal with `promotable:true`, `executionPurpose:"canonical"`, `comparisonId:"fanout-attested-r1"` and the B6 `externalTrustBoundSha256 161e27a3…`: `{"promotable":false,"reasons":[{"code":"EXTERNAL_TRUST_BOUND_UNVALIDATED",…}]}` — the only reason left is the placeholder itself.

**Executed through the real verifier (scratchpad `r1/quarantine-path.test.ts`).** A one-cell canonical root: the two real B5 `ticker-fanout/rate-100` seals relabeled as canonical reps 1–5 (`promotable:true`, `repetitionTotal 5`, `comparisonId fanout-attested-r1`), a `campaign-index/v2` carrying the B6 receipt's candidate/plan/approval/capability/archive digests, and `parseVerifyCampaignIndexArgs` fed the frozen 9.7 argv shape (external trust bound `161e27a3…`, both B6 staged leaves, the one-cell counts, `--expect-measured-repetitions=5`). Only `verifyRunArtifact` was stubbed to PASS — it runs before the quarantine and cannot pass relabeled pilot bytes; every other line of the verifier ran as frozen. Result: `{"ok":false,"code":"TRUST_PROTOCOL","message":"promotable entry fails quarantine for …/reps/ticker-fanout_rate-100/ws/rep-1.sealed.json: EXTERNAL_TRUST_BOUND_UNVALIDATED"}`. Control without the stub on the same root: the real CLI refuses earlier on `TRUST_ANCHOR_MISMATCH; SCHEMA_INVALID_FIELD $.attestationEvidence.cohortObservationEvidence; …; ARTIFACT_BYTE_DIGEST_MISMATCH` (rc 3) — i.e. the stub removed only the stage a genuine B6 seal passes (B5's real seals passed it under the pilot verify at 01:53Z).

**No escape via `promotable:false`.** `artifact-builder.ts:1470-1472` sets `promotable = canonical && measured && classification.promotable`; `verify-artifact.ts:3076-3081` refuses a canonical PASS artifact that is not promotable (`STATUS_CONTRADICTION`); `verify-campaign-index.ts:852-858` refuses an entry whose `promotable` differs from the seal's; `output-policy.ts` `gateOneTransport` refuses `PROMOTION_ENTRY_NOT_PROMOTABLE`. The controller writes `promotable: spec.executionPurpose === "canonical"` for every measured PASS (`compare-controller.ts:3671`). So all 60 B6 seals are `promotable:true`, and the first one opened refuses.

**Consequence in the frozen bytes.** `run_measured_campaign` (command lines 596-618): verifier rc ≠ 0 → `SUCCESS_RC=3` → `ORIGINAL_RC=3`, `TERMINAL_KIND=FAIL` → `finalize_terminal_integrity` (integrity-only pass) → `on_exit` → `cleanup_signing_keys` destroys both campaign keys. The refusal fires after the controller has run all 6 cells × 2 arms × 6 executions (D5: up to 2.32 h, ≈1 h expected) and after the controller has already written the 12 flats. A launch of these bytes cannot PASS.

**History and coverage.** The call landed in `cc866b8b` (2026-09-01, "Anchor sealed artifacts to the staged identity so honest seals verify"); `grep -in quarantine verify-campaign-index.test.ts` → nothing; `grep -rn 'canonicalFanoutComplete).toBe(true)'` and the 12-flat/6-pair positive assertions exist only at the gate-module level (`fanout-promotion.test.ts:346-358` on `evaluateCanonicalFanoutCompletion`), never through `verifyCampaignIndex`. Other production callers of the same placeholder: `run-campaign.ts:1981, 2057` (legacy promotion), `bin/verify-artifact.ts:118`, `render-report.ts:375` — none on the frozen path, but the fix should decide their fate too.

**What must change (for the lead; git was read-only here).** Either make the verifier's promotable-entry check use the anchors it already proves (index candidate / archive / capability digests, the R6 signature graph, `comparisonId`, `artifactKind`, the fake/empty/sentinel digest and unobserved-toolchain rules) without the unconditional trust-bound placeholder, or make `checkPromotionQuarantine` actually validate `externalTrustBound` (e.g. against the index's staged anchors / `external-trust-bound/v1`) and drop the always-on reason — plus a positive test that drives a canonical index of `promotable:true` entries through `verifyCampaignIndex` (ideally the 60/12/6 shape with `--expect-canonical-fanout-complete`) to `ok:true`. Then: new HEAD → lead gate → restage B6 (`abandon` this root first) → Architect → Critic.

## 2. What was verified and holds (carry forward to the re-review)

### 2.1 Source identity and archive (executed)

`git rev-parse HEAD` → `1b9e8092…`; `HEAD^{tree}` → `2057f9ad30096513d6df76d89802fd8b07c43343`; `git status --porcelain -uno | wc -l` → 0. Receipt `candidate`/`candidateHead`/`candidateTreeOid` equal these. `source.tar`: `e4b5c7c5…`, 54,312,960 B, 2,070 members, inventory `998c4ebe…` = `archiveMemberInventorySha256`; `cmp` against the B5 stage's `source.tar` → identical. Extracted into the scratchpad: 1,776 regular files, 0 symlinks; `git hash-object --stdin-paths` joined with paths and `diff`ed against `git ls-tree -r HEAD` (1,776 blobs, no 120000/160000 modes) → zero differences. The archive is the candidate tree by blob identity and the same bytes B5 ran.

### 2.2 Every receipt leaf recomputed (`shasum -a 256`)

authority `8bfc10d9…`; lock `f7bf2b5d…`; manifest `3e1bef3f…`; capability `c7171885…`; TLS cert `fe056da0…`; lease snapshot `758b3335…`; mac leaf `f160cf8f…`; rig leaf `aa42100d…`; launch records ws/bulk `c98d0483…`, ws/fanout `23c380a6…`, wt/bulk `ffffedc4…`, wt/fanout `04e8ca9d…`; role leaves `roles/{server.ts,fanout-role.ts,stage-live-campaign.ts}` → `aeef5d1b…`, `d9c3f971…`, `d218ef43…` (equal in the checkout and on the rig role root `/tmp/ws-wt-linux-build.Wo3bAC/tools/compare`); observation `directoryIdentitySha256 5e0cac1f…` = `linuxDirectoryIdentitySha256`; `authority-digest.bin` = the raw 32 bytes of `8bfc10d9…`. All equal the receipt. `incoming/` copies are `cmp`-identical to the staged originals (receipt, authority, lock, manifest, capability, both leaves, TLS pair, authority digest). `macAddonManifestSha256 3a47ddf8…` is the mktemp-derived placeholder the recorded `2026-09-08-empty-addon-digest.md` deviation names; `linuxAddonManifestSha256` is `sha256("")` (rig `prebuilds/` empty).

### 2.3 Frozen run command (executed; read in full, lines 1-675)

`buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, …})` from HEAD's `stage-live-campaign.ts` with the receipt and the preamble's own literals rebuilt 32,026 bytes; `cmp` → BYTE-IDENTICAL; hash `0e94382e…`. `executionPurposeToSection("canonical")` → `{9.7, 16_200_000}` (`:1090-1091`). Preamble: 13 `unset`s of `AMBIENT_ENV_CLEARED_BY_FROZEN_RUN`; `CAMPAIGN_ID=fanout-attested-r1`, `EXECUTION_PURPOSE=canonical`, `RUN_TIMEOUT_MS=16200000`, `RUN_SECTION=9.7`, both leaf digests, `STAGE_NOT_AFTER_MS=1788990985973`, capability/lock/archive/external-trust-bound digests equal to the receipt; both keys addressed as `…/keys/$CANDIDATE/$CAMPAIGN_ID.{mac,rig}.pk8`. Admission gates (lines 519-528) before any trap: leaf digests vs preamble, `STAGE_NOT_AFTER_MS − now > RUN_TIMEOUT_MS + 5,400,000 = 21,600,000`, the sole `verify-stage-approval` over `$MAC_TRUST/exact-stage-approval.json` (absent today, correct). Traps only after (532-535). Wrapper body identical to B5's (same fragment). Section fragment (661-675) = HEAD's `frozen-run-section-9.7.fragment.sh` verbatim: `test CAMPAIGN_ID = fanout-attested-r1`, `test EXECUTION_PURPOSE = canonical`, `CELLS=ticker-fanout/rate-25,rate-50,rate-100,chat-fanout/subscribers-250,subscribers-500,subscribers-1000` (ladder order), `REPS=5`, `PURPOSE=canonical`, `CAMPAIGN_TIMEOUT_MS=14400000`, `EXPECTED_PASS=60`, `EXPECTED_PROMOTABLE=60`, `EXPECTED_FLATS=12`, `EXPECTED_PAIRED_PROMOTIONS=6`, `EXPECTED_SEALED=60`, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted`. `diff` against the 9.6 fragment: only those literals differ. Controller argv `--cells --reps=5 --execution-purpose=canonical --candidate --campaign --stage=full --staged-dir=$MAC_TRUST --arm-kinds=primary --campaign-timeout-ms=14400000 --write-terminal-record`. Verifier argv adds `--expect-canonical-fanout-complete` (set from the fragment, never a wrapper constant) to the seven counts, both leaves and the four topology flags. Promoted render argv `render-campaign-report.ts --candidate --campaign-id --campaign-root=$OUT --output=$OUT/campaign-report.md` (source defaults to `flats`, `:800`), then three counts: seals `find -type f -name '*.sealed.json'` = 60, flats at `-maxdepth 1` excluding `*.sealed.json`, `campaign-index.json`, `manifest.json`, `controller-terminal.json` = 12 (exactly the verifier's `RUN_CONTROL_FILENAMES`, `verify-campaign-index.ts:143-147`), and `rg -c '^### (WS|WT) attested arm'` = 12 (`/opt/homebrew/bin/rg` 15.1.0 present). D5 literals hold: 16,200,000 − 14,400,000 = 1,800,000 (the 9.7 pairing).

Time budget at review: Mac `date +%s` 1788919456, rig 1788919567521 ms; `notAfterMs − now` ≈ 71.5 M ms against the 21.6 M ms gate; the admission gate closes at 2026-09-09T15:56:25Z.

### 2.4 Cells traced (all six)

`FANOUT_COHORT_CELL_BY_ID` (`evidence.ts:152-160`) maps the six ids to `ticker 25/50/100`, `chat 250/500/1k`; `COHORT_CELL_CARDINALITIES` (`cohort-protocol.ts:5701-5756`): ticker 1/8/100/101 with 250/25,000, 500/50,000, 1,000/100,000; chat 10/8/{250,500,1000}/{260,510,1010} with 300/{75,000,150,000,300,000}; `COHORT_CELL_GRANT_PARAMETERS` (`:5802-5810`) 10,000 ms/100 B ticker, 30,000 ms/128 B chat; readiness `READINESS_DEADLINE_MS_VALUES = {30,000, 90,000}` (`:114-118`), `cohortReadinessDeadlineMs` (`compare-controller.ts:6917-6931`) 30,000 ticker / 90,000 chat; `FANOUT_EXPANDED_DECLARATION_BY_CELL_ID` (`cross-supervisor-protocol.ts:194-222`) the same six counts × 100/128 B; Rust `COHORT_CELLS` (`secure_fs.rs:19651-19731`) the same six rows with `measured_duration_ms`, `message_bytes`, `readiness_deadline_ms`, `cohort_cell()` refusing any other id; Rust grant parse refuses `expectedExpandedDeliveries ≠ expectedOfferedIngress × subscriberCount` (`:12765-12770`); registry constructs exactly `chatCell(250|500|1000)` and `tickerCell(25|50|100)` (`scenario-registry.ts:228-268, 513-517`). Deviation table (retire rate-250, add rate-25 = 1/8/100/101, 250/25,000) matches D3 + the code. Per-subscriber queue tolerance 64 (`fanout-relay.ts:108`, D3 text).

### 2.5 Verifier, gate, render (read; executed where possible)

- `proveRegisteredTopology`: cells set-equal to `--expect-cells`, arms, arm kinds, `measuredRepetitions` 5, `scheduledMeasuredArms` = 6×2×1×5 = 60, every entry PASS with `repetitionTotal` 5, no missing wire.
- `checkExpectedTotals` (`:281-331`) applies under canonical to publishers/subscribers/offered/accepted/delivered vs the cell row (`EXPECTED_TOTALS_MISMATCH`), called at `:937` on the real reconstruction.
- Flats: controller `promoteCampaignFlats` (`compare-controller.ts:1054-1149`) writes `cellSafeId(cell)-{ws,wt}.json` (median seal bytes) at the root for every cell whose primary set clears `evaluateCellPromotionGate`; verifier counts the same names (`safeCellName`, identical `/[/:]/g → _`), pairs = `-ws.json` with a `-wt.json` sibling, refuses stale echoes and flats for unscheduled cells; `evaluateCanonicalFanoutCompletion` (`output-policy.ts:1217-1276`) = six promoted cells ∧ 60 measured PASS seals ∧ zero refusals.
- Warmup writes nothing: `armRepetitionSchedule` (`:415-427`) schedules index 0 + 1..5; the warmup branch `continue`s before any index entry (`:3615-3624`); B5's real root holds exactly `reps/…/{ws,wt}/rep-1.{json,sealed.json}` and no warmup file (`find`). Seal count 60 and flat count 12 therefore agree with the wrapper's finds.
- Render flats path (`render-campaign-report.ts:540-782`): one `### ${WS|WT} ${attested|unattested} arm — \`cell\`` per promoted arm with `renderArmAccounting` (topology, totals, busyMs, main-thread, process, `CLAIM_BOUNDARY_SENTENCE`), then `canonicalFanoutLanguage`; returns 0 whenever the pairs exist.
- **Executed beyond the quarantine** (scratchpad `x1/beyond-quarantine.test.ts`; `verifyRunArtifact`, `verifyArmAttestationEvidence` and the quarantine stubbed, everything else real, the cohort reconstruction opened under the B5 leaves the seals were signed with): the one-cell 5+5 canonical root with its two median flats → `{"ok":true,"passCount":10,"promotableCount":10,"sealedCount":10,"flatCount":2,"pairCount":1,"promotedCells":[],"canonicalFanoutComplete":false,"attestationsVerified":10}` — `checkExpectedTotals` passed under canonical on real totals 1,000/100,000. The real promoted render CLI on that root: exit 0, `rg -c '^### (WS|WT) attested arm'` → 2, per arm `Topology: 1 publisher / 8 workers / 100 subscribers / 101 sessions`, `Totals … offered ingress 1000, accepted ingress 1000, relay writes 100000, delivered 100000, delivered bytes 10000000, post-stop drain 0`, busyMs 1932 ms (18.8 %) / main 2680 ms (26.1 %) / process 4650 ms (45.3 %) for ws and 1511 / 2660 / 10820 ms for wt, the claim-boundary sentence, and `NOT A CANONICAL FANOUT RESULT: 1/6 … 10/60` as expected for one cell. The completion branch itself (`--expect-canonical-fanout-complete` over six cells) could not be executed positively on real bytes — no canonical seals exist — and has no positive test (section 3.2).

### 2.6 Hosts, keys, lease, roots (executed, read-only)

Mac runtime `/usr/local/libexec/webtransport-bun/comparison/1b9e8092…/fanout-attested-r1/{bun,comparison-supervisor}` → `e0c90ec1…` = `macBunSha256`, `501f5d2a…` = `macSupervisorSha256`. `sudo -n -u _wtcompare /bin/sh -c 'if [ -e "$1" ]…'` → PRESENT for `/var/db/…/keys/1b9e8092…/fanout-attested-r1.mac.pk8` (0400, 83 B, recovery pair beside it, `.pub` → `f160cf8f…`; B5's `fanout-pilot-r1.mac.pk8` is gone — destroyed by its run — its `.pub`/recovery remain). `OUT` `.release-evidence/transport-comparison/1b9e8092…/fanout-attested-r1` exists, empty, born 04:00:47 local (one second after the receipt). `exact-stage-approval.json` absent (correct). Rig (`hermes-admin@10.99.0.2`, key `~/.ssh/ubuntu-vm-hermes`): `$RIG_STAGE/bin/comparison-supervisor` `84caa017…`, `observe-directory-identity` `fdd18545…`, `~/.bun/bin/bun` `9fd36f87…`; rig copies of receipt (`d9dc99a4…`), authority, observation (`8e5b7bd5…`, = Mac), lock, manifest, capability all equal; stage dir inode 8265767 / 8 links / 0755 / uid 1001 = the observation's `directoryIdentity`; key `/var/lib/…/keys/1b9e8092…/fanout-attested-r1.rig.pk8` PRESENT (0400 `_wtcompare`, 83 B; `.pub` → `aa42100d…`); lease `/var/lib/…/leases/1b9e8092…/fanout-attested-r1.lease.json` → `758b3335…` = the staged snapshot, `state "armed"`, `notAfterMs 1788990985973`, `privateKeyPath` this candidate's; no `.status.json`; `wtcompare*` janitor units 0 (the recorded janitor-absent deviation). `ss -ltnup`/`-lunp` show no listener on 4433; only the two long-lived `rig-min-*-echo-server` processes run; `replay/{mac,rig}-records` empty on both hosts; no `execution-*` under the rig stage; Mac `campaign-root/` holds exactly lock, reservation (`RESERVED`), manifest, r1-red bundle, source-archive receipt, ssh-host receipt. Mac loadavg 3.9–5.1 during review (acceptance running), rig 0.02.

### 2.7 Tests executed

- `bun test tools/compare/fanout-production-e2e.test.ts` → 19 pass / 0 fail / 393 expect, 196.50 s, exit 0 (the local acceptance); no supervisor, controller or role process survives it on the Mac.
- Scratch harnesses in section 1 and 2.5 (executed against HEAD's modules and the real CLI).
- Not executed here: the unit batches (798/0 and 779/0 at this same HEAD in the two B5 reviews), `cargo test`, the full `bun test`, `check-official-io`.

## 3. Non-blocking findings (to fold into the fix and the re-review)

1. **`promotionEligible` has no consumer.** `reconstructCohortEvidenceOffline` computes the §4.5 promotion predicate — Linux half (`verify-artifact.ts:4124-4153`: per-window `O=A`, `L=D=A×K`, zero relay faults, totals = row, `postStopDrainDelivered` 0) and the D2 worker half (`:4157-4177`: zero malformed/duplicate/reorder, `perSubscriberDelivered[i] = serverAcceptedIngress`) — and returns it, but `grep -rn promotionEligible` finds readers only in `fanout-promotion.test.ts`; `verify-artifact.ts:4241-4258` checks `result.ok` alone, `verify-campaign-index.ts:921` reads the reconstruction for totals only. Rust's twin `CohortConservationV1::promotable()` (`secure_fs.rs:14108`) likewise has only a test caller. The B5 reviews' sentence "per-subscriber … gating promotion (`:4160-4177`)" was a neighbourhood claim. What *is* enforced on a canonical seal: the hard conservation (`cohort-protocol.ts` `recomputeCohortOriginConservation`: per-window `OA=A`, `A≤O`, `L≤A×K`, `D≤L`, bytes, `L+faults=A×K`) plus `EXPECTED_TOTALS_MISMATCH` on the totals, which together force `O=A` and `D=L=A×K` with zero relay faults in every window; relay-side duplicate/reorder/queue-full end the run with `RELAY_DELIVERY` before a seal exists (`fanout-relay.ts:1377-1437`); worker-side duplicates/malformed units fail the role (`fanout-role.ts:1000-1019`). Left soft (computed, never read): post-stop drain > 0 (base plan §2142 says non-promotable), a worker-observed reorder, per-subscriber unevenness. Fix: refuse a canonical `promotable:true` entry whose reconstruction is not `promotionEligible` (verify-campaign-index, beside `checkExpectedTotals`), with a mutation test.
2. **No positive canonical verifier test.** Nothing drives `verifyCampaignIndex` to `ok:true` with promotable entries, 12 flats, 6 pairs and `--expect-canonical-fanout-complete`; the completion branch (`:1151-1181`) will run for the first time in B6. The fix in section 1 should land with that test.
3. **Wrapper `FAILURES` lags `CAMPAIGN_FAILURE_CODES` by `DELIVERY_CONTEXT_MISMATCH`** (command line 281 vs `cross-supervisor-protocol.ts:166`) — B5 finding, unchanged, still a FAIL-under-a-coarser-code, never a PASS.
4. `render-campaign-report.ts` carries no quarantine call (grep 0); `run-campaign.ts:1981/2057`, `bin/verify-artifact.ts:118`, `render-report.ts:375` do — legacy/non-frozen paths, but the same placeholder.
5. Operator preconditions at launch remain ungated by the frozen command (Mac < 4, rig < 0.5); the housekeeping `abandon` for the `efaed8bb`/`ab6126a3`/`b3c71552` pilot stages is still outstanding (unreachable from this campaign: `$CANDIDATE`-scoped key paths, leaf digests in the admission gate, `external-trust-bound/v1`).

## 4. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file after writing: it refuses with `EXACT_STAGE_APPROVAL_REVIEW_NOT_APPROVED` — by design, since line 1 is not `APPROVED` (`:345-347`) — and `extractExactStageReviewLabel` finds each of the five labels exactly once with the values above (recorded in the review transcript). This review therefore cannot be turned into an `exact-stage-approval.json`.
