CHANGES REQUIRED

# Exact-stage Critic review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b)

Reviewer: Claude workflow subagent, Critic pass, 2026-09-09 (~02:45–03:20Z). Pre-traffic review of exact bytes. Git read-only; nothing on the rig modified (ssh as `hermes-admin`, read-only commands and `sudo -n -u _wtcompare test -e` probes only); the frozen command, the controller against the staged pair and the physical preflight were not run; the only file written under the repo is this artifact (`git status --porcelain -uno | wc -l` → 0 before and after; scratch harnesses live in the session scratchpad). The Architect artifact (`reviews/architect.md`, SHA-256 `e8b41af4a7580b6ffd73fcbdd86198c5d9ce6b4896a89f87d14253c75e442b24`) and both B5 exact-stage reviews under `../fanout-pilot-r1/reviews/` were read first and treated as untrusted; every claim below was re-executed here.

## Bindings

- Stage receipt SHA-256: `d9dc99a4299ba45e66e0b11ba26914ddcddfa998c85321a7a7e872b20dc404f7`
- Upcoming run command SHA-256: `0e94382e98a880f800547c9eabf62f95df897b22ec22a03d3c63913b37496c42`
- Candidate HEAD: `1b9e80923a290dd328550d684e5033c5c5e1441d`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

Recomputed with `shasum -a 256` and equal to the receipt: plan `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073` (R6), approval `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`, deviation `ccc0964e73500cae5b394b1cc82929bc044dca413840da2d2d488b36a830be61`, base plan `9374b747…`, cohort-completion amendment `90d469cb…`.

## Verdict

The Architect's blocker is real and I reproduced it independently through the real verifier: `tools/compare/bin/verify-campaign-index.ts:866-882` gates every `promotable:true` index entry on `checkPromotionQuarantine` (`tools/compare/output-policy.ts:686-807`), and that function appends `EXTERNAL_TRUST_BOUND_UNVALIDATED` whenever a bound is supplied and `EXTERNAL_TRUST_BOUND_MISSING` otherwise (`:711-723`), returning `promotable: reasons.length === 0` (`:806`) — it cannot return `promotable:true` for any input. Every measured PASS seal a canonical run writes is `promotable:true` (`bin/compare-controller.ts:3671`; `artifact-builder.ts:1469-1472`; `verify-artifact.ts:3076-3081` refuses the alternative), so the frozen 9.7 verifier refuses the first seal it opens, after all 72 executions, and the frozen wrapper then destroys both keys. Section 1 has my own executions. A fix mutates the candidate, so this root must be abandoned and B6 restaged from the fixed HEAD with fresh reviews (base plan §9). Everything else I could refute I could not; sections 2–4 record it so the re-review can be narrow, and section 3 answers the seven questions the pass was asked.

## 1. Blocking finding, re-verified by execution

**1a. The function alone.** Scratchpad `q-critic.ts` importing `checkPromotionQuarantine` from HEAD:

- the real B5 `ws` seal (`…/fanout-pilot-r1/reps/ticker-fanout_rate-100/ws/rep-1.sealed.json`, originally `promotable:false, pilot, fanout-pilot-r1`) relabeled `promotable:true, canonical, fanout-attested-r1`, bound `161e27a3…`, `expectedComparisonId fanout-attested-r1` → `{"promotable":false,"reasons":[{"code":"EXTERNAL_TRUST_BOUND_UNVALIDATED",…}]}`;
- a synthetic artifact with every other rule satisfied (measured, promotable, matching id, non-legacy source, non-empty digests, clean sidecars) → the same single reason. There is no input that passes.

**1b. Through the real `verifyCampaignIndex` on the frozen argv shape.** Scratchpad `x/critic-verifier.test.ts`: a one-cell canonical root built from the two real B5 seals (relabeled as reps 1–5 per arm, `repetitionTotal 5`, `comparisonId fanout-attested-r1`; index `campaign-index/v2` carrying the B6 receipt's candidate/plan/approval/capability (`c7171885…`)/archive digests, `stagedDir` = this stage), `parseVerifyCampaignIndexArgs` fed the wrapper's exact flag set (lines 605-621 of the frozen command) with the B6 bound and both B6 staged leaves, one-cell counts (10/0/0/10/0/0/10, `--expect-measured-repetitions=5`). Only `verifyRunArtifact` was replaced (`mock.module` on `verify-artifact.ts`), because relabeled pilot bytes cannot pass their internal signatures; it runs before the quarantine (`:851`). Result:

`{"ok":false,"code":"TRUST_PROTOCOL","message":"promotable entry fails quarantine for …/reps/ticker-fanout_rate-100/ws/rep-1.sealed.json: EXTERNAL_TRUST_BOUND_UNVALIDATED"}`

Control: the real CLI, no stubs, same root and argv → refuses earlier at `verifyRunArtifact` (`TRUST_ANCHOR_MISMATCH $.verificationContext.executableSha256; SCHEMA_INVALID_FIELD …; ARTIFACT_BYTE_DIGEST_MISMATCH`), rc 3 — i.e. the stub removed exactly the stage that genuine B6 seals pass (B5's own seals passed it under the pilot verify at 01:53Z) and nothing else.

**1c. Consequence in the frozen bytes (read lines 505-675).** `run_measured_campaign`: controller rc 0 → verifier (`:605-621`) rc 3 → `SUCCESS_RC=3` → `ORIGINAL_RC=3`, `TERMINAL_KIND=FAIL` (`:650-656`) → `finalize_terminal_integrity` (integrity-only pass, `:383-451`; seals, index, flats and terminal remain on disk) → `return 3` → `on_exit` (`:495-511`) → `cleanup_signing_keys` (`:78-125`) destroys `…/keys/1b9e8092…/fanout-attested-r1.{mac,rig}.pk8`. This happens after all 6 cells × 2 arms × (1 + 5) executions. These bytes cannot produce a PASS.

**1d. Coverage.** `grep -in quarantine tools/compare/bin/verify-campaign-index.test.ts` → nothing; `git log -S checkPromotionQuarantine -- tools/compare/bin/verify-campaign-index.ts` → only `cc866b8b` (2026-09-01). `output-policy.test.ts:319-325` and `r1-entrypoint-red.test.ts:682-694` pin the always-on reason. A5/B5 indices carry only `promotable:false` entries (B5 index read: both entries `promotable:false`), so this branch has never executed on a real campaign. The tree's own `verify-campaign-index.test.ts` + `output-policy.test.ts` + `fanout-promotion.test.ts` → 162 pass / 0 fail at HEAD — they are green because none of them asks the verifier to accept a promotable entry.

**Required (for the lead; git read-only here):** make the verifier's promotable-entry gate a real predicate — either drop the placeholder call and use the anchors the verifier already proves (index candidate/archive/capability digests, R6 signature graph, `comparisonId`, `artifactKind`, the sentinel/empty-digest and `TOOLCHAIN_UNOBSERVED` rules, all of which `checkPromotionQuarantine` also evaluates and could keep), or make `checkPromotionQuarantine` validate `externalTrustBound` against the index's staged anchors and remove the unconditional reason — plus a positive test that drives `verifyCampaignIndex` on a canonical `promotable:true` index (ideally the 60/12/6 shape with `--expect-canonical-fanout-complete`) to `ok:true`, and the mutation set. Decide the same for the other callers (`run-campaign.ts:1981, 2057`, `bin/verify-artifact.ts:118`, `render-report.ts:375`). Then new HEAD → lead gate → `stage-live-campaign.ts abandon` on this root (both keys are still present, see 3.6) → restage B6 → Architect → Critic.

## 2. Stage identity — recomputed, holds

`git rev-parse HEAD` → `1b9e8092…`, `HEAD^{tree}` → `2057f9ad30096513d6df76d89802fd8b07c43343`, porcelain clean. `shasum -a 256`: receipt `d9dc99a4…`; command `0e94382e…` (675 lines); `source.tar` `e4b5c7c5…`, 54,312,960 B, `tar tf | wc -l` 2,070, `cmp` against B5's `source.tar` → identical; authority `8bfc10d9…`; TLS cert `fe056da0…`; mac leaf `f160cf8f…`; rig leaf `aa42100d…`; lease snapshot `758b3335…`; capability `c7171885…`; lock `f7bf2b5d…`; manifest `3e1bef3f…`; `roles/server.ts` = `tools/compare/server.ts` = `aeef5d1b…`; `roles/fanout-role.ts` = `tools/compare/bin/fanout-role.ts` = `d9c3f971…`; Mac runtime `comparison-supervisor` `501f5d2a…`, `bun` `e0c90ec1…`. All equal the receipt (`jq` dump of every scalar field). Receipt `notAfterMs 1788990985973`, `externalTrustBoundSha256 161e27a3…`, `rigRoleRootPath /tmp/ws-wt-linux-build.Wo3bAC/tools/compare`, `stageProfile phase-b`.

Frozen command rebuilt from HEAD (`buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, …})`, scratchpad `rebuild.ts`) → 32,022 bytes, `cmp` byte-identical, SHA `0e94382e…`. `executionPurposeToSection("canonical")` → `{9.7, 16_200_000}` (`stage-live-campaign.ts:1090-1091`). Lines 661-675 `diff` against `tools/compare/bin/frozen-run-section-9.7.fragment.sh` → identical: six cells in ladder order, `REPS=5`, `CAMPAIGN_TIMEOUT_MS=14400000`, 60/60/12/6/60, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted`. Preamble (1-33): 13 `unset`s, `RUN_TIMEOUT_MS=16200000`, `STAGE_NOT_AFTER_MS=1788990985973`, both leaf digests, `EXTERNAL_TRUST_BOUND_SHA256` = receipt, `$CANDIDATE`-scoped key paths. Admission gates (519-528) before the traps (532-535): leaf digests, `STAGE_NOT_AFTER_MS − now > RUN_TIMEOUT_MS + 5,400,000`, the sole `verify-stage-approval` over `exact-stage-approval.json` (absent today — correct). Controller argv (550-560) `--cells --reps=5 --execution-purpose=canonical --candidate --campaign --stage=full --staged-dir=$MAC_TRUST --arm-kinds=primary --campaign-timeout-ms=14400000 --write-terminal-record`. Verifier argv (605-621) = the shape my harness fed. Promoted render + three counts (623-635): `*.sealed.json` = 60; root `*.json` minus `RUN_CONTROL_FILENAMES` = 12 (equals `verify-campaign-index.ts` flat filter, executed in 3.5); `rg -c '^### (WS|WT) attested arm'` = 12.

Cells: `COHORT_CELL_GRANT_PARAMETERS` (`cohort-protocol.ts:5799-5808`) ticker 10 s/100 B, chat 30 s/128 B; `READINESS_DEADLINE_MS_VALUES = [30_000, 90_000]` (`:112-119`); `FANOUT_COHORT_CELL_IDS` executed in 3.5 equals the six frozen ids in ladder order; `CANONICAL_FANOUT_MEASURED_SEAL_COUNT` 60, `CANONICAL_FANOUT_CELL_COUNT` 6 (`output-policy.ts:823-834`). Per-subscriber cap `RELAY_SUBSCRIBER_QUEUE_MAX_ITEMS = 64` / 64 KiB (`scenarios/fanout-relay.ts:117-118`).

## 3. The seven refutation targets

### 3.1 Wall-clock budget

Controller constants (read): `WARMUP_DURATION_MS 5_000` (`cohort-protocol.ts:124`), `COHORT_ACQUISITION_DEADLINES = {frameMs 5_000, warmupDrainMs 6_000, captureMs 15_000, roleReceiveMs 5_000, teardownMs 10_000}` (`bin/compare-controller.ts:7468-7474`), `COHORT_DRAIN_DEADLINE_MS 10_000` (`:104`), readiness 30,000/90,000 via `cohortReadinessDeadlineMs` (`:6785`), `measuredDeadlineMs = measured + drain + frame` (`:6787-6790`), `STANDARD_DEADLINES` server-start 30,000 (`:2414-2422`), `COHORT_RECEIPT_VALIDITY_MS 600_000` (`:2841`), campaign timeout as a `Promise.race` → `MEASUREMENT_WINDOW` (`:2715-2731`).

Per-execution bound = readiness + 5,000 + 6,000 + measured + 10,000 + 5,000 + 10,000 = **76 s ticker / 156 s chat** (D5's figures hold); adding the 30 s server-start deadline every execution (the rig server child is torn down per arm, `:2263`, `:5687`, `:6882`) gives 106 s / 186 s. Worst case over 72 executions: 36×106 + 36×186 = 10,512 s = **2.92 h < 14,400 s** (`CAMPAIGN_TIMEOUT_MS`); D5's 2.32 h is the bound without server-start. Verifier + promoted render: harness 1b/3.5 verified 10 cohort seals with reconstruction in 144 ms, so 60 is seconds, well inside the 1,800 s the 9.7 pairing leaves (16,200,000 − 14,400,000).

Measured (B5 execution records, `sudo -n -u _wtcompare cat …/fanout-pilot-r1/campaign-root/execution-00000{1..4}-*.json`): grant `issuedAt` 1788918740944 → 756802 → 773191 → 789526, last sample 1788918805784, terminal 1788918806929 — **15.9–16.4 s per ticker execution** (ramp ≈ 0.7 s, warmup 5 s, window 10 s, drain/teardown ≈ 0.2–0.7 s), the whole 4-execution pilot 66 s. Chat 1k: ramp 1,010 sessions at `COHORT_CONNECTION_RATE_PER_SECOND 500` with `COHORT_MAX_CONNECTIONS_IN_FLIGHT 200` (`cohort-protocol.ts:100-101`) is ≥ 2.02 s by construction and ~3.4 s measured, against a 90,000 ms readiness deadline (26×); even a 10× slower handshake stalls at 34 s and still clears. Expected B6 wall ≈ 36×16.3 + 36×(3.4+5+30+1.5) ≈ 2,020 s ≈ **34 min**.

Lease/admission at a launch today: Mac `date +%s` 1788921012 during review → `notAfter − now` = 69,973,973 ms against the 21,600,000 ms gate; the gate closes at **2026-09-09T15:56:25Z**. Launched at 06:00Z the worst case ends 08:56Z with 13 h of lease left; grants carry `notAfter = issuedAt + 7,200,000` (pilot records) and receipts 600 s, both far above any single execution. No per-execution deadline can be exceeded by an honest slow ramp.

### 3.2 Un-preflighted rows (rate-25, rate-50, chat-250, chat-500)

From the eight receipts (`jq` over `preflight/1b9e8092…/*.json`; all `verdict PASS`, `failedPredicates []`, faults 0, Mac load 3.0–3.5, rig 0.05–0.46): server-child main-thread core, whole-window mean —

| row | 1× window | 1× warmup epoch | 2× window | relay busy 1× | queue peak items |
|---|---:|---:|---:|---:|---:|
| ws rate-100 (10,000 deliveries/s) | 0.257 | 0.032 (200/s) | 0.392 | 0.184 | 100 |
| wt rate-100 | 0.263 | 0.027 | 0.405 | 0.149 | 100 |
| ws chat 1k (10,000/s, 1,010 sessions) | 0.177 | 0.332 (20,000/s) | 0.576 | 0.152 | 1,488 |
| wt chat 1k | 0.179 | 0.358 | 0.601 | 0.077 | 1,488 |

Ticker rows at 100 subscribers differ only in rate; interpolating the two measured points per transport (200/s → 0.03, 10,000/s → 0.26; slope 2.3×10⁻⁵ core per delivery/s) gives rate-50 (5,000/s) ≈ 0.14 and rate-25 (2,500/s) ≈ 0.09 at 1×, and the 2× reading (0.39–0.41 at 20,000/s) bounds their 2× points at ≈ 0.22/0.12. Chat rows keep 300 ingress and scale deliveries with K: chat-500 halves both window (≈ 0.09) and warmup (≈ 0.17–0.18) load; chat-250 quarters them (≈ 0.05 / ≈ 0.09); per-session cost falls with sessions. All sit under the amendment's "every other row ≤ 0.2" and far under 0.50/0.80. Queue depth: the relay-wide peak at chat 1k is 1,488 items across 1,000 subscribers (1.5 per subscriber against the 64 cap) and exactly 100 at ticker (one burst, one item per subscriber); the lower rows have fewer subscribers at the same burst shape. No measurement was needed; none was taken.

### 3.3 Accumulation across 72 executions on one rig

- **Execution records.** The Mac supervisor commits `execution-{:06}-{transport}.json` into the campaign root with `openat_create_new` (O_EXCL, 0600) — `crates/native/src/bin/comparison-supervisor.rs:2992-3001`; the index is the grant's `executionIndex`, issued sequentially per campaign (B5 root holds exactly `execution-000001-ws` … `000004-wt`, warmups included, ~650 B each; B6's `campaign-root/` holds none: lock, reservation, manifest, r1-red bundle, source-archive receipt, ssh-host receipt only). B6 reaches index 72 (≈ 47 KB); O_EXCL can only collide on a reused index, which the sequence rules out. The preflight's hygiene comment ("refuses to start over a root that already holds one", `fanout-physical-preflight.ts:1245-1251`) describes the preflight's own refusal (`consumedCampaignRootReason`, `:1265-1270`), not the supervisor's — the pilot proves multi-execution roots work.
- **Rig.** `find $RIG_STAGE/fanout-pilot-r1 -newer stage-receipt.json` → nothing: the pilot left no per-execution file in its stage; `replay/{mac,rig}-records` empty on both hosts; B6 stage dir has no `execution-*`. `/tmp` tmpfs 6 G, 2.2 G free with the 1,003 M role root already resident; nothing per-execution lands there. Rig `ss -ltnup | grep -c 4433` → 0, load 0.00.
- **Mac ports.** `net.inet.ip.portrange.first/last` 49152–65535 (16,384), `net.inet.tcp.msl` 15,000 → TIME_WAIT 30 s; 18 TIME_WAIT sockets at review. A chat-1k WS execution parks ≤ 1,010 sockets in TIME_WAIT that expire before the next execution's ramp (≥ 40 s later); WT is UDP. Peak ≤ 2,020 ≪ 16,384. `kern.maxfilesperproc` 245,760.
- **Keys/lease** are per-campaign files read per execution; nothing increments.

### 3.4 One non-promotable seal

The controller writes a FAIL row and continues (`bin/compare-controller.ts:3625-3651`: `status:"FAIL", promotable:false`, `failureCode` from the dispatch or `TRUST_PROTOCOL`, `persistIndex`, `continue`); a warmup failure is logged and skipped (`:3615-3624`). `SUBSCRIBER_QUEUE_FULL` closes the session and records `RELAY_DELIVERY` (`scenarios/fanout-relay.ts:1498-1513`); a WT handshake stall past readiness is `COHORT_NOT_READY`. Either way the run completes all 72 executions, `promoteCampaignFlats` promotes only the complete cells, the controller's own render runs, controller rc 0, and the wrapper's verifier refuses on `--expected-pass-count=60` / `--expected-fail-count=0` / `--expected-promotable-count=60` (`verify-campaign-index.ts:1022-1060`), so `SUCCESS_RC≠0` → FAIL → integrity-only record under `$OUT/integrity-only/` → both keys destroyed; every seal, the index, the surviving flats and the terminal stay on disk (§9.8: "only the two private PKCS#8 files are removed"). That is the intended fail-closed outcome the plan and D5 fix (60 fresh seals, no partial claim) — but the operator must know it means **no retry on this stage**: one fault in 60 measured executions costs a restage. The B5 pilot, the four WT preflights and the acceptance suite give no per-session reproduction of the ~5 % FIN-ack class at these cardinalities; it remains the largest single-run risk and is not a defect of the bytes.

### 3.5 Promotion, pairing and completion logic on canonical shapes

- **Real verifier beyond the quarantine** (scratchpad harness with `CRITIC_STUB_QUARANTINE=1`: `verifyRunArtifact`, `checkPromotionQuarantine` and `verifyArmAttestationEvidence` replaced, everything else real; the cohort reconstruction opened under the **B5** leaves the seals were signed with): one-cell 5+5 canonical root plus the two median flats → `{"ok":true,"passCount":10,"promotableCount":10,"sealedCount":10,"flatCount":2,"pairCount":1,"promotedCells":[],"canonicalFanoutComplete":false,"attestationsVerified":10}` — `reconstructCohortEvidenceOffline` and `checkExpectedTotals` (`:930-946`) passed under canonical on the real totals 1,000/100,000, the flat filter counted exactly the two `<cell>-{ws,wt}.json` files, the pair matched.
- **Six-cell completion** (`x/critic-completion.test.ts`, gate module `evaluateCanonicalFanoutCompletion`, `output-policy.ts:1217-1276`): `FANOUT_COHORT_CELL_IDS` = the frozen six; 60 primary canonical PASS `promotable:true` entries with `receiptGraphComplete` + 12 flats → `complete:true, seals 60, promoted 6, flatCount 12`. Mutations → `complete:false`: one `promotable:false` (`PROMOTION_ENTRY_NOT_PROMOTABLE`), one FAIL (`PROMOTION_ENTRY_NOT_PASS`), one `receiptGraphComplete:false` (`PROMOTION_RECEIPT_GRAPH_INCOMPLETE`), 59 seals, one cross-campaign entry (`PROMOTION_CROSS_CAMPAIGN`). A missing flat leaves the gate-level `complete:true` (flats are not its input); that claim is carried by the verifier's `expectedFlatCount`/`expectedPairCount` refusals (`:1183-1200`) and the wrapper's find — held.
- **Why not six real cells:** only `ticker-fanout/rate-100` and `chat-fanout/subscribers-1000` have real seals (B5, preflight scratch at 1×), and `checkExpectedTotals` refuses a relabeled seal for any other row; the 60/12/6 branch of `verifyCampaignIndex` (`:1151-1181`) therefore still has no positive execution on real bytes — the test the fix must add.
- **Render, both callers.** The controller's canonical-only positional call (`bin/compare-controller.ts:2752-2777`, `render-campaign-report.ts <campaignId> <candidate>` from `cwd`, nonzero → exitCode 5 → FAIL) executed on a scratch cwd holding the one-cell root → rc 0, writes `$OUT/report.md` (a `.md`, invisible to the wrapper's `*.json` counts and the verifier's flat filter). The wrapper's promoted argv on the same root → rc 0, `rg -c '^### (WS|WT) attested arm'` → 2, per arm: topology `1 publisher / 8 workers / 100 subscribers / 101 sessions`, totals `offered 1000, accepted 1000, relay writes 100000, delivered 100000, bytes 10000000, drain 0`, busyMs 1932 (18.8 %) / main 2680 (26.1 %) / process 4650 (45.3 %) for ws, 1511 / 2660 / 10820 for wt, and the claim-boundary sentence.

### 3.6 Earlier candidates' keys, leases and roots

Mac (`sudo -n -u _wtcompare` probes): `keys/1b9e8092…/fanout-attested-r1.mac.pk8` PRESENT (0400, 83 B, `.pub` → `f160cf8f…`, recovery pair beside it); `fanout-pilot-r1.mac.pk8` ABSENT (destroyed by its run; `.pub`/recovery remain). 45 candidate key dirs exist; `efaed8bb…`, `ab6126a3…`, `b3c71552…` still hold a live `fanout-pilot-r1.mac.pk8` — the outstanding operator `abandon` the Architect also names. Rig: `keys/1b9e8092…/fanout-attested-r1.rig.pk8` PRESENT, pilot rig key ABSENT; 44 key and lease dirs; lease `leases/1b9e8092…/fanout-attested-r1.lease.json` → `758b3335…` = the staged snapshot, `"state":"armed"`; no `.status.json`; `ws-wt-stage/1b9e8092…/{fanout-attested-r1,fanout-pilot-r1}` plus ~40 older stages. None of that is reachable from B6: the frozen command names both keys as `…/keys/$CANDIDATE/$CAMPAIGN_ID.*.pk8` (lines 39-45), the admission gate digest-checks this stage's two leaves, and `external-trust-bound/v1` `161e27a3…` binds the receipt. `OUT` exists and is empty (born 04:00 local); `exact-stage-approval.json` absent; no supervisor/controller/role process alive on the Mac (`ps`), none on the rig beyond the two long-lived echo servers.

### 3.7 Mac-side load

The frozen command carries no load gate (grep of the 675 lines: none). During this review `sysctl vm.loadavg` read **4.76** (my two acceptance runs) — above the preflight's enforced bound of 4 (`preconditions.macLoadBound 4`, all eight receipts launched at 3.0–3.5). Launch precondition the operator must meet and record: Mac 1-min load < 4 with no foreign process storms (check `ps` parents for `ps axww` storms before trusting the number), rig < 0.5 (0.00 now), no acceptance/test run in flight, and the launch before 15:56:25Z today.

## 4. Non-blocking findings (fold into the fix and the re-review)

1. `promotionEligible` from `reconstructCohortEvidenceOffline` has no production reader (Architect §3.1; `grep -rn promotionEligible` → `fanout-promotion.test.ts` only) — post-stop drain > 0, worker-observed reorder and per-subscriber unevenness stay computed-but-unread. Refuse a canonical promotable entry whose reconstruction is not eligible, beside `checkExpectedTotals`, with a mutation test.
2. No positive canonical verifier test (3.5); the `--expect-canonical-fanout-complete` branch and the controller's positional render both run for the first time in B6 — the fix should land with the 60/12/6 test.
3. Wrapper `FAILURES` (command line 281) lacks `DELIVERY_CONTEXT_MISMATCH` (`cross-supervisor-protocol.ts:166`) — a FAIL under a coarser code, never a PASS; unchanged since B5.
4. The controller's positional render leaves `report.md` beside the wrapper's `campaign-report.md`; harmless to every count, but two reports of one root is worth one line in the deviation set.
5. Housekeeping: `abandon` for the three live `fanout-pilot-r1` Mac keys at `efaed8bb…`/`ab6126a3…`/`b3c71552…`.

## 5. Executed here

- `bun test tools/compare/fanout-production-e2e.test.ts` twice: 19 pass / 0 fail / 393 expect, 196.41 s and 197.13 s, rc 0 both (logs in the scratchpad); no supervisor/controller/role process survives either run.
- `bun test tools/compare/bin/verify-campaign-index.test.ts tools/compare/output-policy.test.ts tools/compare/fanout-promotion.test.ts` → 162 pass / 0 fail.
- Scratchpad harnesses: `q-critic.ts` (1a), `x/critic-verifier.test.ts` (1b and 3.5, both modes), `x/critic-completion.test.ts` (3.5), render on `x/cwd` (3.5), `rebuild.ts` (section 2), plus the real CLI control (rc 3).
- Not executed: `cargo test`, the full `bun test`, `check-official-io` (the two B5 reviews report 798/0 and 779/0 unit batches at this same HEAD).

## 6. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` over this file → `EXACT_STAGE_APPROVAL_REVIEW_NOT_APPROVED`, by design (line 1 is not `APPROVED`, `:345-347`); `extractExactStageReviewLabel` finds each of the five labels exactly once with the values in the bindings block (recorded in the review transcript). This artifact cannot become an `exact-stage-approval.json`.
