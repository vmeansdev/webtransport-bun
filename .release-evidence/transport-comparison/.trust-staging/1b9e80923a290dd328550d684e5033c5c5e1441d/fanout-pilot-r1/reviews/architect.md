APPROVED

# Exact-stage Architect review — B5 pilot `fanout-pilot-r1` (section 9.6, phase-b)

Reviewer: Claude workflow subagent, Architect pass. Review date 2026-09-09. Pre-traffic review of exact bytes; git read-only; nothing on the rig modified; the frozen command, the controller and the preflight tool were not run against the staged pair.

## Bindings

- Stage receipt SHA-256: `404620133ef4e8474b16aa7d744316a07803a8653e812560f0ad3a5356f5c8eb`
- Upcoming run command SHA-256: `10d0358afa663392b72146df08632afdcb0bd258d94e29b9614a067e67d8a812`
- Candidate HEAD: `1b9e80923a290dd328550d684e5033c5c5e1441d`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-pilot-r1`

Approved plan: `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` SHA-256 `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073` (R6). Approval record: `docs/superpowers/plans/approvals/2026-09-08-physical-budget-amendment.md` SHA-256 `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`. Recorded deviation: `docs/superpowers/plans/deviations/2026-09-09-ticker-250-retired-by-preflight.md` SHA-256 `ccc0964e73500cae5b394b1cc82929bc044dca413840da2d2d488b36a830be61`. All three recomputed with `shasum -a 256`; the first two equal the receipt's `approvedPlanSha256` / `approvalRecordSha256`.

Verdict: the staged bytes are the pilot the approved amendment plus its recorded deviation describe, and what they execute is complete. Nothing blocking remains. Two non-blocking observations are recorded in section 12 for the Critic and the lead.

## 1. Source identity (executed)

- `git rev-parse HEAD` → `1b9e80923a290dd328550d684e5033c5c5e1441d`; `git rev-parse HEAD^{tree}` → `2057f9ad30096513d6df76d89802fd8b07c43343`; `git status --porcelain -uno | wc -l` → 0. Receipt `candidate`, `candidateHead`, `candidateTreeOid` and `campaign-root/source-archive-receipt.json` (`finalCandidateHead`, `finalCandidateTreeOid`) all equal these.
- `git log --oneline 0e67a65d..HEAD` lists exactly the eight commits the brief names (973e9b67, 61577a0f, 4423f70e, fe18a527, b3c71552, ab6126a3, 5746a71d, 1b9e8092).
- Archive: `shasum -a 256 source.tar` → `e4b5c7c58cd4dff7352b43dcf7d9be477436eb46a28ba45ec08242486093181e`; `stat -f %z` → 54312960; `tar -tf | wc -l` → 2070; inventory file SHA `998c4ebe…` equals `archiveMemberInventorySha256`. Extracted OUTSIDE the repo into the session scratchpad; `git hash-object --stdin-paths` over all 1776 regular files, joined with paths, `diff`ed against `git ls-tree -r HEAD` (1776 entries): zero differences. No symlink or submodule entries in the tree (`ls-tree` mode 120000/160000 filter empty). The archive is the candidate commit by object identity.
- Three role leaves `roles/{server.ts,fanout-role.ts,stage-live-campaign.ts}` hash to `aeef5d1b…`, `d9c3f971…`, `d218ef43…` in the staging dir, in the worktree (`tools/compare/server.ts`, `tools/compare/bin/fanout-role.ts`, `tools/compare/bin/stage-live-campaign.ts`), in the extracted archive, and on the rig role root `/tmp/ws-wt-linux-build.qQ6yBm/tools/compare` (`sha256sum` over ssh). All equal the receipt's `serverEntrypointSha256`, `fanoutRoleEntrypointSha256`, `stageToolEntrypointSha256` and the linux-stage-observation's `roleRootServerEntrypointSha256`.

## 2. Receipt digests recomputed (executed, `shasum -a 256` over every staged file)

authority.json `7315663e…` = `authoritySha256`; campaign-root/campaign-lock.json `768caed4…` = `lockSha256`; campaign-root/manifest.json `d773c6ef…` = `manifestSha256`; staging-root/staged-capability.json `c2224a9a…` = `capabilitySha256`; tls/staged-server-tls.crt `649fb870…` = `tlsCertificateSha256`; staging-root/rig-signing-key-lease.armed.json `3278cd86…` = `rigSigningKeyLeaseSha256`; staging-root/mac-supervisor-ed25519.pub `4a85c3bc…` = `macSigningPublicKeySha256`; staging-root/rig-supervisor-ed25519.pub `5492aac5…` = `rigSigningPublicKeySha256`; the four `staged-server-launch-record.{ws,wt}.{bulk-source,fanout-cohort}.json` = `2b71aa12…`, `4ae9b7b7…`, `a379f899…`, `aab9127f…` exactly as `stagedServerLaunchRecordSha256ByLaunch`. `incoming/` carries byte-identical copies of the receipt, authority, lock, manifest, capability, both public leaves and the TLS pair. The rig stage dir holds the same receipt (`404620…`), authority, lock, manifest, capability, linux-stage-observation (`4bb4b83e…`, identical to the Mac copy) and lease (`3278cd86…`).

`macAddonManifestSha256` (`77904c8f…`) is `sha256("empty-prebuilds:<mktemp mac build dir>/prebuilds")` per `hashAddonManifest` (`stage-live-campaign.ts:956-958`); the mktemp name is not logged anywhere (`grep -rho 'ws-wt-mac-build\.[A-Za-z0-9]*'` over the evidence tree yields only the `XXXXXX` template), so it is unreproducible by construction. `linuxAddonManifestSha256` `e3b0c442…` is `sha256("")`: the rig stage dir's `prebuilds/` exists and is empty (`ls -la` over ssh), observed at `stage-live-campaign.ts:1388` over `join(root,"prebuilds")`. Both are the placeholder the recorded deviation `2026-09-08-empty-addon-digest.md` names; the addon actually loaded is bound by the role root's directory identity (`88c7c7b7…`, in the observation and the receipt) and the spawn's digests, not by these fields.

## 3. Frozen run command (executed)

`buildFrozenRunCommand({section:"9.6", …})` imported from HEAD's `stage-live-campaign.ts` with the receipt and the preamble's own literals rebuilt the command from HEAD's `frozen-run-wrapper.fragment.sh` and `frozen-run-section-9.6.fragment.sh`; `cmp` against `upcoming-run-command.sh` reports BYTE-IDENTICAL and the rebuilt bytes hash to `10d0358a…` (31859 bytes; `wc -l` reports 675 lines, the brief's "669" is immaterial given byte identity). Read in full:

- Preamble: the 13 `unset`s of `AMBIENT_ENV_CLEARED_BY_FROZEN_RUN`; `CANDIDATE`, `CAMPAIGN_ID=fanout-pilot-r1`, `EXECUTION_PURPOSE=pilot`, `RUN_TIMEOUT_MS=2100000`, `RUN_SECTION=9.6`, both public-leaf digests, `STAGE_NOT_AFTER_MS=1788987134356`, capability/lock/archive/external-trust-bound digests equal to the receipt; `COMPARISON_MAC_SIGNING_KEY` and `COMPARISON_RIG_SIGNING_KEY` are `…/keys/$CANDIDATE/$CAMPAIGN_ID.{mac,rig}.pk8`, so only this candidate's keys can be used.
- Admission gates BEFORE the traps (the a5-admission-gates-before-traps deviation): both staged leaves digest-checked against the preamble; `STAGE_NOT_AFTER_MS − now > RUN_TIMEOUT_MS + 5,400,000 = 7,500,000 ms`; the sole `verify-stage-approval` invocation over `$MAC_TRUST/exact-stage-approval.json` (absent today — correct, it is written after both reviews, and its absence refuses before any key is destroyed). Only then `trap on_signal INT/TERM/HUP` and `trap on_exit EXIT`.
- Section 9.6 fragment: `test "$CAMPAIGN_ID" = fanout-pilot-r1`, `test "$EXECUTION_PURPOSE" = pilot`, `CELLS=ticker-fanout/rate-100`, `REPS=1`, `PURPOSE=pilot`, `CAMPAIGN_TIMEOUT_MS=1800000`, `EXPECTED_PASS=2`, `EXPECTED_PROMOTABLE=0`, `EXPECTED_FLATS=0`, `EXPECTED_PAIRED_PROMOTIONS=0`, `EXPECTED_SEALED=2`, `EXPECT_CANONICAL_FANOUT_COMPLETE=0`, `RENDER_MODE=diagnostic`, `run_measured_campaign`. This is D5 (timeouts 1,800,000 / 2,100,000, REPS 1, pilot) with the deviation's cell substitution (rate-100 for rate-250).
- Controller argv: `--cells=$CELLS --reps=1 --execution-purpose=pilot --candidate --campaign --stage=full --staged-dir=$MAC_TRUST --arm-kinds=primary --campaign-timeout-ms=1800000 --write-terminal-record`. Verifier argv on success: `verify-campaign-index.ts --campaign-root --index --external-trust-bound-sha256 --mac-public-key --rig-public-key --expected-pass-count=2 --expected-fail-count=0 --expected-refused-count=0 --expected-promotable-count=0 --expected-flat-count=0 --expected-pair-count=0 --expected-sealed-count=2 --expect-cells=ticker-fanout/rate-100 --expect-arms=ws,wt --expect-arm-kinds=primary --expect-measured-repetitions=1` (no canonical flag). Diagnostic render: `render-campaign-report.ts --source=sealed-index --allow-non-promotable … --output=$OUT/diagnostic-report.md`.
- `executionPurposeToSection("pilot")` → `{section:"9.6", timeoutMs:2_100_000}` (`stage-live-campaign.ts:1089`), matching `RUN_TIMEOUT_MS`.

## 4. Cell → grant trace (rate-100)

- Deviation note table: retire `ticker-fanout/rate-250`; add `ticker-fanout/rate-25` (1/8/100/101, 10 s, 250 ingress, 25,000 deliveries, 100 B, 30,000 ms). `cohort-cells.conformance.test.ts` parses D3's table from the amendment and applies every `retire`/`add` row of the deviation notes (`:126-220`), asserting every mirror; it is in the 125/0 batch below.
- TS: `COHORT_CELL_CARDINALITIES` row `ticker 100` = publishers 1, workers 8, subscribers 100, sessions 101, measuredIngress 1,000, expandedDeliveries 100,000 (`cohort-protocol.ts:5721-5728`); `FANOUT_EXPANDED_DECLARATION_BY_CELL_ID["ticker-fanout/rate-100"]` = 100,000 × 100 B (`cross-supervisor-protocol.ts:205-208`); `types.ts:30` `ingressRatePerSecond: 25 | 50 | 100`; controller cell list `compare-controller.ts:280`; `messageBytes: 100 | 128` unions; grant parse enforces `expectedExpandedDeliveries = expectedOfferedIngress × subscriberCount` (`cohort-protocol.ts:992-1002`).
- Rust: `COHORT_CELLS` row `cell_id "ticker-fanout/rate-100"` = 1 / `SUBSCRIBER_SHARD_MODULUS` / 100 / 101 / 1,000 / 100,000 / 10,000 ms / 100 B / 30,000 ms (`secure_fs.rs:19679-19690`); grant parse reads `expectedOfferedIngress`/`expectedExpandedDeliveries` and refuses unless the product holds (`secure_fs.rs:12765-12768`).
- `grep -rn 'rate-250'` over non-test TS/Rust/sh sources: no occurrences (refused as unknown, never aliased). Every preflight receipt records `topology` 1/8/100/101, `windowCount` 10, `measuredDurationMs` 10000, `messageBytes` 100 for this cell.

## 5. D2 wire and vocabulary in the staged bytes

`fanout-delivery.ts`: `FANOUT_DELIVERY_SCHEMA "fanout-delivery/c1"`, magic `0xc1`, header 24 bytes; executed `fanoutDeliveryUnitBytes(100)` → 124 (and 152 for chat) — the 124-byte frame. Delivery-context helpers `deliveryContextSha256Of`, `contextTagOfDeliveryContextSha256`, `resolveDeliveryEpochByTag` (`:315-337`). `role-failed/v1`: defined and parsed in `cohort-protocol.ts:3687-3744`, emitted by `fanout-role.ts:1290` via `refuse(DELIVERY_CONTEXT_MISMATCH_FAILURE_CODE, …)`, accepted at every await by `remote-supervisor.ts:8640-8645`. `DELIVERY_CONTEXT_MISMATCH`: `cohort-protocol.ts:55`, `CAMPAIGN_FAILURE_CODES` entry `cross-supervisor-protocol.ts:166`, Rust vocabulary `secure_fs.rs:12197`. Pinned by `fanout-delivery.test.ts`, `campaign-failure-vocabulary.test.ts`, `bin/fanout-role.test.ts`, `bin/compare-controller.test.ts`, `fanout-executor.test.ts`, `cohort-protocol.test.ts` (all green below).

## 6. D6 verifier, attested CPU, render

- `verify-campaign-index.ts` `checkExpectedTotals` (`:281-331`) applies to `pilot` and `canonical`, reads `cohortCellCardinality(cell)` and refuses with `EXPECTED_TOTALS_MISMATCH` when publishers, subscribers, offered ingress, accepted ingress or delivered differ from the cell; called at `:937`. Pinned in `verify-campaign-index.test.ts` (2 references).
- `verify-artifact.ts`: grant `workerCount === COHORT_WORKER_COUNT` (`:3708`), exactly 8 worker children covering the shards (`:3855-3858`), per-subscriber delivered = server-accepted ingress and zero malformed/duplicate/reorder gating promotion (`:4160-4177`).
- `serverChildCpu` on `rig-server-snapshot-receipt/v1`: TS key set and `serverChildCpuIssue` with `mainThreadMs ≤ processMs` (`server-observation-artifact.ts:195-275`); Rust key set `secure_fs.rs:15339`, `server_child_cpu_window` `:15752`, baseline captured at the measure-start ack `:17108/:17413`; `/proc/<pid>/task/<pid>/stat` read in `comparison-supervisor.rs:1391-1406`.
- `render-campaign-report.ts` sealed-index diagnostic prints topology (publishers/workers/subscribers/sessions, `:260`), recomputed totals (`:266`), `busyMs`, main-thread CPU and process CPU as share of window (`:271-282`) and the D5 claim-boundary sentence (`:70-74`).

## 7. Hosts and launch material (executed)

- Mac runtime `/usr/local/libexec/webtransport-bun/comparison/<candidate>/fanout-pilot-r1/{bun,comparison-supervisor}` → `e0c90ec1…` = `macBunSha256`, `501f5d2a…` = `macSupervisorSha256`. Rig `$RIG_STAGE/bin/comparison-supervisor` → `84caa017…` = `linuxSupervisorSha256`; `bin/observe-directory-identity` → `fdd18545…` = `linuxObserverSha256`; `/home/hermes-admin/.bun/bin/bun` → `9fd36f87…` = `linuxBunSha256`.
- Keys: `sudo -n -u _wtcompare test -e /var/db/webtransport-bun/comparison/keys/<candidate>/fanout-pilot-r1.mac.pk8` → PRESENT; same probe over ssh for `/var/lib/webtransport-bun/comparison/keys/<candidate>/fanout-pilot-r1.rig.pk8` → PRESENT. Authoritative rig lease `/var/lib/webtransport-bun/comparison/leases/<candidate>/fanout-pilot-r1.lease.json` (read as `_wtcompare`): `state "armed"`, `notAfterMs 1788987134356`, `rigPublicKeySha256 5492aac5…`, private key path this candidate's; no `.status.json` beside it. Its bytes equal the staged snapshot (`3278cd86…`).
- Time budget: `notAfterMs 1788987134356` = 2026-09-09T20:52:14Z; at review (rig `date +%s%3N` ≈ 1788916783) 70.3 M ms remain against the 7.5 M ms admission gate; the gate closes at 2026-09-09T18:47Z.
- Janitor unit: not present on the rig (`systemctl list-units` shows only the Actions runner) — the recorded `2026-09-08-rig-janitor-absent.md` deviation; expiry is the `notAfterMs` checks.
- Replay ledgers `replay/{mac-records,rig-records}` empty on both hosts; `campaign-root/campaign-reservation.json` state `RESERVED`.

## 8. Abandoned roots cannot reach this campaign

Key dirs for `efaed8bb`, `ab6126a3`, `b3c71552` still hold `fanout-pilot-r1.{mac,rig}.pk8` on both hosts (listed as `_wtcompare`), and their stage dirs remain. They are unreachable from this run: the frozen command names `…/keys/$CANDIDATE/$CAMPAIGN_ID.*.pk8` with `CANDIDATE=1b9e8092…`, the admission gate digests the staged public leaves against the receipt's `4a85c3bc…`/`5492aac5…`, `external-trust-bound/v1` binds candidate + campaignId + authority + capability + lock + archive + both public keys + both directory identities (`stage-live-campaign.ts:2434-2447`), and the rig's lease binds this candidate's private-key path. The operator's `abandon` for `efaed8bb` that D5 calls for is still outstanding — housekeeping, not a reachability path.

## 9. D4 preflight receipts (executed re-verification)

`.release-evidence/transport-comparison/preflight/1b9e8092…/` holds eight receipts (SHA-256): ws-chat-1k-1x `39b4c3ce…`, ws-chat-1k-2x `bde06fda…`, ws-ticker-100-1x `d5687f8a…`, ws-ticker-100-2x `b802e766…`, wt-chat-1k-1x `902d8540…`, wt-chat-1k-2x `c2cb6d0a…`, wt-ticker-100-1x `ddb09fd6…`, wt-ticker-100-2x `971d562b…`. A scratch script imported `parsePreflightReceipt`, `preflightOfferPlan`, `evaluatePreflightPredicate` and `cellIdSlug` from HEAD and, for each: parse OK (closed key set, plan restated); predicate re-evaluated from the recorded inputs → PASS with zero failures; `mode "physical"`; `candidate` and `campaignId` bind this candidate and `fanout-pilot-r1`; file name equals `<transport>-<slug>-<pass>.json`; every window `O = A`, `D = A × K` (recomputed sums equal `totals`); all ten fault counters zero; warmup `macDelivered` = 10 × publishers × K; sampler pid = capture pid; `main-thread mean = mainThreadMs / intervalMs` recomputed; NOFILE 524288/524288; rig per-core MHz, RSS, Mac load and eight worker CPUs present; `campaignRootRecordsBefore []`, `campaignRootRecordsRemoved ["execution-000001-<transport>.json"]`; sealed artifacts under the preflight `scratch/` dir, outside every campaign root. Means: ws rate-100 0.257 / 0.392, wt rate-100 0.263 / 0.405, ws chat-1k 0.177 / 0.576 (warmup 0.332), wt chat-1k 0.179 / 0.601 (warmup 0.358); bounds 0.50 / 0.80 hold with the 1x warmup epoch covered; Mac load at each start 3.02–3.49 (< 4), rig 0.05–0.46 (< 0.5). The staged `campaign-root/` now holds exactly `campaign-lock.json, campaign-reservation.json, manifest.json, r1-red-approval-bundle.json, source-archive-receipt.json, ssh-host-receipt.json` and no `execution-*.json` (`find`). The tool is `bin/fanout-physical-preflight.ts`, allow-listed in `official-io-allowlist.json:88` (its test at `:83`), not reachable from the wrapper or the controller.

## 10. Tests executed (all at HEAD, this worktree)

- `bun test tools/compare/fanout-production-e2e.test.ts` → 19 pass, 0 fail, 393 expect() calls, 196.75 s, exit 0 (the local acceptance; its stderr carries the negative-path refusals the suite provokes).
- `bun test cohort-cells.conformance.test.ts bin/fanout-physical-preflight.test.ts bin/stage-live-campaign.test.ts` → 125 pass, 0 fail.
- `bun test bin/verify-campaign-index.test.ts bin/render-campaign-report.test.ts fanout-relay.test.ts cohort-protocol.test.ts cross-supervisor-protocol.test.ts server-observation-artifact.test.ts` → 279 pass, 0 fail.
- `bun test fanout-delivery.test.ts campaign-failure-vocabulary.test.ts bin/fanout-role.test.ts bin/compare-controller.test.ts fanout-promotion.test.ts cohort-seal.test.ts server-fanout-cohort.test.ts fanout-executor.test.ts fanout-supervisor-integration.test.ts` → 375 pass, 0 fail.
- Total 798 pass / 0 fail. Not executed by me: the preflight `--loopback` smoke (its unit suite above is green), `cargo test`, the full `bun test` sweep and `check-official-io` (the lead gate's; not re-run here).

## 11. Parser self-check

After writing, `parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file and printed the five bindings (recorded in the review transcript).

## 12. Non-blocking observations (carried to the Critic and the lead)

1. **Wrapper terminal-record vocabulary lags `CAMPAIGN_FAILURE_CODES`.** `frozen-run-wrapper.fragment.sh:281` (`const FAILURES = […]` inside `parse_controller_terminal_record`, present verbatim in the staged command) lists 18 codes and omits `DELIVERY_CONTEXT_MISMATCH`, which `cross-supervisor-protocol.ts:166` added and which `compare-controller.ts:1452` (`campaignFailureCodeNamedIn(reason) ?? "TRUST_PROTOCOL"`) can place into `controller-terminal/v1.failureCode`. No test ties the two lists (only `fanout-physical-preflight.test.ts:816` reads the fragment, for another purpose). Effect if it fires: the wrapper's parser exits 3, the wrapper keeps `TERMINAL_KIND=FAIL`, writes `controller-terminal.wrapper-substitute.json` with `CHILD_LIFECYCLE`, runs integrity-only, exits non-zero and destroys both keys — a FAIL either way, never a PASS. For this pilot it cannot arise from version skew (both hosts run the same proven bytes) and would only report a runtime defect under a coarser code. Fix belongs in a later freeze (the fragment is frozen into this command); recommend a test that derives the wrapper's list from `CAMPAIGN_FAILURE_CODES`.
2. **Operator preconditions at launch.** The frozen command has no Mac-load gate; D4's operator precondition (< 4) was met at each preflight start, but during this review the Mac load was 4.4–5.7 (WindowServer, ChatGPT/Codex helpers, Claude helpers). Launch when `sysctl -n vm.loadavg` reads < 4 and the rig `/proc/loadavg` < 0.5 (0.02 at review), well before the 2026-09-09T18:47Z admission deadline. `campaign-root/r1-red-approval-bundle.json` carries the role string `focused-a5-architect-critic-verifier` inherited from A5 — cosmetic; the exact-stage gate is `verify-stage-approval`.
