APPROVED

# Exact-stage Critic review — B5 pilot `fanout-pilot-r1` (section 9.6, phase-b)

Reviewer: Claude workflow subagent, Critic pass (separate from and after the Architect). Review date 2026-09-09. Pre-traffic review of exact bytes. Git read-only; nothing on the rig modified; the frozen command, the controller and the physical preflight were not run against the staged pair. The only file written is this artifact. Every claim below was re-verified by execution in this session; the Architect's artifact (`reviews/architect.md`, SHA-256 `d8b60a13c3a72039c15564aacfc863767b00017ffd648c05e37fc5bf770ec946`) was read first and treated as untrusted.

## Bindings

- Stage receipt SHA-256: `404620133ef4e8474b16aa7d744316a07803a8653e812560f0ad3a5356f5c8eb`
- Upcoming run command SHA-256: `10d0358afa663392b72146df08632afdcb0bd258d94e29b9614a067e67d8a812`
- Candidate HEAD: `1b9e80923a290dd328550d684e5033c5c5e1441d`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-pilot-r1`

Approved plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39f6a05cdd473b88a3dcaac943ed3e6fd809593e1898e9f992d5d6d073` (R6); approval record `docs/superpowers/plans/approvals/2026-09-08-physical-budget-amendment.md` → `0e54de0824a9a0623cf319b8b4dd0f6596ce849565aad62b46d2f9857898cabd`; deviation `docs/superpowers/plans/deviations/2026-09-09-ticker-250-retired-by-preflight.md` → `ccc0964e73500cae5b394b1cc82929bc044dca413840da2d2d488b36a830be61` (all `shasum -a 256`; the first two equal the receipt's `approvedPlanSha256` / `approvalRecordSha256`).

Verdict: I set out to refute readiness along the seven lines the brief names and could not. The staged bytes are HEAD by object identity, the frozen command is HEAD's fragments byte for byte, the eight D4 receipts re-evaluate to PASS from their recorded inputs and are cryptographically bound to this staged pair's keys, the preflight left the staged root exactly as the stage left it, the retirement is D3's own rule applied and the conformance suite binds the deviation note (mutation-proven), no earlier candidate's material can reach this run, and the frozen 9.6 verifier and render accept a correct pilot on real seals from this staged pair while refusing the two negative controls. Nothing blocking remains. Non-blocking observations are in section 9.

## 1. Source identity and archive (executed)

- `git rev-parse HEAD` → `1b9e8092…`; `git rev-parse HEAD^{tree}` → `2057f9ad30096513d6df76d89802fd8b07c43343`; `git status --porcelain -uno | wc -l` → 0 at the start and at the end of this review. `git log --oneline 0e67a65d..HEAD` → exactly 973e9b67, 61577a0f, 4423f70e, fe18a527, b3c71552, ab6126a3, 5746a71d, 1b9e8092.
- Receipt fields `candidate`, `candidateHead`, `candidateTreeOid` equal the live values; `campaign-root/source-archive-receipt.json` `finalCandidateHead`/`finalCandidateTreeOid` likewise.
- `source.tar`: SHA-256 `e4b5c7c5…`, `stat -f %z` 54312960, `tar -tf | wc -l` 2070, inventory `archive-member-inventory.txt` → `998c4ebe…` = `archiveMemberInventorySha256`. Extracted into the session scratchpad (outside the repo); `git hash-object --stdin-paths` over its 1776 regular files joined with paths and `diff`ed against `git ls-tree -r HEAD` (1776 blobs) → zero differences; no symlink/submodule entries (`ls-tree` mode 120000/160000 filter empty). The archive is the candidate tree by blob identity.

## 2. Every receipt leaf recomputed (`shasum -a 256`, executed)

`authority.json` `7315663e…`; `campaign-root/campaign-lock.json` `768caed4…`; `campaign-root/manifest.json` `d773c6ef…`; `staging-root/staged-capability.json` `c2224a9a…`; `tls/staged-server-tls.crt` `649fb870…`; `staging-root/rig-signing-key-lease.armed.json` `3278cd86…`; `mac-supervisor-ed25519.pub` `4a85c3bc…`; `rig-supervisor-ed25519.pub` `5492aac5…`; the four launch records `2b71aa12…`/`4ae9b7b7…`/`a379f899…`/`aab9127f…`; `linux-stage-observation.json` `4bb4b83e…`; `source-archive-receipt.json` `35630e64…` (= lock and capability `sourceArchiveReceiptSha256`); `ssh-host-receipt.json` `b28b2f96…` (= capability `sshHostReceiptSha256`); `r1-red-approval-bundle.json` `0b1591c5…` (= lock/capability `r1RedApprovalBundleSha256`). Every value equals the receipt. `incoming/` copies are `cmp`-identical to the staged originals (receipt, authority, lock, manifest, capability, both public leaves, TLS pair, authority digest).

Role leaves: `roles/{server.ts,fanout-role.ts,stage-live-campaign.ts}` → `aeef5d1b…`, `d9c3f971…`, `d218ef43…`, equal in the worktree (`tools/compare/server.ts`, `tools/compare/bin/fanout-role.ts`, `tools/compare/bin/stage-live-campaign.ts`), in the extracted archive (by blob identity) and on the rig role root `/tmp/ws-wt-linux-build.qQ6yBm/tools/compare` (`sha256sum` over ssh), and equal to `serverEntrypointSha256`, `fanoutRoleEntrypointSha256`, `stageToolEntrypointSha256` and the observation's `roleRootServerEntrypointSha256`.

## 3. Frozen run command (executed)

`buildFrozenRunCommand({section:"9.6", …})` imported from HEAD's `stage-live-campaign.ts`, fed the staged receipt and the preamble's own literals (`REPO`, `MAC_TRUST`, `MAC_RUNTIME`, `RIG`, `RIG_STAGE`, `SSH_KEY`, `MAC_BUN=/Users/vmeansdev/.bun/bin/bun` — a symlink to `/opt/homebrew/bin/bun` 1.3.14, verified present — `OUT`, `runTimeoutMs` 2,100,000) rebuilt the command; `cmp` against `upcoming-run-command.sh` → BYTE-IDENTICAL; the rebuilt bytes hash to `10d0358a…` (31863 bytes, 675 lines). `executionPurposeToSection("pilot")` → `{9.6, 2_100_000}` (`stage-live-campaign.ts:1081-1096`).

Read in full (lines 1-675):

- Preamble unsets the 13 `AMBIENT_ENV_CLEARED_BY_FROZEN_RUN` names; `CANDIDATE`, `CAMPAIGN_ID`, `EXECUTION_PURPOSE=pilot`, `RUN_TIMEOUT_MS=2100000`, `RUN_SECTION=9.6`, both public-leaf digests, `STAGE_NOT_AFTER_MS=1788987134356`, capability/lock/archive/external-trust-bound digests all equal the receipt; both campaign keys are addressed as `…/keys/$CANDIDATE/$CAMPAIGN_ID.{mac,rig}.pk8`.
- Admission gates (lines 511-528) run before any trap: both staged leaves digest-checked against the preamble; `STAGE_NOT_AFTER_MS − now > RUN_TIMEOUT_MS + 5,400,000`; the sole `verify-stage-approval` over `$MAC_TRUST/exact-stage-approval.json` (absent today — correct; a refusal here leaves both keys intact). Traps (INT/TERM/HUP → `on_signal`, EXIT → `on_exit`) are installed only after those pass (lines 532-535).
- Section fragment (lines 662-675): `CELLS=ticker-fanout/rate-100`, `REPS=1`, `PURPOSE=pilot`, `CAMPAIGN_TIMEOUT_MS=1800000`, `EXPECTED_PASS=2`, `EXPECTED_PROMOTABLE=0`, `EXPECTED_FLATS=0`, `EXPECTED_PAIRED_PROMOTIONS=0`, `EXPECTED_SEALED=2`, `EXPECT_CANONICAL_FANOUT_COMPLETE=0`, `RENDER_MODE=diagnostic`. Controller argv `--cells --reps=1 --execution-purpose=pilot --candidate --campaign --stage=full --staged-dir=$MAC_TRUST --arm-kinds=primary --campaign-timeout-ms=1800000 --write-terminal-record`. Verifier argv on success carries both staged public leaves and the seven count expectations plus `--expect-cells/arms/arm-kinds/measured-repetitions` (no canonical flag); render is `--source=sealed-index --allow-non-promotable … --output=$OUT/diagnostic-report.md`.
- Failure honesty (brief item 6): `on_exit` always calls `cleanup_signing_keys`, which destroys the Mac key, probes its absence as `_wtcompare`, destroys the rig key over ssh, probes its absence as `_wtcompare` on the rig, and returns 70 with `CLEANUP_FAILED` on any non-zero destroy or non-ABSENT verdict; an unproven rig verdict records `recovery/rig-disconnect-requirement.json` (deferred-only). `run_measured_campaign` maps controller rc ≠ 0, verifier rc ≠ 0, render rc ≠ 0 and count rc ≠ 0 to `ORIGINAL_RC ≠ 0` with `TERMINAL_KIND=FAIL` and runs `finalize_terminal_integrity` (integrity-only verifier, this-attempt directory, `INTEGRITY_DONE` only after every evidence write). A missing or unparseable `controller-terminal.json` after rc 0 becomes rc 68 + a `CHILD_LIFECYCLE` substitute; `parse_controller_terminal_record` refuses PASS unless rc 0, `campaignStatus` PASS, null codes and `trafficStarted` true. No path after the traps exits without the key cleanup.

## 4. D4 preflight receipts — do they prove what the amendment requires, and do they bind this pair? (executed)

`.release-evidence/transport-comparison/preflight/1b9e8092…/` holds exactly eight receipts: ws-chat-1k-1x `39b4c3ce…`, ws-chat-1k-2x `bde06fda…`, ws-ticker-100-1x `d5687f8a…`, ws-ticker-100-2x `b802e766…`, wt-chat-1k-1x `902d8540…`, wt-chat-1k-2x `c2cb6d0a…`, wt-ticker-100-1x `ddb09fd6…`, wt-ticker-100-2x `971d562b…`.

A scratch script (`eval-receipts.ts`) imported `parsePreflightReceipt`, `preflightOfferPlan`, `evaluatePreflightPredicate`, `cellIdSlug`, `OPERATOR_LOAD_BOUND`, `MAIN_THREAD_CORE_BOUND` from HEAD's `bin/fanout-physical-preflight.ts` and, for every receipt: closed key set parse OK, offer restates the plan; predicate re-evaluated from the recorded inputs → PASS with zero failures; `mode "physical"`; `candidate` = `1b9e8092…` and `campaignId` = `fanout-pilot-r1`; filename = `<transport>-<slug>-<pass>.json`; `runId` = `fanout-pilot-r1/<cell>/<transport>/measured-1`. Checked against D4's text:

- **1× pass, O = A and A = rate per window**: `acceptedPredicate "equals"`, `requiredAcceptedPerWindow` 100 (ticker) / 10 (chat), every origin window offered = accepted = required; totals O = A = 1,000 / 300.
- **2× pass, A ≥ 2× binding**: `at-least`, required 200 (ticker, binding = row 100) / 40 (chat, binding = warmup 20); every window accepted = 200 / 40; `pacingSource "preflight-input"`, publisher rate 200 / 4 per publisher; O = A = 2,000 / 1,200.
- **D = A × K counted Mac-side**: `deliveredByOriginWindow` = accepted × 100 / × 1,000 in every window (10,000 / 20,000 ticker; 10,000 / 40,000 chat), recomputed sums equal `totals.delivered` (100,000 / 200,000 / 300,000 / 1,200,000); the receipt's `windows.note` names the source: publisher partials (offered), Linux relay observation (accepted), the eight worker partials (delivered) — origin-window counts, "never wall-clock samples" (`fanout-physical-preflight.ts:2597-2612` reads `publishers[].offeredByOriginWindow`, `observation.acceptedIngressByOriginWindow`, `workers[].deliveredByOriginWindow`).
- **Warmup epoch through the production lifecycle**: `warmup.macDelivered` = 10 × publishers × K (1,000 ticker, 10,000 chat) = `relayDeliveries`; `macOffered` = 10 × publishers = `relayIngress`; the seal each pass produced carries `cohortWarmupEpoch`, `roleWarmupCompletionManifest`, `rigWarmupDrainedReceipt` and their signatures (real production frames).
- **Zero faults**: all ten counters zero, `ingressRefusals` 0 in every receipt.
- **Main-thread means within bounds** (whole-window means, `mainThreadMs / intervalMs` recomputed to 1e-9): ws ticker 0.257 / 0.392, wt ticker 0.263 / 0.405, ws chat 0.177 / 0.576 (warmup 0.332), wt chat 0.179 / 0.601 (warmup 0.358), against 0.50 / 0.80; `mainThreadMs ≤ processMs` everywhere; per-second maxima recorded (0.30–0.84), not gated, as D4 says.
- **Both hosts' preconditions**: `enforced true`; Mac load at start 3.02–3.49 (< 4), rig 0.05–0.46 (< 0.5); rig per-core MHz at all four boundaries, RSS (peak 128 MB), NOFILE 524288/524288; eight Mac worker CPUs per receipt; sampler pid = capture pid.
- **Timeline**: from the seals' `metrics.provenance` the measured windows are disjoint and sequential (ws-ticker-2x 00:59:09–19Z, wt-ticker-1x 00:59:59–01:00:09Z, ws-ticker-1x 01:01:29–39Z, wt-ticker-2x 01:02:51–01:03:01Z, ws-chat-1x 01:04:38–01:05:07Z, ws-chat-2x 01:08:47–01:09:17Z, wt-chat-1x 01:12:36–01:13:05Z, wt-chat-2x 01:13:22–01:13:52Z), all after the stage's `issuedAtMs` 00:56:34Z; no pass overlapped another.

**Binding to this exact staged pair, not merely by string.** `openPhysicalPair` (`fanout-physical-preflight.ts:1629-1840`) verifies the staged dir, refuses a candidate other than the receipt's, spawns the real Mac supervisor over the staged campaign-root/staging-root with `COMPARISON_MAC_SIGNING_KEY` and the real rig supervisor over ssh with `COMPARISON_RIG_SIGNING_KEY` and the receipt's `rigRoleRootPath`. The seal under `scratch/ws-ticker-fanout_rate-100-1x/rep-1.sealed.json` records `source.sourceSha 1b9e8092…`, `archiveSha256 e4b5c7c5…`, `executableSha256 c2224a9a…` (this stage's capability), `comparisonId fanout-pilot-r1`, `executionPurpose pilot`, and a signed `cohortObservationEvidence`. Proof by execution (section 7): the frozen verifier argv opens that attestation graph under the staged public leaves and passes; substituting the efaed8bb stage's rig public leaf (`b50822d4…`) refuses with `SCHEMA_INVALID_FIELD $.attestationEvidence.cohortObservationEvidence`. The receipts therefore could not have been produced against other bytes or other keys.

Retirement evidence: `preflight/ab6126a3…/ws-ticker-fanout_rate-250-{1x,2x}.json` → `9e58570f…` / `a4845673…` exactly as the deviation cites; re-read: 1× PASS main 0.468, 2× FAIL main 0.936 (`LIFECYCLE_COMPLETE`, `OFFERED_EQUALS_ACCEPTED`, `ACCEPTED_PER_WINDOW`, `DELIVERED_EQUALS_ACCEPTED_TIMES_K`, `MEASURED_MAIN_THREAD_CPU`). D3's rule applies as written.

## 5. The preflight's footprint on the staged pair (executed)

- The Mac supervisor's only durable campaign-root write is the execution record, created O_EXCL by `CampaignRootSink::commit` (`crates/native/src/bin/comparison-supervisor.rs:2985-3030`, `execution-{:06}-{}.json`); the replay ledger is `MemoryReplayLedger` (`secure_fs.rs:11920`, in-process only) and the TS filesystem ledger is "unused by seal path" (`remote-supervisor.ts:2895-2906`). Each receipt records `campaignRootRecordsBefore []` and `campaignRootRecordsRemoved ["execution-000001-<transport>.json"]` (`5746a71d`: `listExecutionRecords`/`consumedCampaignRootReason`/`removeExecutionRecords`, `:1253-1283`, and the refusal at `:1674-1677`).
- Staged `campaign-root/` now holds exactly `campaign-lock.json, campaign-reservation.json, manifest.json, r1-red-approval-bundle.json, source-archive-receipt.json, ssh-host-receipt.json` (`ls`), all with the stage's 02:56 mtime and the digests in section 2; `campaign-reservation.json` still `RESERVED`; `replay/{mac-records,rig-records}` empty on the Mac (`find -type f | wc -l` → 0) and on the rig (0); the rig stage tree holds no `execution-*` (`find … -name 'execution-*' | wc -l` → 0); `prebuilds/` empty on both hosts; the directory identities the run re-checks are intact (Mac campaign-root inode 382102675 mode 0700 links 8 = reservation/capability; rig stage dir inode 8265729 mode 0755 links 8 = observation `directoryIdentity`).
- The rig lease is byte-identical to the staged snapshot (`sha256sum` as `_wtcompare` → `3278cd86…`, `state "armed"`, `notAfterMs 1788987134356`, no `.status.json` beside it); the rig sampler was a read-only `/proc` reader over ssh as `hermes-admin` (`LINUX_SAMPLER_SCRIPT`, `:1852-1875`); no `fanout-preflight-runtime-*` roots remain under `$TMPDIR`; no supervisor, controller, role or preflight process is alive on either host; port 4433 has no listener on the rig (`ss -ltnup`/`-lunp`; the two long-lived `rig-min-*-echo-server` processes hold 4446/4447).
- `OUT` (`.release-evidence/transport-comparison/1b9e8092…/fanout-pilot-r1`) exists and is empty, born 02:56:35 — one second after the stage receipt, the same shape A5 launched from (its `OUT` was born 1 s after its receipt and the run passed); the fresh-root check (`stage-live-campaign.ts:2799-2811`) is a stage-time check and the preflight dir is outside it, as D4 states.

## 6. Scope of the last three commits, and the conformance binding (executed)

- `ab6126a3` and `5746a71d` touch only `bin/fanout-physical-preflight.ts` and its test (`git show --stat`; `git -c diff.external= diff --no-ext-diff ab6126a3^ 5746a71d -- tools/compare/bin/fanout-physical-preflight.ts` read in full): sampler liveness by `/proc/$pid` instead of `kill -0`, a kept stderr tail, and the campaign-root hygiene. Both are inside D4's tool, a non-production `cliEntryTs` (`official-io-allowlist.json:88`), unreachable from the wrapper or controller.
- `1b9e8092` non-test diff read in full: it retires `ticker-fanout/rate-250` and adds `rate-25` in `cohort-protocol.ts` (`COHORT_CELL_CARDINALITIES`, label union, grant parameters), `cross-supervisor-protocol.ts` (`FANOUT_EXPANDED_DECLARATION_BY_CELL_ID`), `evidence.ts`, `scenario-registry.ts`, `types.ts` (`25 | 50 | 100`), `client.ts`, `compare-controller.ts` (`PHASE4_GATE_CELLS`, `cohortReadinessDeadlineMs`), `render-phase4-report.ts`, the 9.6/9.7 fragments, `secure_fs.rs` `COHORT_CELLS`, the preflight's `PREFLIGHT_CELL_IDS`, plus fixture vectors regenerated for `rate-100` and the deviation note. Nothing outside D3's rule; the amendment bytes are unchanged (`c98fdb39…`).
- `cohort-cells.conformance.test.ts:123-235` parses D3's table from the amendment and every `| action | cell id | … |` table under `docs/superpowers/plans/deviations/` (oldest first), applying `retire`/`add` and re-sorting each ladder by load. Mutation proof, run in the extracted archive copy in the scratchpad (the repo untouched): unmutated → 32 pass / 0 fail; deviation note hidden → 8 failures; `add` row ingress 250 → 260 → 7 failures; copy restored and `cmp`-identical to the repo file. In the repo from the root: 32 / 0.

## 7. The frozen verifier and render on real seals from this staged pair (executed)

A synthetic campaign root in the scratchpad held a `campaign-index/v2` (candidate, plan/approval/capability/archive digests from the stage receipt, `executionPurpose pilot`, two measured `primary` entries) over the preflight's real `ws`/`wt` `ticker-fanout/rate-100` 1× seals.

- `verify-campaign-index.ts` with the frozen 9.6 argv (external trust bound `63ea97fa…`, both staged public leaves, pass 2 / fail 0 / refused 0 / promotable 0 / flat 0 / pair 0 / sealed 2, `--expect-cells=ticker-fanout/rate-100 --expect-arms=ws,wt --expect-arm-kinds=primary --expect-measured-repetitions=1`) → `VERIFY_CAMPAIGN_INDEX_OK pass=2 fail=0 refused=0 promotable=0 sealed=2 flats=0 pairs=0 promotedCells=0 canonicalFanoutComplete=false integrityOnly=false attestationsVerified=2`, exit 0. `checkExpectedTotals` (`verify-campaign-index.ts:281-331`, called at `:937`) therefore does NOT fire on 1,000 / 100,000, and the attested `serverChildCpu` passed the artifact verifier's finiteness/ordering rule.
- Negative control A (the 2× seals, same argv) → `EXPECTED_TOTALS_MISMATCH: ticker-fanout/rate-100 sealed offered ingress 2000 (cell 1000), accepted ingress 2000 (cell 1000), delivered 200000 (cell 100000) under pilot`, exit 3. The refusal is live.
- Negative control B (efaed8bb's rig public leaf) → `TRUST_PROTOCOL: artifact verify failed … SCHEMA_INVALID_FIELD $.attestationEvidence.cohortObservationEvidence`, exit 3.
- `render-campaign-report.ts --source=sealed-index --allow-non-promotable` on the same root → exit 0; the report prints per arm the topology `1 publisher / 8 workers / 100 subscribers / 101 sessions`, the recomputed totals `offered ingress 1000, accepted ingress 1000, relay writes 100000, delivered 100000, delivered bytes 10000000, post-stop drain 0`, and the three attested figures with shares — ws: busyMs 1888 ms (18.4 %), main-thread 2640 ms (25.7 %), process 4600 ms (44.8 %); wt: 1524 ms (14.9 %), 2700 ms (26.3 %), 10800 ms (105.2 %) — followed by the D5 claim-boundary sentence and `NON-PROMOTABLE PILOT EVIDENCE`.

## 8. Hosts, keys, reachability, validity window (executed, read-only)

- Mac runtime `/usr/local/libexec/webtransport-bun/comparison/1b9e8092…/fanout-pilot-r1/{bun,comparison-supervisor}` → `e0c90ec1…` = `macBunSha256`, `501f5d2a…` = `macSupervisorSha256`. Rig `$RIG_STAGE/bin/comparison-supervisor` → `84caa017…`, `bin/observe-directory-identity` → `fdd18545…`, `/home/hermes-admin/.bun/bin/bun` → `9fd36f87…`; rig copies of receipt (`40462013…`), authority, observation, lease all equal the Mac's.
- Keys: `sudo -n -u _wtcompare /bin/sh -c 'if [ -e "$1" ]; then printf PRESENT; else printf ABSENT; fi'` → PRESENT on the Mac (`/var/db/…/keys/1b9e8092…/fanout-pilot-r1.mac.pk8`, dir 0700 `_wtcompare`, key 0400 83 B, recovery pair beside it) and on the rig (`/var/lib/…/keys/1b9e8092…/fanout-pilot-r1.rig.pk8`, 0400 `_wtcompare` 83 B). The `.pub` leaves beside them hash to `4a85c3bc…` / `5492aac5…` = the staged leaves = the preamble.
- Reachability of abandoned material: key dirs for `efaed8bb`, `ab6126a3`, `b3c71552` (both hosts) and `1622c363` (Mac, public only) still hold `fanout-pilot-r1` material, and their stage dirs and leases remain. None can reach this run: every key path in the frozen command is `…/keys/$CANDIDATE/$CAMPAIGN_ID.*` with `CANDIDATE=1b9e8092…`; the admission gate digests the staged leaves against `4a85c3bc…`/`5492aac5…`; the seals' signature graph is opened only under those leaves (negative control B above); the rig lease binds this candidate's private-key path; `external-trust-bound/v1` binds candidate, campaign, authority, capability, lock, archive, both public keys and both directory identities. The operator `abandon` D5 calls for on `efaed8bb` is still outstanding (housekeeping, not a path).
- Validity window: rig `date` 1788917831 s and Mac 1788918164000 ms at review; `notAfterMs 1788987134356 − now` ≈ 68.97 M ms against the gate's 7,500,000 ms; the admission gate closes at 2026-09-09T18:47:14Z. Lease `janitorUnit` is not loaded on the rig (`systemctl list-units --all 'wtcompare*'` → 0 units), the recorded janitor-absent deviation; expiry is the `notAfterMs` checks.

## 9. Tests executed (HEAD, this worktree; cwd = repo root unless stated)

- `bun test tools/compare/fanout-production-e2e.test.ts` twice, sequentially: 19 pass / 0 fail / 393 expect, 196.82 s, exit 0; then 19 / 0 / 393, 196.48 s, exit 0.
- `bun test tools/compare/bin/fanout-physical-preflight.test.ts` → 39 pass / 0 fail. `fanout-physical-preflight.ts --loopback --ignore-load --transport ws --cell ticker-fanout/rate-100 --pass 1x --out <scratchpad>` → `preflight ws ticker-fanout/rate-100 1x: PASS`, exit 0 (its own fixture pair; the staged campaign-root listing was unchanged afterwards).
- Batches: `cohort-cells.conformance` 32/0, `bin/stage-live-campaign` 54/0 (from the root), `bin/verify-campaign-index` 57/0, `bin/render-campaign-report` 27/0, `campaign-failure-vocabulary` 3/0, `fanout-delivery` 15/0; `cross-supervisor-protocol` + `server-observation-artifact` + `cohort-protocol` + `fanout-relay` 195/0; `bin/compare-controller` + `bin/fanout-role` + `fanout-executor` + `cohort-seal` + `fanout-promotion` + `server-fanout-cohort` + `fanout-supervisor-integration` 357/0. Total 779 pass / 0 fail from the root, plus 38 acceptance passes.
- Not executed here: `cargo test`, the full `bun test` sweep, `check-official-io` (the lead gate's).

## 10. Non-blocking observations

1. **Wrapper `FAILURES` lags `CAMPAIGN_FAILURE_CODES` by one code.** Diffed by execution: the frozen command's list (`frozen-run-wrapper.fragment.sh:281`) has 18 names, `CAMPAIGN_FAILURE_CODES` (`cross-supervisor-protocol.ts:166`) 19; the only difference is `DELIVERY_CONTEXT_MISMATCH`, which `compare-controller.ts:1452` can write into `controller-terminal/v1.failureCode`. Traced through the frozen bytes: the parser exits 3 → `TERMINAL_KIND=FAIL`, `CONTROLLER_RC` stays non-zero, a `CHILD_LIFECYCLE` substitute is written, integrity-only runs, both keys are destroyed — a FAIL reported under a coarser code, never a PASS (a PASS record cannot carry a failure code). Same finding as the Architect's §12.1; fix in a later freeze with a test deriving the list from the vocabulary.
2. **`stage-live-campaign.test.ts` is cwd-dependent.** From `tools/compare` seven tests fail with `ENOENT … tools/compare/tools/compare` (`productionEnvNamesRead`, `:1936-1943`, walks a repo-relative path); from the repo root 54/0. Test hygiene only; the staged bytes are unaffected.
3. **Operator preconditions at launch are not gated by the frozen command.** Mac load during this review reached 6.7–7.3 (`sysctl -n vm.loadavg`; the acceptance and unit batches were running). D4's < 4 held at every preflight start (3.02–3.49). Launch when the Mac reads < 4 and the rig `/proc/loadavg` < 0.5 (0.14 at review), before 2026-09-09T18:47Z.
4. The preflight's warmup CPU interval is the barrier's own epoch (`warmupStartedAtMacNs → warmupCompletedAtMacNs`, 4.77–5.05 s here, `fanout-physical-preflight.ts:2716-2723`, tolerance 600 ms) rather than a literal 5,000 ms; the readings (0.027–0.358) are far under 0.50 either way.
5. `staged-server-launch-record.*.json` carry `stageReceiptSha256` all-zero; the receipt binds the records by digest instead (record before receipt by construction). Cosmetic, pre-existing.
6. Housekeeping still owed to the operator: `abandon` for the `efaed8bb`, `ab6126a3` and `b3c71552` `fanout-pilot-r1` stages (keys present on both hosts), none reachable from this run (section 8).

## 11. Parser self-check

After writing, `parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file and printed the five bindings (recorded in the review transcript). `git status --porcelain -uno` → 0 after this file was written (it lives under the untracked `.trust-staging/` tree).
