CHANGES REQUIRED

# Exact-stage Critic review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b), third stage (run #2 candidate)

Reviewer: Claude workflow subagent, Critic pass, 2026-09-09 (~06:15–06:40Z). Pre-traffic review of exact bytes. Git read-only (`git status --porcelain -uno | wc -l` → 0 before and after; `git rev-parse HEAD` → `839d9ccc…` throughout). Nothing on the rig modified: ssh as `hermes-admin` ran `date`, `cat /proc/loadavg`, `stat`, `sha256sum`, `ls`, `find`, `df`, `ss`, `ps` and `sudo -n -u _wtcompare test -e`/`stat`/`sha256sum`/`cat`/`find` probes only. The frozen command was not run, the controller was not pointed at the staged pair, the preflight was not run. The only file written under the repo is this artifact; every harness lives in the session scratchpad under `scratchpad/critic/` (`rigproof/`, `drive.ts`, `mut.ts`, `mut2.ts`, `rebuild.ts`, `root5*`, `root6`, `staged88*`, `tree/`, logs). The Architect artifact (`reviews/architect.md`, SHA-256 `aefbdffdfba9b8a722bf750a5f3d76b86ca41aeea16eb626f6af75a797a94c33`, first line `CHANGES REQUIRED`) and the four earlier B6 review artifacts under `../../{1b9e8092…,88b14a03…}/fanout-attested-r1/reviews/` were read first and treated as untrusted; every claim below was re-executed here from my own copies and harnesses, and the proving command is cited.

## Bindings

- Stage receipt SHA-256: `cb1b312b81f2e3df0491edbd5c7344abf45e4f5844be93109ded7015cd7285a7`
- Upcoming run command SHA-256: `8898dfd4d7557ac0dae1789f77e2b3f5305e677a8032e6e990832a31c4309349`
- Candidate HEAD: `839d9ccc4919583aabb12662ad68c2de5e94e90b`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

`shasum -a 256` here, equal to the receipt: plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39…`; approval → `0e54de08…`; deviations `2026-09-09-ticker-250-retired-by-preflight.md` → `ccc0964e…`, `2026-09-09-b6-verify-external-trust-bound.md` → `5c23bce5…`.

## Verdict

**Two blockers, both proved by execution on real bytes. Do not launch this stage.**

**Blocker 1 — the Architect's finding is confirmed independently: run #2 refuses execution 65 exactly as run #1 did.** cd8f95da raised the rig's replay ledger (`accepted`, 64 → 84) and left `MAX_SESSIONS_PER_CAMPAIGN = 64` (`crates/native/src/secure_fs.rs:18106`) checked on the same `accept_execution` path (`:18389`) — and nothing on a PASS arm ever removes its session from `RigCohortRuntime::sessions`. My own scratch probes against a byte-identical copy of the crate (§1.2) show 64 accepted executions with no refusal leave `session_count()==64`, `accepted_count()==64 < 84`, and the 65th `accept_execution` returns `CohortRefusal::Overflow` (`COHORT_PROTOCOL`); a PASS arm driven to `rig-server-stopped-ack` stays resident, and `teardown_all` (what `serve` runs at EOF) reaps without releasing. Run #1 reached execution 65 with 11 warmups + 53 measured = 64 accepted and zero refusals, so both counters stood at 64; the fix removed one of two identical conditions. The committed 72/84 test releases with `close_all` between executions, which production calls only from `refuse_arm` — it states its own input — and the new conformance test pins the schedule against the ledger bound only, under a title that claims the rig accepts every frozen schedule.

**Blocker 2 — the stop gate's "6/6 cells comparable" is not enforced by any frozen byte, and I drove three real-seal roots through the whole success path to a wrapper PASS with reports reading 3/5, 4/5 and 0/5 comparable.** `verify-campaign-index` counts pairs by filename (`tools/compare/bin/verify-campaign-index.ts:1233-1237`) and never opens a flat to bind it to the index entry it stands for; the promoted renderer emits the twelve `### WS|WT attested arm` headings (`tools/compare/bin/render-campaign-report.ts:707-717`) *before* `pairTrustContexts`/`compareRunArtifacts` (`:723-737`) and exits 0 on `INCOMPATIBLE` (only `REFUSED` exits 3, `:851-855`); the wrapper's three counts (command lines 627-631) read sealed files, flat files and headings. Executed (§3.4): the real verifier answers `VERIFY_CAMPAIGN_INDEX_OK … flats=10 pairs=5` rc 0 on a root whose two WT flats were swapped across cells and on a root whose WT flat is a different repetition than its WS flat; the promoted render on those roots exits 0 with 10 headings and prints `3/5` and `4/5 cells comparable`; on a root whose index names another stage's leaves the render prints `0/5 … 5 rejected` and still exits 0 with 10 headings. Every clause of the frozen `run_measured_campaign` passes on those roots, so `TERMINAL_KIND=PASS` would be recorded above a report the stop gate rejects. This path is reached on real bytes and it is unenforced; since `secure_fs.rs` must change and both supervisors must be rebuilt for Blocker 1, the restage should carry the one-line fix (§3.4) rather than leave the last stop-gate clause to a human reading `campaign-report.md` after both keys are gone.

Everything else holds (§§3–6): the other three fixes reproduce on run #1's 53 real seals with the real verifier, the real receipt graphs and no stub (53/53 close with leaves, 0/53 without; 5 cells → 10 flats; `pass=50 promotable=50 flats=10 pairs=5 attestationsVerified=50` through the frozen argv; 5/5 comparable with topology, totals, three attested figures and claim boundary per arm), the archive is the candidate tree by blob identity, the run command is byte-identical to `buildFrozenRunCommand("9.7")` from HEAD, the bound recomputes, both keys are present, the lease is armed, both hosts are clean, the diff reaches no traffic-path module, and the local acceptance passed twice.

## 1. Blocker 1 — the rig session bound

**1.1 Read at HEAD (`crates/native/src/secure_fs.rs`, `crates/native/src/bin/comparison-supervisor.rs`).** `MAX_SESSIONS_PER_CAMPAIGN: usize = 64` at `:18106`, whose own comment says "Sessions are released on every arm's terminal path (`close_all`)". `accept_execution` checks `self.accepted.len() >= MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN` at `:18320` (84 = `COHORT_CELLS.len() * 2 * (1+5+1)`, `:18130-18132`) and then `self.sessions.len() >= MAX_SESSIONS_PER_CAMPAIGN → Err(CohortRefusal::Overflow)` at `:18389-18391`, before the insert at `:18416`. Every mutation of the rig runtime's `sessions` (`grep -n "sessions\.remove\|sessions\.retain\|sessions\.clear\|sessions\.drain" secure_fs.rs` → `:18479` remove+reinsert in `accept_cohort`, `:18567` `clear()` in `close_all`; `:24002/:24018` are the Mac module). `close_all` callers (`grep -rn "close_all(" crates/native/src crates/native/tests`): `comparison-supervisor.rs:1118` only, inside `close_arm` (`:1108`), whose sole caller is `refuse_arm` (`:1094`), called from the three `Err(code)` branches of `serve` (`:904/:936/:969`). The rig dispatch for a PASS arm's last frame, `rig-teardown-server-request` (`:586-589`), calls `session.teardown_server(...)` on a `&mut RigCohortSession` borrowed from `runtime.session_mut(...)` (`:571-573`) — a method on the session cannot remove the session from the map it was borrowed out of, and `cohort_request` touches the map nowhere after. `serve` runs once per process (`main`, `:3318`) and on EOF calls `teardown_cohort` → `teardown_all` (`:975`, `:617-622`, `secure_fs.rs:18534-18538`), which reaps every session and clears none.

**1.2 Executed — my own probes, not the Architect's.** Scratch crate copy `scratchpad/critic/rigproof/` (root `Cargo.toml` trimmed to `crates/native`, `Cargo.lock` and the crate copied verbatim; `cmp` of `secure_fs.rs`, `comparison-supervisor.rs` and `Cargo.toml` against HEAD → identical; same source file set). `tests/critic_rig_probe.rs` = HEAD's `rig_cohort_runtime.rs` plus three tests (`scratchpad/critic/cargo-probe-2.log`), `CARGO_TARGET_DIR=~/.cache/wtb-target cargo test -p native --test critic_rig_probe critic_` → **3 passed, 0 failed**:
- `critic_run1_shape_64_pass_arms_no_refusal_then_65th_overflows`: 64 × `accept_execution` with no `close_all` → `accepted_count()==64`, `session_count()==64`, `MAX_SESSIONS_PER_CAMPAIGN==64`, `MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN==84`; the 65th → `CohortRefusal::Overflow`, `.code()=="COHORT_PROTOCOL"`, ledger unchanged at 64.
- `critic_close_all_between_executions_is_what_makes_the_65th_admissible`: the same 64 with `close_all` after each → the 65th accepted, `accepted_count()==65`. The only difference between a PASS arm and a refused arm at this candidate is whether `close_all` ran.
- `critic_pass_arm_after_server_stopped_ack_stays_resident_in_runtime`: `accept_cohort` → the runtime's own session swapped into the harness → spawn server/publisher/8 workers, admit registrations, `mark_ready`, drain, barrier, `stop_and_capture`, `teardown_server` (ack `rig-server-stopped-ack/v1`, stage `ServerStopped`) → swapped back → `session_count()==1`; then `teardown_all` → still `1`.

**1.3 Run #1's own bytes agree.** `campaign-index.json` (my copy `scratchpad/critic/run1/`, 60 entries, `jq`): 53 PASS, then `chat-fanout/subscribers-1000` ws reps 4–5 and wt reps 1–5 FAIL with `failureCode COHORT_PROTOCOL`, `sealedPath null`; `b6-run-1.log` line: `rig refused rig-accept-execution-request/v1 with COHORT_PROTOCOL: overflow`, the wt warmup `SKIPPED` on the same refusal. 11 warmups + 53 measured = 64 accepted, no refusal before the 65th ⇒ `sessions.len()==64` there too — the state my first probe reconstructs.

**1.4 What the committed tests state.** `cargo test -p native --test rig_cohort_runtime` at HEAD → 52 passed (`scratchpad/critic/cargo-head.log`). `one_campaign_accepts_the_largest_frozen_schedule_with_teardown_between_executions` (`rig_cohort_runtime.rs:3841-3900`) calls `runtime.close_all(&mut reaper)` after every `accept_execution` (`:3856`) — the refusal path's release — so its 72-and-84 answer describes a campaign of 72 refused arms. `cohort-cells.conformance.test.ts` ("the rig accepts every execution of every frozen schedule", `git show cd8f95da`) reads `MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN` from the Rust source, checks `72 <= 84`, and never reads `MAX_SESSIONS_PER_CAMPAIGN`'s value against the schedule; its title is false at HEAD.

**1.5 Required change (for the fix agent and the fourth stage).** Release the rig session on the arm's success terminal — after `rig-server-stopped-ack` is written (a success-path `close_arm` that keeps `accepted` so the replay guard and `closed` semantics survive) — and pin it with a test that drives a PASS arm through `ResidentLoop::serve`'s own frame dispatch (the harness at `comparison-supervisor.rs:5838-5960` already opens on the Mac and accepts on the rig over real frames) and then accepts a 65th execution on the same runtime; make the conformance test compare every frozen schedule against *both* bounds, or against the session bound with the release proven. `secure_fs.rs` changes ⇒ rebuild both supervisors, restage (new candidate, receipt, command, keys), re-review.

## 2. (a) Every path from "72 executions sealed" to the wrapper's PASS, and what each was executed on

The frozen 9.7 bytes (rebuilt byte-identical from HEAD, §5.2) run, in order:

| # | Path (frozen command line / code) | Executed on |
|---|---|---|
| 1 | Controller per-execution: rig `accept_execution` (`secure_fs.rs:18250`) | **run #1's real 72 executions → refused at 65 (§1.3); my probes (§1.2)**. Blocker 1. |
| 2 | Controller in-process seal → index entry (`compare-controller.ts:3437` snapshot) | run #1's real 53 seals + 7 FAIL rows (index copy) |
| 3 | `resolveStagedSigningLeaves(spec)` before traffic (`:3373-3381`) | real 88b14a03 stage copy → ok; missing dir / no `stagedDir` / bad rig leaf → `STALE_OR_INVALID_STAGING` (§3.3) |
| 4 | `promoteCampaignFlats` with `sealClosesReceiptGraph` (`:3854-3862`, `:996`, `:1076`) | **real 53 seals**: 53/53 close with leaves, 0/53 without; root6 → 5 cells / 10 flats, chat-1k `PROMOTION_ENTRY_NOT_PASS`+`PROMOTION_MEASURED_SET_INCOMPLETE`; root5 → 5/10/`refusals: []` |
| 5 | Final index write, `controller-terminal.json`, controller rc (`:3863-3898`) | run #1's real terminal record (`controllerExitCode 5`, `CHILD_LIFECYCLE`); the rc-0 branch by reading only (`return { ok: true }` after promotion regardless of promoted count — the wrapper's `EXPECTED_FLATS=12` is what refuses a short promotion, executed as root6 → `TRUST_PROTOCOL … ws primary is FAIL`) |
| 6 | `verify-campaign-index` (lines 611-630): bound recompute, leaves, `--stage-receipt`, counts, cells/arms/kinds/reps, every seal's attestation graph and totals | **real root5 through the frozen argv shape** → `VERIFY_CAMPAIGN_INDEX_OK pass=50 fail=0 refused=0 promotable=50 sealed=50 flats=10 pairs=5 attestationsVerified=50`, rc 0, no stub; wrong bound → `EXTERNAL_TRUST_BOUND_MISMATCH` rc 3; no `--stage-receipt` → `EXTERNAL_TRUST_BOUND_UNVALIDATED` rc 3; root6 60/12/6 + flag → `TRUST_PROTOCOL … chat-fanout/subscribers-1000 ws primary is FAIL` rc 3 |
| 6a | `--expect-canonical-fanout-complete` positive branch (six cells, `evaluateCanonicalFanoutCompletion`) | **stub-shaped only** (`verify-campaign-index.canonical.test.ts` 60/12/6, in the 476-pass batch); root5 + flag → `TRUST_PROTOCOL: … requires a canonical index scheduling all 6 fanout cells` rc 3. `checkExpectedTotals` for `chat-fanout/subscribers-1000` on a complete set: no real set exists. Both refuse closed; same residual the prior reviews named — a wasted stage, never a false PASS. |
| 7 | `render-campaign-report.ts` promoted (lines 623-626): index `stagedDir` → leaves, per-cell `pairTrustContexts` → `compareRunArtifacts` (`cohortPairIdentityRejections`) | **real root5** → rc 0, `formalComparable=5/5 refused=0`, 10 attested-arm headings, `Signing leaves: resolved … (mac 42038271…, rig 371a4f90…)`, per arm topology (`1 publisher / 8 workers / 100 subscribers / 101 sessions`), totals, busyMs / main-thread CPU / process CPU with window %, claim-boundary sentence |
| 8 | Wrapper counts: `*.sealed.json` = 60, flats = 12, headings = 12 (lines 627-631) | real root5 → 50 / 10 / 10 (six cells ⇒ 60 / 12 / 12). **Also executed on three roots the stop gate rejects → all three counts pass (Blocker 2, §3.4)** |
| 9 | Sealed-index diagnostic (only if `RENDER_MODE≠promoted`; not on this path) | root6 → rc 0, 8 `unattested` rows, leaves line present |
| 10 | `on_exit` → key destruction, integrity-only record | the wrapper shell; run #1's own `integrity-only/` shows it executed (`exit-status.txt`, `terminal-kind.txt`) |

Nothing on this list needed a stub except 6a. Row 1 is Blocker 1; row 8 is Blocker 2.

## 3. (b) The four fixes, attacked

**3.1 Acceptance ledger arithmetic (cd8f95da).** `MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN = 6 × 2 × (1 + 5 + 1) = 84`; the 9.7 schedule is 6 × 2 × (1 + 5) = 72; margin 12 = one re-opened execution per arm. `grep -in "retry\|re-open\|reopen" compare-controller.ts` → nothing: the controller has no retry path today, so the margin is unreachable and the ledger cannot be exceeded by warmup + measured + a retried arm. A refused acceptance mints nothing (`:18320` returns before `:18408`). Moot at this candidate because the session bound refuses at 65 first (§1); after the fix the ledger is the only campaign-long bound and 84 > 72 holds.

**3.2 Pair identity (839d9ccc) — every field, on real flats.** Harness `scratchpad/critic/mut.ts`/`mut2.ts` over the real promoted rate-25 flats from root5 (`cohortPairIdentityRejections` at the parsed level, the real retained grant re-canonicalised with `canonicalJson` and re-digested per mutation):
- The real pair → `[]`, and the full `pairCompatibilityRejections` → none.
- Each of the 17 identity fields mutated alone in the WT grant → exactly one `COHORT_PAIR_IDENTITY_MISMATCH`, refused at grant parse ("the retained cohort grant does not parse: executionSha256 does not match the embedded execution" for the execution fields; "grant workload identity differs from the signed execution" for `scenarioHash`; "expectedProcessCount/expectedSessionCount mismatch", "expectedExpandedDeliveries is not ingress * subscribers", "field types" for the cardinalities). A single field cannot be moved without breaking the grant's own self-binding, so a flat cannot pair on what it says about itself.
- The comparison branch itself, on pairs of *real* grants: cross-cell rate-25 ws vs subscribers-250 wt → `cellId, scenarioHash, publisherCount, subscriberCount, expectedOfferedIngress, expectedExpandedDeliveries, messageBytes, measuredDurationMs, runId`; rate-25 vs rate-100 → `cellId, scenarioHash, expectedOfferedIngress, expectedExpandedDeliveries, runId`; cross-repetition (wt rep-1 under the ws rep-3 flat) → `repetitionIndex, runId` ("do not fold to one pair slot: …/ws/measured-3 vs …/wt/measured-1"); cross-candidate (run #1 rate-100 ws vs the real 1b9e8092 pilot rate-100 wt) → `campaignId, candidate, sourceArchiveSha256, stagedCapabilitySha256, executionPurpose, repetitionIndex, repetitionTotal, runId`. That is 15/17 fields refused by name at the comparison branch on real bytes; `repetitionKind` and `workerCount` never differ between any two real seals I hold (all `measured`, all 8 workers) and are exercised at that branch only by the committed shaped-artifact tests — single-field edits of both refuse at parse, above.
- Self-declared slot: artifact `repetitionIndex 9` vs grant 3 → "declares repetitionIndex 9 but its signed grant names 3"; tampered grant digest → "does not decode to its own digest"; `transport` flipped → "grant workload identity differs from the signed execution"; `runId` slot edited → parse refusal. Bulk shape kept: both arms without cohort evidence → `RUN_ID_MISMATCH, RAW_SIDECAR_DIGEST_MISMATCH` (the old rule); one cohort + one bulk → `COHORT_PAIR_IDENTITY_MISMATCH`.
- The median rule makes WS and WT publish the same repetition by construction (`output-policy.ts:1173-1219`, one `pick` for both paths), so `repetitionIndex` agrees on a real promotion; run #1's five promoted cells all pair (5/5).

**3.3 Wrong leaves (2bf0ccd2 / b510036a).** Rig leaf swapped for the pilot's under the 88b14a03 receipt (`staged88-badrig`) → `resolveStagedSigningLeaves` → `STALE_OR_INVALID_STAGING: staged rig public key does not match the receipt`; missing dir → "stage receipt unreadable"; no `stagedDir` → "promotion needs --staged-dir"; index without `stagedDir` → "campaign index records no stagedDir". A receipt-*consistent* wrong stage (the real efaed8bb pilot stage, whose leaves resolve `ok`) handed to `sealClosesReceiptGraph` → **0/50 close**, `promoteCampaignFlats` → 0 cells, every cell `PROMOTION_RECEIPT_GRAPH_INCOMPLETE`: a graph closes under the leaves that signed it or not at all.

**3.4 Renderer vs verifier staging root, and the heading count (Blocker 2 in detail).** The controller writes `stagedDir: spec.stagedDir` verbatim into the index (`compare-controller.ts:3875`, parsed at `:4009-4012` from `--staged-dir=`, which the command passes as `"$MAC_TRUST"` at line 556); the verifier's leaves are `$MAC_TRUST/staging-root/{mac,rig}-supervisor-ed25519.pub` (lines 614-615) and `campaignIndexSigningLeaves` reads `<stagedDir>/staging-root/` through `<stagedDir>/stage-receipt.json` (`output-policy.ts`, b510036a). Same directory by construction; run #1's real index carries `stagedDir` = its own `MAC_TRUST`, and the real render's line 6 names the same two leaf digests the verifier flags name. Executed, promoted argv (lines 623-626) over roots cloned from the promoted root5:

| Root | verify-campaign-index (frozen shape) | render rc | headings | report line 4 |
|---|---|---|---|---|
| `root5` (real) | OK, rc 0 | 0 | 10 | 5/5 comparable |
| `root5-swap` (rate-25 ↔ subscribers-250 WT flats swapped) | **OK, rc 0** | **0** | 10 | 3/5 comparable; 2 `INCOMPATIBLE` `COHORT_PAIR_IDENTITY_MISMATCH … cellId` |
| `root5-xrep` (rate-25 WT flat = rep-1 seal, WS = rep-3) | **OK, rc 0** | **0** | 10 | 4/5; `repetitionIndex: 3 vs 1` |
| `root5-pilotstage` (index `stagedDir` → the real pilot stage) | n/a (verifier reads `$MAC_TRUST`, not the index) | **0** | 10 | 0/5; 5 `INCOMPATIBLE` `COHORT_EXPORT_RECEIPT_INVALID` |
| `root5-badrig` (`stagedDir` → receipt-inconsistent rig leaf) | n/a | 3 | 10 | 0/5; 5 `REFUSED (SIGNING_LEAVES_UNRESOLVED)` |
| `root5-nostage` (`stagedDir` → nonexistent) | n/a | 3 | 10 | 0/5; 5 `REFUSED` |

Wrapper counts on every row: sealed 50, flats 10, headings 10 (⇒ 60/12/12 for six cells). The heading count survives a refused cell (rc 3 is what catches it, via `RENDER_RC`) and survives an `INCOMPATIBLE` cell with rc 0, which nothing catches: `renderFromFlats` returns `RENDER_REFUSED_EXIT_CODE` only when `refused > 0` (`render-campaign-report.ts:851-855`), `rejected` never reaches the exit code, and the verifier binds a flat to its cell by filename only (`verify-campaign-index.ts:1252-1300`: `safeToCell` from the name, then the cell's *index* set through the gate — the flat's bytes are verified as a seal but never matched to the median entry's `artifactSha256`/cell/repetition). The live campaign's stagedDir and leaves are right by construction (rows 4-6 are renderer-only attacks), and on run #1's real seals every pair is comparable — but the frozen bytes cannot tell a 6/6 report from a 3/6 one, and the stop gate names 6/6. Fix for the restage, either of: (i) the renderer exits non-zero in promoted mode when `comparable < cells.length`; (ii) the wrapper adds `test "$(rg -c '^\| `.*` \| \*\*COMPATIBLE\*\*' "$OUT/campaign-report.md")" = 6` beside the heading count; plus, for the verifier, bind each flat's `artifactSha256` to the gate's median entry.

## 4. (c) What the diff reaches

`git diff --stat 88b14a03..839d9ccc` → 18 files, +2077/−107, the four commits read with `git show`. Rust: `secure_fs.rs` +39/−3 and the test file only; the non-comment production lines are the two constants, the derived bound, the `accepted.len()` comparison and `accepted_count()` — nothing in `comparison-supervisor.rs`, nothing in the child/traffic code. TypeScript production files: `compare.ts`, `evidence.ts` (one union member), `output-policy.ts` (+214, the leaf readers), `remote-supervisor.ts` (re-export of `TRUST_BOOTSTRAP_STAGING_ROOT`, now importing `output-policy.ts`), `render-report.ts`, `verify-artifact.ts`, and the five `bin/` CLIs. Import sweep over the **extracted archive** (`scratchpad/critic/tree/tools/compare`, `grep -rl` per changed module, tests excluded): `output-policy.ts` is imported by the controller, the CLIs, `render-report.ts`, `run-campaign.ts`, `remote-supervisor.ts` and `verify-artifact.ts`; `compare.ts` by the two renderers and `render-report.ts`; `remote-supervisor.ts` by the controller, the preflight, the stage/mint tools and the fixture signer — none by `server.ts`, `bin/fanout-role.ts`, `scenarios/fanout-relay.ts`, `scenarios/fanout-wire.ts` or `adapters/*`. `server.ts` imports exactly `adapters/{transport,ws,wt}.ts, child-pipe-protocol.ts, cohort-protocol.ts, cross-supervisor-protocol.ts, scenario-registry.ts, scenarios/fanout-relay.ts, scenarios/fanout-wire.ts, secure-fs.ts, server-snapshot-protocol.ts, types.ts, node:fs`; `fanout-role.ts` imports `child-pipe-protocol.ts, cohort-protocol.ts, cross-supervisor-protocol.ts, scenarios/fanout-relay.ts, scenarios/fanout-wire.ts, secure-fs.ts, bun:ffi, node:crypto, node:tls` — no changed module in either graph (the one `adapters/transport.ts` hit is a comment at `:156`). `evidence.ts` is imported by `adapters/ws-worker.ts`/`sink-worker.ts`, and its only change is a type-union member (no runtime effect). Timing: `git diff … -- compare-controller.ts remote-supervisor.ts | grep -iE 'timeout|_MS'` → nothing. Role leaves unchanged since the pilot (`roles/server.ts` `aeef5d1b…`, `roles/fanout-role.ts` `d9c3f971…`, equal on the rig's role root by `sha256sum`). Both supervisors rebuilt: Mac `3940724c…`, rig `d4f8352a…` = `/tmp/ws-wt-linux-build.Jcujvj/target/release/comparison-supervisor`, whose `secure_fs.rs` `dce68909…` and `comparison-supervisor.rs` `fccf4dd1…` equal HEAD's files (`sha256sum` there, `shasum` here). The D4 preflight receipts under `preflight/1b9e8092…/` therefore still describe this candidate's physics; the rig binary's only behavioural change is the ledger bound.

## 5. (d) Validity, wall clock, hosts

**5.1 Validity.** Mac `date +%s` 1788935184 (06:26:24Z) vs rig 1788935140 at 06:25:40Z (no skew); `notAfterMs 1789005125524` = 2026-09-10T01:52:05Z; remaining 69,941,524 ms > required 21,600,000 (`RUN_TIMEOUT_MS + 5,400,000`); the admission gate (command lines 521-523) closes at **2026-09-09T19:52:05Z**. Lease on the rig: `state armed`, `lastTransitionReason stage-only-commit`, `privateKeyPath` this key, digest `cf79651c…` = the staged snapshot = the receipt.

**5.2 Bytes recomputed.** `HEAD^{tree}` → `4e5ccdb5…` = `candidateTreeOid`; `source.tar` → `5bed4213…`, 54,425,600 B, 2,073 members; extracted 1,779 regular files, 0 symlinks; `git hash-object --stdin-paths` per path `diff`ed against `git ls-tree -r 4e5ccdb5` → **zero differences**. `shasum -a 256` over the stage: receipt `cb1b312b…`, command `8898dfd4…` (680 lines), authority `ee13bccd…` = `xxd -p authority-digest.bin`, lock `743c8775…`, manifest `803662b0…`, capability `cb9ec938…`, TLS cert `ba02e2fb…`, lease snapshot `cf79651c…`, mac leaf `76d7cf54…`, rig leaf `19c238cc…`, launch records `161472e9…`/`e3f00f9b…`/`10162e0a…`/`3d46ea9c…` = the receipt's map, `roles/*` as above and `stage-live-campaign.ts` `2e3506d9…`; `linux-stage-observation.json` `ebe5394b…`/`/tmp/ws-wt-linux-build.Jcujvj/tools/compare`/`d4f8352a…`; `incoming/*` `cmp`-identical to the staged originals (10/10); `bin/`, `prebuilds/`, `replay/{mac,rig}-records` empty; `campaign-root/` = lock, reservation, manifest, r1-red bundle, source-archive receipt, ssh-host receipt — no execution records. External bound: `externalTrustBoundSha256` from HEAD over the receipt's ten preimage fields → `912cc04f…` = receipt. Frozen command: `scratchpad/critic/rebuild.ts` → `buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, …})` → 32,378 B, `cmp` **byte-identical**, lines 666-680 identical to `frozen-run-section-9.7.fragment.sh`; the 13 `unset`s, `RUN_TIMEOUT_MS=16200000`, `STAGE_NOT_AFTER_MS=1789005125524`, leaf digests, `$CANDIDATE`-scoped key paths, admission gates before traps, the section literals (six cells in ladder order, `REPS=5`, 60/60/12/6/60, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted`, `CAMPAIGN_TIMEOUT_MS=14400000`) all as the brief states.

**5.3 Hosts.** Mac: `$MAC_RUNTIME/{comparison-supervisor,bun}` → `3940724c…`/`e0c90ec1…` = receipt (= `~/.bun/bin/bun`); `sudo -n -u _wtcompare`: `keys/839d9ccc…/fanout-attested-r1.mac.pk8` **PRESENT** (0400, 83 B), `.pub` → `76d7cf54…`; `OUT` exists and is empty; `exact-stage-approval.json` absent (correct); no comparison process and nothing on 4433 after each acceptance run; `portrange` 49152–65535, `tcp.msl 15000`, 6 TIME_WAIT; no `ps axww` storm. Rig: stage dir `inode 8265845 / 8 links / 0755 / uid 1001` = the observation; `bin/comparison-supervisor` `d4f8352a…`; rig copies of receipt/capability equal; `keys/839d9ccc…/fanout-attested-r1.rig.pk8` **PRESENT** (0400 `_wtcompare`, 83 B), `.pub` → `19c238cc…`; 44 key dirs; `ss` → nothing on 4433; only the two `rig-min-*-echo-server` processes; 0 `execution-*` records, 0 replay files; `/tmp` tmpfs 2.1 G free; load 0.00.

**5.4 Wall clock on real bytes.** Run #1 (seal mtimes, UTC): launch 03:53:58, first ticker seal 03:54:40, three ticker cells (36 executions) done 04:03:55 → ≈15.5 s/execution; chat-250 + chat-500 (24 executions) 04:03:55 → 04:18:58 → ≈38 s/execution; the four chat-1k executions that ran, ≈40 s each. Projected 72: ≈9.5 + ≈24 min ≈ **34 min**, far inside `CAMPAIGN_TIMEOUT_MS` 14,400 s and the 21,600 s validity requirement; verifier + render on 50 real seals took seconds here.

**5.5 Mac load — operator gate.** `vm.loadavg` 3.84 / 6.17 / 6.69 before the first acceptance run, 4.57 / 5.76 / 6.41 before the second, 4.96 / 5.15 / 5.98 after (no comparison process alive); top consumers `WindowServer` 51 %, `Codex (Service)` 14.5 % + `Codex (Renderer)` 5 % (the ChatGPT app), `Claude Helper` 9 %. Same gate as the earlier reviews: launch only with 1-min load < 4, the ChatGPT/Codex app quiet, rig < 0.5 (0.00 now), no acceptance run in flight, before 19:52:05Z — after a fourth stage.

## 6. Carry-over from the earlier B6 reviews, at this candidate

- Held non-blocking items still hold: the quarantine's proof is structural (`output-policy.ts` triple); `promotionEligible` has no production reader (`grep -rn promotionEligible tools/compare --include='*.ts' | grep -v test` → `verify-artifact.ts` writers only); wrapper `FAILURES` lacks `DELIVERY_CONTEXT_MISMATCH` (`grep -c` on the frozen file → 0; `cross-supervisor-protocol.ts:135/166`) — a FAIL under a coarser code; the lease's `janitorUnit` names a unit that does not exist; the controller's positional render leaves `report.md` beside `campaign-report.md` (run #1's log: `render-campaign-report exited 1 (flats/index still landed)`, harmless to every count).
- Older keys: `keys/1b9e8092…/fanout-attested-r1.{mac,rig}.pk8` PRESENT on both hosts, `keys/efaed8bb…/fanout-pilot-r1.mac.pk8` PRESENT on the Mac; `88b14a03…` private halves destroyed by run #1's exit as designed. Unreachable from this run (`$CANDIDATE`-scoped paths, leaf digests gated at admission, bound recomputed by the verifier); housekeeping `abandon` still owed.
- The first stage's finding 2 ("no positive canonical verifier test") is discharged only by the stub-shaped 60/12/6 test; the six-cell positive branch remains unexecuted on real bytes (§2 row 6a).
- Duplicate `--external-trust-bound-sha256` flags: first occurrence wins (executed by accident: two flags, the second wrong → OK; one wrong flag → `EXTERNAL_TRUST_BOUND_MISMATCH` rc 3). The frozen command carries the flag once; informational.

## 7. Executed here

- Local acceptance `bun test tools/compare/fanout-production-e2e.test.ts` **twice**: 19 pass / 0 fail / 393 expect, **199.37 s** (06:17:53–06:21:12Z) and **197.38 s** (06:21:38–06:24:55Z), exit 0 both (`scratchpad/critic/acceptance-{1,2}.log`); afterwards no supervisor/controller/role/server process and no 4433 listener.
- `bun run typecheck` → clean. `bun test` over `compare-controller`, `render-campaign-report`, `verify-artifact` (bin), `evidence`, `cohort-cells.conformance`, `verify-campaign-index.canonical`, `verify-campaign-index`, `output-policy`, `stage-live-campaign`, `fanout-promotion`, `compare` → **476 pass / 0 fail / 2475 expect** (9.84 s). `bun tools/compare/check-official-io.ts` → `failure-count=0`.
- `cargo test -p native --test rig_cohort_runtime` at HEAD → 52 passed; `cargo test … --test critic_rig_probe critic_` on the byte-identical crate copy → 3 passed (§1.2).
- Scratch harnesses: `drive.ts` (§2 rows 3-4, §3.3), `mut.ts`/`mut2.ts` (§3.2), `rebuild.ts` (§5.2), the verifier and render CLI matrix (§2 rows 6-9, §3.4), the archive extraction + blob diff, the rig and Mac probes.
- Not executed: the frozen command, the controller against the staged pair, the preflight, the full `bun test`.

## 8. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` over this file refuses it with `EXACT_STAGE_APPROVAL_REVIEW_NOT_APPROVED` — correct, the first line is CHANGES REQUIRED and this artifact must not be consumable as an approval; `extractExactStageReviewLabel` returns each of the five bindings above exactly once (recorded in the review transcript).
