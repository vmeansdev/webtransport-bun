CHANGES REQUIRED

# Exact-stage Architect review — B6 canonical `fanout-attested-r1` (section 9.7, phase-b), third stage

Reviewer: Claude workflow subagent, Architect pass, 2026-09-09 (~05:55–06:15Z). Pre-traffic review of exact bytes. Git read-only (`git status --porcelain -uno | wc -l` → 0 before and after; `git rev-parse HEAD` → `839d9ccc…` throughout). Nothing on the rig modified: ssh as `hermes-admin` ran only `date`, `cat /proc/loadavg`, `stat`, `sha256sum`, `ls`, `find`, `df`, `ss`, `ps` and `sudo -n -u _wtcompare test -e`/`stat`/`sha256sum`/`cat` probes. The frozen command was not run, the controller was not pointed at the staged pair, the preflight was not run. The only file written under the repo is this artifact; every harness lives in the session scratchpad (`scratchpad/arch/`). The two prior B6 review pairs under `../../{1b9e8092…,88b14a03…}/fanout-attested-r1/reviews/` were read first and treated as untrusted.

## Bindings

- Stage receipt SHA-256: `cb1b312b81f2e3df0491edbd5c7344abf45e4f5844be93109ded7015cd7285a7`
- Upcoming run command SHA-256: `8898dfd4d7557ac0dae1789f77e2b3f5305e677a8032e6e990832a31c4309349`
- Candidate HEAD: `839d9ccc4919583aabb12662ad68c2de5e94e90b`
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`
- Campaign ID: `fanout-attested-r1`

Recomputed with `shasum -a 256`, equal to the receipt: plan `docs/superpowers/plans/2026-09-08-physical-budget-amendment.md` → `c98fdb39…`; approval → `0e54de08…`.

## Verdict

**BLOCKER — run #2 fails at execution 65 exactly as run #1 did.** The cd8f95da fix raised the rig's acceptance-ledger bound (`accepted`, 64 → 84) but the rig's *live-session* bound (`MAX_SESSIONS_PER_CAMPAIGN = 64`) is checked on the same accept path (`crates/native/src/secure_fs.rs:18389`, inside `accept_execution`, before the session is inserted at `:18416`), and nothing on a PASS arm's terminal path ever removes a session from `RigCohortRuntime::sessions`. Run #1 reached execution 65 with 64 accepted executions and no refusal, so *both* counters stood at 64; the fix removed one of the two conditions that produced `COHORT_PROTOCOL: overflow`. I proved this by execution (§1.2): a PASS arm driven through the exact session calls the binary dispatches stays resident after `rig-server-stopped-ack`, and 64 accepted executions with no refusal make the 65th `accept_execution` refuse `Overflow` with the ledger at 64 < 84. The committed Rust test that "accepts 72" calls `close_all` between executions — a call production makes only from `refuse_arm` — so it states its own input and does not cover the production path. This is a stop-gate path (60 fresh PASS seals) reachable only by stub at this candidate, hence CHANGES REQUIRED rather than a carry-over.

Everything else I was asked to verify holds (§§2–5): the other three fixes reproduce on run #1's real seals (53/53 close with leaves, 5 cells → 10 flats → the real verifier through the frozen argv answers 50/50/10/5 with 50 real attestations and no stub, the promoted render says 5/5 comparable with 10 attested-arm headings), the archive is the candidate tree by blob identity, the run command is byte-identical to `buildFrozenRunCommand("9.7")` from HEAD, both keys are present, the lease is armed, both hosts are clean, and the rig binary is built from HEAD's sources. None of that reaches execution 65.

## 1. The rig cap (fix cd8f95da) — the blocker

**1.1 Read at HEAD.** `git show cd8f95da -- crates/native/src/secure_fs.rs`: +39/−7, constants `WARMUP_REPETITIONS_PER_ARM = 1`, `CANONICAL_MEASURED_REPETITIONS = 5`, `MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN = COHORT_CELLS.len() * 2 * (1+5+1)` (= 84 for the six rows), the `accepted.len()` check at `:18320` now against 84, and `accepted_count()`. `MAX_SESSIONS_PER_CAMPAIGN = 64` (`:18106`) is untouched and is still checked in `accept_execution` at `:18389` ("Built before anything is committed … `if self.sessions.len() >= MAX_SESSIONS_PER_CAMPAIGN { return Err(CohortRefusal::Overflow) }`") and in `accept_cohort`'s no-session branch at `:18488`. Every mutation of the rig runtime's `sessions` map (`awk 'NR>=18090 && NR<=18600 && /self\.sessions/'`): insert `:18416` (accept_execution), remove+reinsert `:18479/:18482/:18518` (accept_cohort, net zero), `clear()` `:18567` (`close_all`). No other removal exists. `close_all` callers crate-wide (`grep -rn "close_all(" crates/native/src crates/native/tests`): `src/bin/comparison-supervisor.rs:1118` only, inside `close_arm` (`:1108`), whose sole caller is `refuse_arm` (`:1094`). `serve` (`:814`) is called once per process (`main`, `:3318`), reads frames to EOF, and on EOF calls `teardown_cohort` → `teardown_all` (`secure_fs.rs:18533-18538`), which reaps and does not clear. A PASS arm's last rig frame, `rig-teardown-server-request` (`:586-589`), dispatches `session.teardown_server(...)` — a method on the session, which cannot remove itself. The Mac side is not the cap: its `executions`/`sessions` maps are released by `terminal_execution` on both success paths (`:24368`, `:24525`), and run #1's rig refusal was of a Mac-signed receipt for execution 65, i.e. the Mac had already constructed it.

**1.2 Executed.** Scratch copy of the workspace (`scratchpad/arch/rigproof/`, `Cargo.toml` members trimmed to `crates/native`, crate copied verbatim) plus `tests/rig_success_path_probe.rs` = a copy of `rig_cohort_runtime.rs` with two appended tests. `cd rigproof && CARGO_TARGET_DIR=~/.cache/wtb-target cargo test -p native --test rig_success_path_probe probe_` → **2 passed, 0 failed** (`scratchpad/arch/rigprobe.log`):
- `probe_a_pass_arm_torn_down_through_the_binarys_calls_leaves_its_session_resident`: `runtime.accept_cohort` → swap the runtime's own session into the harness → spawn server, publisher, 8 workers, admit registrations, `mark_ready`, `drive_to_drained`, barrier, `stop_and_capture`, `teardown_server` (ack `rig-server-stopped-ack/v1`, stage `ServerStopped`) → swap back → `session_count() == 1` and `session_mut(execution)` still `Ok`.
- `probe_sixty_four_pass_arms_with_no_refusal_overflow_the_65th_on_the_session_bound`: 64 × `accept_execution` with no `close_all` → `accepted_count() == 64`, `session_count() == 64`, `64 < MAX_ACCEPTED_EXECUTIONS_PER_CAMPAIGN`; the 65th → `CohortRefusal::Overflow`, code `COHORT_PROTOCOL`, ledger unchanged at 64.

The committed suite at HEAD: `CARGO_TARGET_DIR=~/.cache/wtb-target cargo test -p native --test rig_cohort_runtime` → 52 passed (`the_acceptance_ledger_is_bounded_by_the_frozen_schedule_not_the_session_bound`, `one_campaign_accepts_the_largest_frozen_schedule_with_teardown_between_executions` both ok). The second drives `accept_execution` → `close_all` → next; `close_all` is the refusal path's release (§1.1), so the test's 72-and-84 answer is about a campaign of 72 refused arms, not 72 PASS arms. `cohort-cells.conformance.test.ts` pins the TS constant to the Rust bound, not the session bound.

**1.3 What run #1 says.** `campaign-index.json` (60 entries): 53 PASS then `chat-fanout/subscribers-1000` ws reps 4–5 and wt reps 1–5 FAIL with `rig refused rig-accept-execution-request/v1 with COHORT_PROTOCOL: overflow`; the wt warmup made the eighth refusal. 11 warmups + 53 measured = 64 accepted, zero refusals before the 65th, so `sessions.len()` was also 64 there. The same refusal, at the same frame, with the same code, is what the fixed binary returns (§1.2).

**1.4 Required change.** Release the rig session on the arm's success terminal (after `rig-server-stopped-ack` is written, or a success-path `close_arm` that keeps `accepted` so the replay guard and `closed` semantics survive), pin it with a test that drives a PASS arm through `ResidentLoop::serve`'s own dispatch and then accepts a 65th execution on the same runtime — and, since `secure_fs.rs` changes, rebuild both supervisors and restage (new candidate, receipt, command, keys). `MAX_SESSIONS_PER_CAMPAIGN` should then bound concurrency as its comment claims.

## 2. The other three fixes on run #1's real bytes (all executed)

Scratch copies: `scratchpad/arch/run1/fanout-attested-r1/` (campaign root: index, 53 seals, terminal record), `scratchpad/arch/staged88/` (the 88b14a03 `stage-receipt.json` + `staging-root/` leaves; `resolveStagedSigningLeaves({stagedDir})` → ok). Roots built by `scratchpad/arch/drive.ts`: `root6` (all 60 entries, `sealedPath` rewritten to the copy, `stagedDir` → `staged88`) and `root5` (the five complete cells, 50 entries, `scheduledMeasuredArms 50`).

**2.1 Promotion (2bf0ccd2).** `sealClosesReceiptGraph(entry, anchors)` with the index's own anchors (candidate `88b14a03…`, capability `a2e21409…`, archive `09390c30…` = the receipt's) plus the leaves → **53/53 true; without the leaves 0/53**. `promoteCampaignFlats` (the controller's own function, the frozen path's gate) on `root6` → `promotedCells` = rate-25/50/100, subscribers-250/500; 10 flats written; refusal `chat-fanout/subscribers-1000: PROMOTION_ENTRY_NOT_PASS (ws rep 4, ws rep 5, wt reps 1–5), PROMOTION_MEASURED_SET_INCOMPLETE (ws 4,5; wt 1–5)`. On `root5` → the same five cells, 10 flats, `refusals: []`. The frozen argv's `--staged-dir="$MAC_TRUST"` is where `resolveStagedSigningLeaves` reads `stage-receipt.json` and `staging-root/` (command lines 21, 556); `useInProcessSeal` is true when the Mac supervisor has control pipes (`compare-controller.ts:3326`), which it does in `--stage=full`.

**2.2 Verifier (b510036a, 839d9ccc) through the frozen 9.7 argv shape** (command lines 611–630 verbatim, counts scaled to the root; bound = the 88b14a03 receipt's `cac9d358…`, both real leaves, `--stage-receipt`):
- `root5`, 50/0/0/50/10/5/50, five cells, no completion flag → `VERIFY_CAMPAIGN_INDEX_OK pass=50 fail=0 refused=0 promotable=50 sealed=50 flats=10 pairs=5 promotedCells=0 canonicalFanoutComplete=false attestationsVerified=50`, rc 0. **No stub anywhere**: real `verifyRunArtifact`, real cohort reconstruction and `checkExpectedTotals` for all five rows, real attestation graphs under the real leaves, the real quarantine with the real recomputed bound, and the new `cohortPairIdentityRejections` on five real ws/wt pairs whose run ids and raw sidecar digests differ.
- `root5` with `--expect-canonical-fanout-complete` → `TRUST_PROTOCOL: canonical fanout completion requires a canonical index scheduling all 6 fanout cells`, rc 3 (correct: the completion branch needs six cells; it has positive execution only on the tree's stub-shaped 60-seal canonical test, as the prior reviews recorded).
- `root6`, the frozen 60/12/6 argv with the flag → `TRUST_PROTOCOL: chat-fanout/subscribers-1000 ws primary is FAIL; every registered measured arm must PASS`, rc 3.
- `root5` without `--stage-receipt` → `EXTERNAL_TRUST_BOUND_UNVALIDATED … pass --stage-receipt with both staged public leaves`, rc 3.

**2.3 Render (b510036a).** `bun tools/compare/bin/render-campaign-report.ts --candidate=88b14a03… --campaign-id=fanout-attested-r1 --campaign-root=root5 --output=root5/campaign-report.md` (the wrapper's promoted argv, lines 623–626) → rc 0, `formalComparable=5/5 refused=0`; report line 4 `5/5 cells comparable; 0 rejected or quarantined`, line 6 `Signing leaves: resolved from the campaign index's stagedDir (mac 42038271…, rig 371a4f90…)`; `rg -c '^### (WS|WT) attested arm'` → **10**; each arm section carries the topology line (`1 publisher / 8 workers / 100 subscribers / 101 sessions`), the totals line (rate-25: offered 250, accepted 250, relay writes 25000, delivered 25000, bytes 2500000, drain 0), the three attested figures (busyMs, main-thread CPU, process CPU, each with its window %) and the claim-boundary sentence. The wrapper's other two counts on `root5`: 50 sealed, 10 flats (→ 60/12/12 for six cells; the hardcoded 12 matches six promoted cells × two arms). The same promoted render over `root6` (five promoted cells beside one unpromoted) → rc 0, 5/5. Sealed-index diagnostic over the 60-entry `root6` (`--source=sealed-index --allow-non-promotable`) → rc 0, `sealedIndexEntries=60`, the signing-leaves line present, the seven FAIL rows `unattested`, 0 attested-arm headings (it re-verifies nothing, as documented).

**2.4 Reached only by stub, stated plainly.** The six-cell `--expect-canonical-fanout-complete` positive branch and `checkExpectedTotals` for `chat-fanout/subscribers-1000` (three PASS seals exist but no complete set) — both refuse closed and are the same residual the prior reviews named. Nothing in §2 needed a stub at this candidate. §1 is the path that could only be reached by stub, and it is the blocker.

## 3. The bytes (all recomputed here)

- `git rev-parse HEAD` → `839d9ccc…`; `HEAD^{tree}` → `4e5ccdb5c40d75ea4d193d4b49f9222ed7f5f58a` = `candidateTreeOid`; porcelain 0. `source.tar` → `5bed4213…`, 54,425,600 B, `tar -tf | wc -l` 2,073; inventory → `72552215…`. Extracted to `scratchpad/arch/tree/`: 1,779 regular files, 0 symlinks; `git hash-object --stdin-paths` per path `diff`ed against `git ls-tree -r 4e5ccdb5` (1,779 blobs: 1,768 × 100644, 11 × 100755) → **zero differences**.
- `shasum -a 256` over the stage: receipt `cb1b312b…`, command `8898dfd4…` (680 lines by `wc -l`; the brief's 674 is the same file), authority `ee13bccd…` = `authority-digest.bin`, lock `743c8775…`, manifest `803662b0…`, capability `cb9ec938…`, TLS cert `ba02e2fb…`, lease snapshot `cf79651c…`, mac leaf `76d7cf54…`, rig leaf `19c238cc…`, launch records ws/bulk `161472e9…`, ws/fanout `e3f00f9b…`, wt/bulk `10162e0a…`, wt/fanout `3d46ea9c…`, roles `server.ts` `aeef5d1b…`, `fanout-role.ts` `d9c3f971…`, `stage-live-campaign.ts` `2e3506d9…`; `linux-stage-observation.json` `directoryIdentitySha256 ebe5394b…`, `roleRootPath /tmp/ws-wt-linux-build.Jcujvj/tools/compare`, `linuxSupervisorSha256 d4f8352a…`. `incoming/` copies equal the staged originals; `bin/`, `prebuilds/`, `replay/{mac,rig}-records` empty; `campaign-root/` = lock, reservation, manifest, r1-red bundle, source-archive receipt (`finalCandidateTreeOid 4e5ccdb5…`), ssh-host receipt — no execution records.
- Frozen command: `scratchpad/arch/rebuild.ts` calls `buildFrozenRunCommand({section:"9.7", executionPurpose:"canonical", runTimeoutMs:16_200_000, stageReceipt, paths from the preamble})` from HEAD → 32,378 B, `cmp` **byte-identical**, SHA `8898dfd4…`; lines 666–680 `diff` against `frozen-run-section-9.7.fragment.sh` → identical; `executionPurposeToSection("canonical")` → `{9.7, 16_200_000}` (`stage-live-campaign.ts:1097`). Preamble: 13 `unset`s, `CANDIDATE 839d9ccc…`, `RUN_TIMEOUT_MS=16200000`, `STAGE_NOT_AFTER_MS=1789005125524`, both leaf digests, capability/lock/archive/bound = receipt, `$CANDIDATE`-scoped key paths. Admission gates (519–528: leaf digests, `STAGE_NOT_AFTER_MS − now > 21,600,000`, the sole `verify-stage-approval`) run before the traps (532–535). Controller argv (550–560) as in §2.1; success verifier (611–630) passes `--stage-receipt="$MAC_TRUST/stage-receipt.json"`; section literals: the six cells in ladder order, `REPS=5`, `PURPOSE=canonical`, `CAMPAIGN_TIMEOUT_MS=14400000`, 60/60/12/6/60, `EXPECT_CANONICAL_FANOUT_COMPLETE=1`, `RENDER_MODE=promoted`.
- Time: `date -u` 06:09:38Z → remaining 70,947,524 ms > 21,600,000; the gate closes at **2026-09-09T19:52:05Z**; `notAfterMs` = 2026-09-10T01:52:05Z. Mac `1788934387` vs rig `1788934388` (no skew).
- Mac: `/usr/local/libexec/webtransport-bun/comparison/839d9ccc…/fanout-attested-r1/{comparison-supervisor,bun}` → `3940724c…`/`e0c90ec1…` = receipt (= `~/.bun/bin/bun`). `sudo -n -u _wtcompare`: `keys/839d9ccc…/fanout-attested-r1.mac.pk8` **PRESENT** (0400, 83 B), `.pub` → `76d7cf54…`, recovery pair present. `OUT` exists and is empty; `exact-stage-approval.json` absent (correct). No supervisor/controller/role/server process, nothing on 4433 (after my acceptance run finished).
- Rig: stage dir `inode 8265845 / 8 links / 0755 / uid 1001` = the observation; `bin/comparison-supervisor` **`d4f8352a…`** = receipt = `/tmp/ws-wt-linux-build.Jcujvj/target/release/comparison-supervisor` (same digest, 2,054,928 B, built 05:54Z from that tree, whose `crates/native/src/secure_fs.rs` `dce68909…` and `bin/comparison-supervisor.rs` `fccf4dd1…` equal HEAD's files by `sha256sum`/`shasum`; the 88b14a03 stage's binary was `84caa017…`); `observe-directory-identity` `46e71758…`, `~/.bun/bin/bun` `9fd36f87…`; rig copies of receipt/authority/lock/manifest/capability equal; role root `server.ts`/`fanout-role.ts` → `aeef5d1b…`/`d9c3f971…`; `keys/839d9ccc…/fanout-attested-r1.rig.pk8` **PRESENT** (0400 `_wtcompare`, 83 B), `.pub` → `19c238cc…`; lease → `cf79651c…` = the staged snapshot (`lastTransitionReason stage-only-commit`, `notAfterMs 1789005125524`, `privateKeyPath` this key), no `.status.json`; `ss` → nothing on 4433; only the two long-lived `rig-min-*-echo-server` processes; 0 `execution-*` records, 0 replay files; `/tmp` tmpfs 2.1 G free; load 0.00.

## 4. Carry-over from the earlier B6 reviews, at this candidate

- The diff `88b14a03..839d9ccc` (18 files, +2077/−107, four commits, read with `git show`) touches no relay, transport, server, worker or role file and no timing constant in the controller (`git diff … -- compare-controller.ts remote-supervisor.ts | grep -iE 'timeout|_MS'` → nothing); `secure_fs.rs` is the ledger bound only. The D4 preflight receipts still describe this candidate's physics; wall-clock sums (≈34 min expected, 2.92 h worst < 14,400 s) stand; the 12-heading count matches six promoted cells (§2.3).
- Held non-blocking items still hold: the quarantine's proof is structural; `promotionEligible` has no production reader; wrapper `FAILURES` lacks `DELIVERY_CONTEXT_MISMATCH` (`grep -c` on the frozen file → 0; a FAIL under a coarser code); the lease's `janitorUnit` has no unit; the older stages' keys remain present (housekeeping, unreachable from this run).
- Mac load: `vm.loadavg` 8.5 / 9.5 / 7.7 right after my acceptance run, with `WindowServer` 33 %, `Codex Framework` (ChatGPT app) 23 %, Claude helpers 21 % — the same operator gate as before (1-min load < 4, no `ps` storms, rig < 0.5).

## 5. Executed here (beyond §§1–3)

`bun run typecheck` → clean. `bun test` over `compare-controller`, `render-campaign-report`, `verify-artifact`, `evidence`, `cohort-cells.conformance`, `verify-campaign-index.canonical`, `verify-campaign-index`, `output-policy`, `stage-live-campaign` → **413 pass / 0 fail / 2357 expect** (10.0 s). `bun tools/compare/check-official-io.ts` → `status=PASS failure-count=0`. `cargo test -p native --test rig_cohort_runtime` → 52 passed. Local acceptance `bun test tools/compare/fanout-production-e2e.test.ts` → **19 pass / 0 fail / 393 expect, 197.09 s, exit 0**; afterwards no comparison process and no 4433 listener. Not executed: the frozen command, the controller against the staged pair, the preflight.

## 6. Parser self-check

`parseExactStageReviewBindings` from HEAD's `stage-live-campaign.ts` was run over this file after writing and refuses it with `EXACT_STAGE_APPROVAL_REVIEW_NOT_APPROVED` — correct, since the first line is CHANGES REQUIRED and this artifact must not be consumable as an approval. `extractExactStageReviewLabel` over the same text returns the five bindings above, each label present exactly once (recorded in the review transcript).
