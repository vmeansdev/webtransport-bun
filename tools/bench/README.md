# Benchmark scripts

Run against addon or reference server. Record baselines for:
- handshake p95/p99
- datagram throughput and p95 latency
- stream throughput and CPU usage

Example: `bun tools/bench/datagram-throughput.ts` (when implemented).
