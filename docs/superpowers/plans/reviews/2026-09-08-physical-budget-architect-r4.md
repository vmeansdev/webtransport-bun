APPROVED

# Architect review R4 — physical-budget amendment (compact delivery frames, hardware-derived cells)

Reviewer: Claude (Architect pass, revision R4), 2026-09-08. Git read-only (`git status --porcelain | grep -v '^??' | wc -l` → 0 before and after; only this file written). No campaign, stage or controller started; nothing under `.release-evidence/`, `/var/db/webtransport-bun`, `/usr/local/libexec/webtransport-bun`, the rig stage dirs or keys touched. Rig access: one read-only ssh (`cat /proc/loadavg` → 0.01 0.08 0.08; `ls -d /tmp/amendment-review-*` → 0; `pgrep -af 'amendment-review|combined-relay|fanout-physical'` → 1, the ssh shell carrying the pattern itself; `eno1/speed` → 1000). No scratch created on the rig; no new measurement was needed — every closure below is a text-against-HEAD check or arithmetic over numbers the R2/R3 Critic artifacts carry.

## Bindings

- Amendment SHA-256: `b8a4bcfe61b420923875c3a8456eedd3ecfa43b06a39cdd4d89a07fee24f7ef8` (`shasum -a 256 docs/superpowers/plans/2026-09-08-physical-budget-amendment.md`, 160 lines, revision R4; matches the dispatch SHA)
- Base plan SHA-256: `9374b7470223655bff5d118c1a8812f62e62515aa8ac56e38ff61eb4f4b961d9` (re-hashed: match)
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5` (`git rev-parse HEAD`; tracked tree clean)
- R3 pair read first (`reviews/2026-09-08-physical-budget-{architect,critic}-r3.md`), then the R2 and R1 pairs for the reopened items.

## Verdict in one paragraph

Nothing blocking remains. All nine Architect R3 findings and all ten Critic R3 findings close as written, verified by execution against HEAD: the pre-check bar is now ≥ 2× and rate-250 on WT computes to 2.488× (`1e6/(4.08+12)/25000`); the `role-failed/v1` carrier is now specified at the hops that exist (`PHASE_B_ROLE_CHILD_SCHEMAS` + bound in `child-pipe-protocol.ts:1571-1602`, `PHASE_B_ROLE_CHILD_ORIGINATED_SCHEMAS` at `supervisor-protocol.ts:1872-1880`, `MacRoleChildControlChannel.receive` at `remote-supervisor.ts:8676` with the poison at `:8626`, the five driver awaits at `compare-controller.ts:7704/7775/7846/7924/7959`, `closedCohortFailureCode :6368-6370`, the seal at `:6454`, the campaign terminal at `:1431-1460`), with the deadline race closed by first-unit refusal and non-overwrite; the WT delivery-channel paragraph now states what HEAD does (`server.ts:1172-1175`, b7a93bbe is an ancestor) and the context-as-queue-head contract is implementable because both enqueue points see an empty queue (`fanout-relay.ts:996` refuses `warmup-drained` with `queuedItems !== 0`); D4's bound is a whole-window mean with the per-window max recorded, the 1× and warmup predicates are integers on every row, and the preconditions and receipt fields are named; `serverChildCpu` goes into the rig-authored `rig-server-snapshot-receipt/v1` at the two receive sites (`comparison-supervisor.rs:2308`, `:2407`), not into the barrier-bound ack; the vocabulary test asserts the union (executed: 3 + 18 = 21 today); the mirror list names the pinned vectors and no longer names `comparison-supervisor.rs`; chat-250 is interpolated; the bit-rate label is right. What is left is line-level: three fixture/pin citations in D6/D3 point at neighbouring records, and one more pinned hex for the retired ticker id (the `cohort-grant/v1` vector shared by `cohort-protocol.test.ts:3709` and `crates/native/tests/cohort_protocol.rs:970`) is missing from D3's pinned-vector enumeration — the same class R3 finding 7 carried as non-blocking. Neither changes a number, a record, or a mechanism.

## R3 findings closure

Architect R3 (1–9):

| # | R3 finding | Status | Evidence (executed this pass) |
|---|---|---|---|
| 1 | Pre-check "≥ 2.5×" refuses rate-250 (2.4876×) | **CLOSED** | D1: "must additionally be ≥ 2× the binding rate … rate-250 on WT pre-checks at 62,189/s = 2.49×". `python3`: rate-50 12.44×, rate-100 6.22×, **rate-250 2.488×**, chat-250 12.32×, chat-500 6.07×, chat-1k 2.945× on WT; WS 39.4/19.7/7.87/38.2/18.2/8.36× — every row ≥ 2×. The D3 "2.5×" print for rate-250 is now a harmless rounding (D1 prints 2.49× itself). |
| 2 | Carrier at the wrong hops (`cohort-protocol.ts` frame table, "Mac supervisor records", `:1431-1460` as the arm entry, `remote-supervisor.ts:7206`) | **CLOSED** | D2 fail-closed 2 now: registry `child-pipe-protocol.ts:1571-1587` + bound `:1596-1602` (executed: 15-entry `PHASE_B_ROLE_CHILD_SCHEMAS`; `roleChildFrameBoundForSchema` at `:1596`; `encodeRoleChildFrame :1628-1640` refuses "unregistered role-child schema"); originated set `supervisor-protocol.ts:1871-1880` (executed: 7-entry `PHASE_B_ROLE_CHILD_ORIGINATED_SCHEMAS` at `:1872`); parser home `cohort-protocol.ts`; "`receive` accepts `role-failed/v1` at **every** await and returns `{ok:false, code: frame.code, message}`; the poison keeps that code" — `receive` is the one method behind all seven awaits (`grep -n 'receive("role-\|receive("connect-permit' compare-controller.ts` → `:7704, :7775, :7846, :7924, :7959` plus the permit awaits), and `poison` (`remote-supervisor.ts:8626`) stores `${code}: ${message}` and returns `{ok:false, code}`; driver pass-through → `closedCohortFailureCode` (`:6368-6370`, `isCampaignFailureCode(code) ? code : "COHORT_PROTOCOL"`) → `failureCode` at `:6454` (`sed -n 6448,6457p`); campaign terminal `:1431-1460` derives from `CAMPAIGN_FAILURE_CODES` (executed: today an 18-entry `as const satisfies` list with `reason.includes`). `grep -n 'remote-supervisor.ts:7206\|onOpen' amendment.md` → 0. Deadline race: "on the first wrong unit (never by shortfall: … 11,000 ms, `compare-controller.ts:6743/:7427/:7462`, before the worker's 15,000 ms budget, `fanout-role.ts:1361-1362`)" — executed: `warmupDeadlineMs: WARMUP_DURATION_MS + inputs.deadlines.warmupDrainMs` (`:6743`), `warmupDrainMs: 6_000` (`:7427`), `ROLE_WARMUP_DEADLINE_CODE = "WARMUP_DEADLINE_EXCEEDED"` (`:7462`), worker `config.warmupDurationMs + COHORT_DRAIN_DEADLINE_MS` (`:1363-1364`, 5,000 + 10,000); "a driver deadline that fires after a `role-failed/v1` was received does not overwrite it" — HEAD's `receive` loop shifts `this.ready` before it tests the deadline (`:8691-8720`), so a buffered frame is always read first; implementable as stated. |
| 3 | WT "at admission" pinned to `onOpen`, an order the relay cannot produce, HEAD mis-cited | **CLOSED** | D2 "Delivery channel": "At HEAD the WT server requests that stream at accepted subscriber registration (`server.ts:1173-1175`, since b7a93bbe) and keeps a lazy open … (`:1038-1069`); the role is unknown at `handle.onSession` (`:1032`) and the accept is written inside `handleRegister` (`fanout-relay.ts:676`) before the server learns the role … R4 keeps the request at accepted subscriber registration and **removes the lazy fallback**". Executed: `grep -n 'onOpen\|handle.onSession\|openDeliveryStream' server.ts` → `onOpen :694` (WS), `handle.onSession :1030`, `openDeliveryStream :1038, :1065 (lazy), :1174 (register)`; `sed -n 1160,1176p` → `if (routing.kind === "register" && routing.role === "subscriber") openDeliveryStream();`; `git log -S'routing.kind === "register"' -- server.ts` → b7a93bbe; `git merge-base --is-ancestor b7a93bbe HEAD` → ancestor; `sed -n 670,678p fanout-relay.ts` → `this.sendFrame(session, accept)` inside `handleRegister`; `sed -n 2263,2282p fanout-role.ts` → detached `for await (const uni of client.incomingUnidirectionalStreams())`. "Before the accept frame is answered" is gone. Context-as-queue-head: §C below — implementable. |
| 4 | chat-250 not interpolated | **CLOSED** | D3 row: 4.23 / 191k / 38× / 61.6k / 12.3× — recomputed 4.23 µs (4.08 + 0.90 × 150/900), WS 191,205 (38.24×), WT 61,614 (12.32×). |
| 5 | Which record carries `serverChildCpu`; who reads `/proc` | **CLOSED** (with line residue, finding 2) | D6: "the CPU figures go in a **rig-authored** record: the rig supervisor reads `/proc/<child>/stat` … and `/proc/<child>/task/<child>/stat` on receipt of `server-measure-start-ack/v1` (`comparison-supervisor.rs:2310`) and of `server-capture-ack/v1` (`:2407`), carries the first read forward, and mints `serverChildCpu: {processMs, mainThreadMs, windowMs}` into `rig-server-snapshot-receipt/v1` … — not into `rig-measure-start-ack/v1`, whose digest the start barrier binds". Executed: `pipe.receive("server-measure-start-ack/v1")` at `:2308`, `pipe.receive("server-capture-ack/v1")` at `:2407`; pid check `:2157-2161`; TS `RigServerSnapshotReceiptV1` at `server-observation-artifact.ts:160-190`; Rust key set `SERVER_SNAPSHOT_RECEIPT` at `secure_fs.rs:15308-15337` (27 keys, exact-key admission via `:15362`), mint `:17595-17633`, receipt carried in the evidence at `:17685-17686`; the Mac-side `cohort-observation-evidence/v1` (`:23425-23470`) embeds `rigMeasureStartAck` but **not** the snapshot receipt, so the pinned hex vectors under `fixtures/cohort-evidence-vectors/` indeed move only through the slice 3 regeneration, as D6 says. Fixture/pin lines are off (finding 2). |
| 6 | Vocabulary test must assert the union | **CLOSED** | D2: "the new cross-language test parses the Rust literals from `secure_fs.rs` and asserts `SECTION_7_CODES == REFUSAL ∪ FAILURE` (22 = 3 + 19 after the addition)". Executed python over both files: `3 18 21 True` (`SECTION_7_CODES` at `secure_fs.rs:12171`); pins today `cross-supervisor-protocol.test.ts:655-656` (3 / 18) and `rig_cohort_runtime.rs:2746`, `mac_cohort_runtime.rs:2108` (21). |
| 7 | Mirror residue: pinned evidence vectors | **CLOSED** (one more pinned hex, finding 1) | D3 names `mac_cohort_runtime.rs:4846/:4943` (`S3_VECTOR_TICKER_10K` — executed `:4846`, `:4943`), `:5073-5085` and `:5514-5545` (executed: `:5514-5545` is `the_ticker_10k_evidence_vector_is_reproducible_and_pinned`, 134_555 bytes; the `cohort_cell("ticker-fanout/rate-10000")` call is at `:5162`, not `:5073-5085`, which is the `CHAT_1K_CELL` manifest test — line residue), `fixtures/cohort-evidence-vectors/*.ticker-fanout_rate-10000.*` pinned at `cross-supervisor-protocol.test.ts:2417-2437` (executed: `cellId: "ticker-fanout/rate-10000", size: 134_555`), `WTB_EVIDENCE_VECTOR_DIR` (`:5440-5462`). |
| 8 | D1 bit-rate label | **CLOSED** | D1: "the frame-plus-WS-header bit rate is 25.6 Mbit/s (ticker, 128 B units) / 31.2 Mbit/s (chat, 156 B units), payload alone 20.0 / 25.6 Mbit/s" — 25,000 × 128 × 8 = 25.6 M; × 156 = 31.2 M; × 100 = 20.0 M; × 128 = 25.6 M ✓. |
| 9 | `comparison-supervisor.rs` in the mirror list | **CLOSED** | `grep -n 'comparison-supervisor.rs' amendment.md` → lines 141 (D6, the CPU capture site) and 152 (slice 4, same) only; D3's mirror list no longer names it. |

Critic R3 (1–10):

| # | R3 finding | Status | Evidence |
|---|---|---|---|
| 1 | Pre-check (Architect 1) | **CLOSED** | as above. |
| 2 | Carrier hops + deadline race not admitted | **CLOSED** | as Architect 2: first-unit refusal, the 11,000 vs 15,000 ms sentence, the non-overwrite sentence, and "the mixed-version test produces the mismatch on the first unit and asserts the sealed `failureCode` is `DELIVERY_CONTEXT_MISMATCH`". `grep -c WARMUP_DEADLINE_EXCEEDED cross-supervisor-protocol.ts` → 0 still (not a member — which is why the first-unit rule is load-bearing, and the text says so). |
| 3 | WT channel (Architect 3) | **CLOSED** | as above. |
| 4 | "≤ 0.80 over each window" fails the pilot row | **CLOSED** | D4: "≤ 0.50 core as the mean over the whole measured window (10 s / 30 s) and over the whole 5 s warmup epoch in the 1× pass, and ≤ 0.80 core as the mean over the whole measured window in the 2× pass; the per-origin-window maximum is recorded, not gated (per-1 s readings at the ticker 2× point spread 0.72–0.87 around a 0.75 mean)"; D3 "whole-window means: … 0.74–0.76 at 2× (three runs)" — R2 d4v2 0.74, R2 smoke 0.76, R3 0.75 (Critic R3 §A and finding 4). |
| 5 | 1× pass has no rate predicate; warmup has no interval | **CLOSED** | D4: "`A_origin[w] = row ingress ÷ windows` in the 1× pass" and "`D_warmup = 10 × publisherCount × K` counted on the Mac over the epoch's own 5,000 ms interval". Integers on every row: `windowCount: 10 \| 30` (`cohort-protocol.ts:1805`) → 2,500/10 = 250 (ticker rows 50/100/250 per window), 300/30 = 10 (chat); 2× → 500 / 20; `WARMUP_MESSAGES_PER_PUBLISHER = 10`, `WARMUP_INTERVAL_MS = 500`, `WARMUP_DURATION_MS = 5_000` (`cohort-protocol.ts:113-115`) → D_warmup 1,000 (ticker) / 25,000 / 50,000 / 100,000 (chat). Not vacuous: a publisher that under-offers now fails `A_origin[w] = row ingress ÷ windows`; a relay that refuses fails `O = A`; a relay that drops fails `D = A × K` on the Mac. |
| 6 | Rig CPU frequency unpinned/unrecorded | **CLOSED** | D1 "Rig clock" row (`acpi-cpufreq`, 1.4–2.1 GHz + boost 3.7, "frequency-dependent and conservative in the throttled direction"); D4 receipt "per-core MHz of the rig at each interval boundary". |
| 7 | `decoded.value.length !== 1` dropped silently | **CLOSED** | D2 fail-closed 1 names it; `sed -n 2253p fanout-role.ts` → `if (decoded.ok && decoded.value.length === 1) {`. |
| 8 | Mac host state | **CLOSED** | D4 operator precondition ("Mac idle (load average < 4, no foreign process storms), rig idle (load < 0.5), both recorded") and receipt fields ("the Mac's load average and per-worker CPU"). |
| 9 | Architect 4/5/6/8 confirmed | **CLOSED** | as above. |
| 10 | Mirror residue beyond Architect 7 (`:4846/:4943`; `comparison-supervisor.rs`) | **CLOSED** | D3 names `:4846/:4943`; `comparison-supervisor.rs` out of the mirror list. |

R2 items the R3 pair had reopened — architect r2 1 (carrier), 2b (uni stream at warmup-context time), 5 (chat 250), 9 (bit rate); critic r2 1, 2, 5, 8 — close with the rows above. R1 Critic 3 (ii) (the closed code reaching the seal) closes with Architect 2. No R1/R2 residue remains open.

## A. D3 under the R4 rule (recomputed)

`ceiling(K, t) = 1e6 / (relay_us(K) + send_us(t))`, `send_us` 1.0 / 12.0, `relay_us` interpolated, binding = max(measured, warmup), pre-check ≥ 2×:

| Cell | K | binding/s | relay µs | WS ceiling / × | WT ceiling / × | pre-check ≥ 2× | D3 prints |
|---|--:|--:|--:|--:|--:|---|---|
| rate-50 | 100 | 5,000 | 4.08 | 196,850 / 39.4 | 62,189 / 12.44 | OK | 197k/39× · 62k/12.4× ✓ |
| rate-100 | 100 | 10,000 | 4.08 | 196,850 / 19.7 | 62,189 / 6.22 | OK | 197k/19.7× · 62k/6.2× ✓ |
| rate-250 | 100 | 25,000 | 4.08 | 196,850 / 7.87 | 62,189 / **2.488** | OK | 197k/7.9× · 62k/2.5× ✓ (D1: 2.49×) |
| chat 250 | 250 | 5,000 | 4.23 | 191,205 / 38.2 | 61,614 / 12.32 | OK | 191k/38× · 61.6k/12.3× ✓ |
| chat 500 | 500 | 10,000 | 4.48 | 182,482 / 18.2 | 60,680 / 6.07 | OK | 182k/18× · 61k/6.1× ✓ |
| chat 1k | 1,000 | 20,000 | 4.98 | 167,224 / 8.36 | 58,893 / 2.945 | OK | 167k/8.4× · 59k/2.9× ✓ |

Measured utilisation at the binding rate (the rule): rate-250 WT 0.42 (R3) / 0.45 (R2) at 1×, 0.74 / 0.75 / 0.76 at 2×; chat 1k 0.32 at its warmup rate, 0.54 at 2× (Critic R3 §A table, `r3/raw/*-server.json` medians; conservation exact, faults 0). Both bounds hold on every measured row. Cardinalities: ticker 2,500 ingress = 250 × 10 s; 250,000 = 2,500 × 100; chat 300 × K; sessions K + publishers; `publisherRatePerSecond` (`compare-controller.ts:4631-4634`) = 50 / 100 / 250 / 1 / 1 / 1 integers. Readiness closed set after the removals {30,000, 90,000}: enforced at grant parse `cohort-protocol.ts:834` and connect permit `:3869`; removed constants `:103-104` and set members `:105-110`; Rust rows `secure_fs.rs:19636, :19649`; controller switch `:6874-6889` (executed: `case "chat 5k": return 180_000; case "chat 10k": return 300_000` present at HEAD, in D3's list).

## B. Mirror completeness (executed)

`git grep -nE 'ticker-fanout/rate-(10000|50000|100000)([^0-9]|$)|chat-fanout/subscribers-(5000|10000)([^0-9]|$)' -- tools crates ':!*.test.ts' ':!*fixtures*' ':!crates/native/tests'` → `secure_fs.rs:19574,19587,19600,19626,19639`; `compare-controller.ts:277, :4096`; `frozen-run-section-9.{6,7}.fragment.sh:4`; `render-phase4-report.ts:23`; `cross-supervisor-protocol.ts:195-215`; `evidence.ts:154-159` — all in D3. Label unions: `cohort-protocol.ts:5596-5602` (`CohortCellId`), `COHORT_CELL_CARDINALITIES :5615`, `COHORT_CELL_GRANT_PARAMETERS :5713-5721`; `PHASE4_GATE_CELLS :275-278, :3943`; `scenario-registry.ts:228` (`chatCell(1_000 | 5_000 | 10_000)`), `:256-270` (`tickerCell` → `ticker-fanout/rate-${rate}`), `:514-519`; `types.ts:30` (`10_000 | 50_000 | 100_000`); `client.ts:1442, :1470`. Test files naming a retired id or label: 22 by my pattern (D3 "24–26 by pattern") — informational. Rust test pins: `mac_cohort_runtime.rs:4846, :4943, :5162, :5514-5545` — D3 has all but the `:5162` line (cites `:5073-5085` for it). **Not in D3:** the pinned grant hex (finding 1).

## C. The delivery context as the head of the subscriber queue (against `fanout-relay.ts` as written)

- `QueuedDelivery` (`:364-370`) is `{bytes, originWindowIndex, linuxAcceptedOrdinal, enqueuedAtMs, warmup}`; the queue is a plain array (`queue: QueuedDelivery[]`, `:381`), so a head insert is `unshift`. The per-subscriber cap is tested per item at `:1292-1308` (`RELAY_SUBSCRIBER_QUEUE_MAX_ITEMS = 64`, `:108`) and the write deadline per head item in `pump` at `:1355-1367` (`RELAY_WRITE_DEADLINE_MS = 5_000`, `:119`); a would-block leaves the head in place (`:1370`), so "retried by the same pump" is HEAD behaviour.
- **Ordering.** The warmup context is enqueued at `bindWarmupEpoch`, which requires phase `registration` (`:762`) — no delivery has been queued for anyone yet; the measured context is enqueued inside `openMeasuredWindow`, which requires phase `warmup-drained` (`:1080`), and that phase is refused while `this.queuedItems !== 0` (`:996-1000`). So at both enqueue points every subscriber queue is empty and "at the head" can never place a measured context ahead of a pending warmup frame. A late or re-registered subscriber has a fresh session and an empty queue. The contract is implementable exactly as written.
- **Counting.** `pump` charges `writesCompleted`/`writeBytes`/`postStopRelayWrites` for every non-warmup head it writes (`:1377-1383`); a `kind: "context"` item must be excluded there and from `queueDrops`' fault path — D2 states "not counted as a delivery or as relay bytes"; the close-time "closed with N queued deliveries" refusal (`:571-578`) would count a pending context as one, which is the intended fail-closed outcome for a context that never went out. Nothing else reads the queue.
- The sink split (`RelaySessionSink` `{trySend, close}`, `:165-168`, gaining `trySendDelivery`) and the removal of the lazy path at `server.ts:1063-1066` are as R3 §D found: bounded.

## D. `role-failed/v1`, the driver and the deadline (executed)

`receive` (`remote-supervisor.ts:8676-8745`) is a single method: it drains `this.ready` first and only then tests `until - Date.now()`, so a `role-failed/v1` that arrived before the driver reached that child is decoded before the deadline can fire; with the schema accepted at decode time (instead of `STATE_INVALID` at `child-pipe-protocol.ts:1673-1679`), `poison` (`:8626`) records the child's code and every `if (!x.ok) return x` in `MacRoleChildCohortDriver` (`:7704-7778` seen) returns it unchanged to `driveCohortArm` → `:6454`. `DELIVERY_CONTEXT_MISMATCH` as a member makes `closedCohortFailureCode` an identity for it. The sequential per-child await order means a failing child later in the list is read after the earlier children complete their warmup — inside the 11,000 ms deadline only if the earlier children complete, which they do in the mixed-version scenario (every child refuses the first unit) and in any single-child mismatch (the healthy children finish at ~5 s). The text's first-unit rule is what makes this hold; it is stated.

## E. D6 and the capture path (executed)

Both read instants are the receive sites (`comparison-supervisor.rs:2308` for `server-measure-start-ack/v1`, `:2407` for `server-capture-ack/v1`); the pid is the forked one (`:2157-2161`). `rig-server-snapshot-receipt/v1` is minted after capture (`secure_fs.rs:17595-17633`, bounded by `RIG_COHORT_RECEIPT_MAX_BYTES`) and signed (`:17633`), carried in the server observation evidence (`:17685-17686`) and admitted by the Mac verifier (`:21108-21125`); its exact key set is `SERVER_SNAPSHOT_RECEIPT` (`:15308-15337`) on the Rust side and `RigServerSnapshotReceiptV1` (`server-observation-artifact.ts:160-190`) on the TS side, with the evidence key lists at `server-observation-artifact.test.ts:107, :126`. The record choice is right and the ack's digest (barrier-bound) is untouched. The `cohort-observation-evidence/v1` vector members (`secure_fs.rs:23425-23470`) do not include the snapshot receipt, so D6's "the pinned hex vectors … move only through the slice 3 regeneration" holds.

## F. D4 placement, 9.6/9.7 literals, lease (executed)

- `bun tools/compare/check-official-io.ts` at HEAD → `failure-count=0`; `official-io-allowlist.json` has `cliEntryTs` (10 entries); the fresh-root check tests `<candidate>/<campaignId>`, `macRoot`, `rigRoot` only (`stage-live-campaign.ts:2799-2808`); `abandon` at `:76`, `:3476`.
- 9.6 at HEAD: `CAMPAIGN_ID=fanout-pilot-r1`, `pilot`, `CELLS=ticker-fanout/rate-10000`, `REPS=1`, `CAMPAIGN_TIMEOUT_MS=1800000`, `EXPECTED_PASS=2 … EXPECTED_SEALED=2`; `stage-live-campaign.ts:1089` pilot `2_100_000` = +300,000 ✓ (D5). 9.7: `CAMPAIGN_TIMEOUT_MS=43200000`; `:1091` canonical `45_000_000` = +1,800,000; D5's `14,400,000 / 16,200,000` keeps the pairing ✓; `EXPECTED_PASS=60 EXPECTED_FLATS=12 EXPECTED_PAIRED_PROMOTIONS=6 EXPECTED_SEALED=60` = 6 cells × 2 arms × 5 ✓. Worst case 36 × 76 + 36 × 156 = 8,352 s = 2.32 h ✓ (`COHORT_ACQUISITION_DEADLINES :7425-7431`).
- Lease: `frozen-run-wrapper.fragment.sh:480-481` requires `RUN_TIMEOUT_MS + 5400000` remaining = 21,600,000 ✓ (D5).

## Findings

No blocking findings.

### 1. Non-blocking — one more pinned hex for the retired ticker id: the `cohort-grant/v1` vector

`tools/compare/cohort-protocol.test.ts:3709` (`RUST_PINNED_TICKER10K_GRANT_HEX`, "the ticker 10k shape: 1 publisher, 8 shards, 100 subscribers") and `crates/native/tests/cohort_protocol.rs:970` (same literal, fed to `CohortGrantV1::parse_signed` via `pinned_grant()`, `:986-1121`). Grant parse resolves the cell with `cohort_cell(cell_id)?` (`secure_fs.rs:19695`), so once `ticker-fanout/rate-10000` is refused the pinned grant fails to parse on both sides. It is not among D3's pinned vectors (which name the manifest vector `:4846/:4943` and the evidence vector) and, like them, cannot be asserted by the conformance test; it is regenerated by the pair owner for a live ticker id (rate-250 has the same 1 × 8 × 100 shape, so the residue-class-window property the pin exists for survives). Add it to D3's pinned list and slice 3. Command: `grep -n 'RUST_PINNED_TICKER10K_GRANT_HEX' tools/compare/cohort-protocol.test.ts crates/native/tests/cohort_protocol.rs; sed -n 19690,19695p crates/native/src/secure_fs.rs`.

### 2. Non-blocking — three fixture/pin line citations point at the neighbouring record

- D6 "fixture `cohort-fixture-signing.ts:363-415`" — those lines mint `rig-measure-start-ack/v1` (`:364`, signed at `:387`) and the `server-loop-utilization/v1` frame (`:393`); the snapshot-receipt fixture is at `:440-472` (used at `:554`). Command: `grep -n 'rig-server-snapshot-receipt/v1\|rig-measure-start-ack/v1' tools/compare/cohort-fixture-signing.ts`.
- D6 "pin `server-observation-artifact.test.ts:568`" — `:563-568` asserts `RUST_PINNED_MEASURE_START_ACK_HEX`, the pin of the record D6 explicitly leaves untouched; it does not move under D6. The snapshot receipt has no hex pin; its key set is asserted through the evidence key lists at `:107` / `:126` and the Rust `SERVER_SNAPSHOT_RECEIPT` set (`secure_fs.rs:15308-15337`). Command: `sed -n 560,568p tools/compare/server-observation-artifact.test.ts; grep -rn RUST_PINNED tools/compare/*.test.ts`.
- D3 "`mac_cohort_runtime.rs:5073-5085`" — that range is the `CHAT_1K_CELL` manifest test; the ticker `cohort_cell("ticker-fanout/rate-10000")` call is at `:5162`. Command: `grep -n 'rate-10000' crates/native/tests/mac_cohort_runtime.rs`.

Trivial offsets, for the record: `handle.onSession` is `server.ts:1030` (D2 says `:1032`); the Rust vocabulary list is `secure_fs.rs:12171-12193` with the enum-to-string mapping near `:12300-12318` (D2 says `:12188-12316`); the worker warmup budget is `fanout-role.ts:1363-1364` (D2 says `:1361-1362`, inherited from the Critic).

### 3. Informational — "a child that exits without the frame still maps to `UNEXPECTED_EOF`/`CHILD_LIFECYCLE`"

`UNEXPECTED_EOF` is a channel code, not a `CampaignFailureCode`, so it seals as `COHORT_PROTOCOL` through `closedCohortFailureCode`; `CHILD_LIFECYCLE` is a member and seals as itself. The sentence describes today's behaviour, which the amendment keeps; no change needed.

## Verified without finding

- Every D1 number cited in D3/D4 traces to a raw file or a Critic table (R2 §A medians, R3 §A rows `r3row1x-b` 0.42, `r3d4v2-b` 0.75 (0.72–0.87), `r3warm1x` 0.32, `r3warm2x` 0.54); the three REPORT.md files exist under `.scratch/2026-09-08-physical-budget/`.
- The compact frame and both context key sets unchanged from R3 §C: every field the worker's book and the §4.5 equations read is bound; the scratch codec's five-field preimage (`tree/bench/compact-codec.ts:52-57`) is correctly disclaimed.
- WT unit discriminator: `encodeFanoutWtFrame` writes a `u32be` prefix (`fanout-wire.ts:1073-1083`), so a legal JSON unit begins `0x00`; `LengthPrefixedFrameReader` (`adapters/wt.ts:2194`) throws on an over-cap prefix (`:2225-2231`) — the mixed-mode branch must precede `push`, as D2 states.
- `rejectEpochMixing` keys on `kind.startsWith("warmup-")` (`fanout-wire.ts:371-372`) as cited.
- `verify-artifact.ts:4124-4149` is the Linux half only; `grep -c 'malformedCount\|perSubscriberDelivered' verify-artifact.ts` → 0 at HEAD, so D2 fail-closed 3 is a real addition.
- `estimateDeliveryBytes` (`fanout-relay.ts:1711-1719`) sizes from a canonical-JSON probe today — D2 fail-closed 4 is needed.
- Rig idle and clean; tracked tree untouched.

## Two-perspective note

- Perfectionist: two rounds were refused for citing code that did not exist; R4 still cites three fixture/pin lines that name the neighbouring record, and one pinned hex for the retired id is missing from a list whose whole point is completeness. Each is a grep away and the Critic should confirm them before the approval record is written.
- Pragmatist: every mechanism now points at the code that implements it, every number reproduces, the rule is measured on every row, and the residue is line numbers in a test-fixture citation plus one more vector regenerated by the same owner that regenerates the other two. Nothing here changes a byte of the rule, the rows, the frame, or the records. Approved.
