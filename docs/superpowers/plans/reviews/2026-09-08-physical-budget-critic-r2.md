CHANGES REQUIRED

# Critic review R2 — physical-budget amendment (compact delivery frames, hardware-derived cells)

Reviewer: Claude (Critic pass, revision R2), 2026-09-08. Git read-only (`git status --porcelain | grep -v '^??' | wc -l` → 0 before and after; only this file written). No campaign, stage or controller started; nothing under `.release-evidence/`, `/var/db/webtransport-bun`, `/usr/local/libexec/webtransport-bun`, the rig stage dirs or keys touched. Rig access: read-only ssh plus scratch benchmarks under `/tmp/amendment-review-r2-critic/` on port 4572 with the R1 throwaway P-256 cert, deleted afterwards (`ls -d /tmp/amendment-review-*` → none, `pgrep -af amendment-review` → none, `RIG_SCRATCH_DELETED`). Raw outputs, the harness and the Mac client are in this session's scratchpad: `…/scratchpad/r2/{combined-relay-wt.ts, wt-count-client.ts, run-combined.sh}` and `…/scratchpad/r2/raw/combined-*-{server,client0..3}.json`. Every number below names its file or command.

## Bindings

- Amendment SHA-256: `2c982d544a2f3ebab999aff0d6da5cfc54b123398b2aca36cc2acb98de935066` (`shasum -a 256 docs/superpowers/plans/2026-09-08-physical-budget-amendment.md`, 138 lines, revision R2; matches the dispatch SHA)
- Base plan SHA-256: `9374b7470223655bff5d118c1a8812f62e62515aa8ac56e38ff61eb4f4b961d9`
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5`
- Architect R2 artifact SHA-256: `3021ac1da48435c8fca86e510ec2292ee70c51f65f53b8e4346f0200bbcf9415` (re-verified, not trusted; §B)
- R1 artifacts read: `reviews/2026-09-08-physical-budget-architect.md`, `reviews/2026-09-08-physical-budget-critic.md`
- Rig build tree used for the measurement: `/tmp/ws-wt-linux-build.03pyUO` (the budget agents' Linux build). `sha256sum` of its `tools/compare/scenarios/fanout-relay.ts`, `fanout-wire.ts`, `adapters/wt.ts`, `server.ts`, `cohort-protocol.ts`, `packages/webtransport/src/index.ts` equal `shasum -a 256` of the same paths at HEAD (`65ca27c8…`, `f9255536…`, `1e61745a…`, `36a8fcd0…`, `991b6554…`, `94e1772b…`).
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`

## Verdict in one paragraph

Both Architect R2 blocking findings reproduce by execution: the role child has no frame that carries a failure code (it prints to stderr and exits 1, the supervisor turns the pipe EOF into `UNEXPECTED_EOF`, the controller only echoes the stderr line — `compare-controller.ts:6654-6656` — and the sealed `failureCode` comes from a hardcoded 18-entry substring list); and the WT delivery channel D2 names does not exist when the warmup context is sent, so the context takes the control-backlog path whose retry loop re-parses every pending session's context on every pump. I re-sized that path with constants measured this pass (`JSON.parse` of a 727 B context is 2.6–3.0 µs on the rig, an empty `pump()` at K=2,500 is 360 µs, one `relay.counters()` call is 664 µs): ≈11 s of relay thread at the chat-2.5k warmup open, not ≈29 s, against a 3.2 s per-subscriber queue and a 5 s write deadline — the conclusion stands. Then the brief's own question (a): I ran the unmodified production `FanoutRelay`, the verbatim production settler and the real native WT writer on the rig against 100 and 2,500 real WT sessions on the Mac. The additive `relay_us(K) + send_us(t)` model predicts the **ceilings** well (K=100: model 90k, measured 90–95k sustained; K=2,500: model 67.8k, measured 73–75k) but under-states main-thread **utilisation** below the ceiling by 30–50 %, because two main-thread costs are outside both the model and `onRelayWork`: the settler's `relay.counters()` calls (`server.ts:629-656`, 1 + 2·⌈K/256⌉ per ingress, each an O(K log K) re-sort — 664 µs at K=2,500) and the WT writer's asynchronous completion work (≈4–6 µs per write at the operating points). The consequence that blocks is a row-sizing one the amendment does not see: **chat warmup offers 2× the measured rate** (10 publishers × 2/s × K), so chat 2.5k's warmup is 50,000 deliveries/s — the amendment's own D4 rate — and on WT that runs the relay thread at **0.98 core for 5 s** (measured: 49,770/s, zero faults, per-subscriber queue ≤ 3, so it completes, with no headroom; at 1× the thread is at 0.74). Against the measured ceiling the warmup margin is 1.5×, not the 2.5× the budget rule demands, and D4's receipt as written would record "busy 0.42" for that saturated thread. Two more text defects follow from the same data: D4's pass predicate does not bind offered = accepted at the 2× rate, and D5's claim boundary calls `busyMs` "a CPU-accounting comparison at equal work" although it covers 43–66 % of the WT arm's main-thread work and none of its 3–4 native cores. All fixes are text plus one row decision; the measurement, frame and mirror work stand.

## R1 findings closure

Architect R1 (1–12):

| # | R1 finding | Status | Evidence (this pass) |
|---|---|---|---|
| 1 | WT stream for the context unstated; egress throws on non-JSON | **NOT CLOSED** | D2 now declares the context a delivery-channel frame routed by first byte, but on WT the bytes at `sink.trySend` are `u32be ‖ JSON` (`fanout-wire.ts:1073-1083`; `server.ts:865-890` reads the prefix), so `{` never occurs first; the uni stream is opened lazily by the first delivery-routed `trySend` (`server.ts:1038-1069`, returns `would-block` at `:1066-1069`), so with "would-block semantics unchanged" the context is backlogged (`fanout-relay.ts:1791-1810`) and retried with a parse per session per pump (`:1341`, `:1827-1838`). Architect R2 finding 2 confirmed; re-sized in §D. |
| 2 | Kept and retired `subscribers-1000/-5000` | CLOSED | `grep -n 'Retired ids' amendment.md` → five retired ids, `subscribers-1000` kept; none of the D3 rows is retired. |
| 3 | Mirror list omitted the id constructors/unions | CLOSED | D3 names `scenario-registry.ts:228/:256-270/:514-519`, `types.ts:30`, `client.ts:1442,1470`, `:6874-6889`, `PHASE4_GATE_CELLS`, the unions. My grep (§E) → one residue `compare-controller.ts:4096` (help text). |
| 4 | B6 timeout omitted arms, no literal | CLOSED (arithmetic slip) | Literals `14,400,000` / `16,200,000` pinned, `stage-live-campaign.ts:1091` named (`:1090` canonical `45_000_000` at HEAD). Worst case recomputed 9,432 s (finding 8). |
| 5 | D4 2× unreachable at the top rows | CLOSED for the measured window, **reopened for warmup** | Rows bounded by WT-as-written; rate-500 and chat 5k out. But chat warmup is 2× the measured rate (finding 3). |
| 6 | Margins for the ticker frame only | CLOSED | Per-row WS/WT columns reproduce (§B). |
| 7 | Payload bit-rate line | CLOSED (label) | 25.6/31.2 Mbit/s are 25,000 × (124+4) × 8 and 25,000 × (152+4) × 8 — frame + WS header; payload is 20.0/25.6. |
| 8 | Exact keys of both contexts, `warmupNonce`, verification point, LE tag | CLOSED | Both key sets, `warmupNonce`, "first 4 LE bytes", "verified at consumption, in inbox order" are in D2. |
| 9 | Vocabulary each new code joins; `EXPECTED_TOTALS_MISMATCH` placement | **PARTIALLY CLOSED** | Vocabulary named; index refusal placed in `verify-campaign-index.ts` (local `{ok:false, code, message}` style at `:207-232` confirmed). "pinned by the existing cross-language vocabulary test": no such test — TS pins 18 (`cross-supervisor-protocol.test.ts:655`), Rust pins 21 (`rig_cohort_runtime.rs:2746`, `mac_cohort_runtime.rs:2108`), `grep -rln secure_fs.rs tools/compare` → comments and the audit only. Carrier: finding 2. |
| 10 | 113.6 B wording | CLOSED | "≥113.6 B before margin (940.7e6/8/1e6 − 4)"; 940.7e6/8/1e6 − 4 = 113.59. |
| 11 | Orphaned readiness member; paths | CLOSED | `READINESS_DEADLINE_MS_CHAT_10K` removed from the set "and its Rust mirror" (the Rust mirror is the `COHORT_CELLS` row `secure_fs.rs:19649`; `grep -n READINESS secure_fs.rs` → none); D6 paths `tools/compare/bin/…`. |
| 12 | Historical evidence with retired ids | CLOSED | D3 last sentence + `2026-09-08-retired-cell-ids-in-history.md` in D6. |

Critic R1 (1–10):

| # | R1 finding | Status | Evidence (this pass) |
|---|---|---|---|
| 1 | Budget rule not per-topology; chat 5k does not fit | CLOSED | D1 per-K relay row; `ceiling(K,t)` interpolated; chat 5k retired. Recomputed from `raw/relay/k*.jsonl` (`deliveriesPerS`, `warmup:false`, medians of 5): 245,114 / 200,696 / 128,999 / 71,361, zero-encoder 78,657 → 4.08 / 4.98 / 7.75 / 14.01 µs. Identical. |
| 2 | WT write ≈7 µs on the relay thread; D4 bar unreachable | CLOSED | D1 send-cost row; recomputed 140,680 msg/s at main-thread 0.963 (median of the 11 samples, `raw/wt-n128-m100-critic1-threads.jsonl`) → 6.85 µs; WSS 889,444 at 0.867 → 0.97 µs. Option (b) chosen. Combined measurement in §A refines what the 7 µs covers. |
| 3 | D2 fault semantics have no sink | **PARTIALLY CLOSED** | (i) `onMalformed` + `recordMalformed` — stated; (iii) verifier worker-counter and `perSubscriberDelivered` refusals with mutation tests — stated (`grep -n 'malformedCount\|perSubscriberDelivered' verify-artifact.ts` → none at HEAD, confirmed Linux-only at `:4124-4149`); (iv) book keyed by session roleId — stated. (ii) child exit with a closed code the controller sees — no carrier (finding 2). |
| 4 | D4 can pass vacuously | **PARTIALLY CLOSED** | Offer 2× for the full window, production settler path, real WT connector, `D = A × K` counted by workers, zero queue-full/write-timeout, receipt fields — all stated. Residues (§E): warmup not in scope and it is the binding load for chat rows; the predicate does not bind `O = A` or `A ≥ 2×` per window; the receipt's "busy fraction" is not the thread's utilisation (finding 3, 5). |
| 5 | Architect 1–4 confirmed | as above | |
| 6 | Dropping the per-frame digest weakens nothing | CLOSED | D2 "Why no per-frame payload digest" restates §6; `git grep -nE 'deliveryContextSha256|wireSha256|frameDigest' -- tools crates` (non-test) → none: nothing seals a delivery frame's digest. |
| 7 | Say what B6 can claim | CLOSED as text, **reopened on honesty** | D5 "Claim boundary" exists and D6 prints busy figures beside totals. What `busyMs` covers on the WT arm is measured in §A and is not "a CPU-accounting comparison" (finding 4). |
| 8 | Chat 5k on WT: readiness/memory pass, loop fails | CLOSED for 5k; chat 2.5k re-done in §C | Readiness 2,510 sessions: 3.2–3.4 s measured for 2,500 real WT sessions this pass vs 180,000 ms; memory 261 MB vs 6,769 MB available (`free -m` today); the loop budget at chat 2.5k: passes at the measured rate, fails the 2.5× rule at the warmup rate (finding 3). |
| 9 | Queue tolerance at rate-500 was 128 ms | CLOSED | D3: 256 ms at rate-250, 6.4 s at chat; recomputed; D4 records max depth. Measured this pass: per-subscriber depth ≤ 1 at K=100/500 ingress/s, ≤ 3 at K=2,500/20 ingress/s (`queueItemsPeak` 100 and 7,380). |
| 10 | Small items | CLOSED | D2 fail-closed 4 (`estimateDeliveryBytes`), tag-based warmup test, 9.6 wall unchanged, arch 8/11 closed, arch 9 partial. |

## A. Question (a): the two costs measured together on the rig

**Harness** (`scratchpad/r2/combined-relay-wt.ts`, run by `run-combined.sh`): the unmodified `FanoutRelay` imported from the HEAD-identical build tree, transport `wt`; `createRelaySettler` copied verbatim from `server.ts:629-656` (it re-arms one `pump()` per `setImmediate` while items remain and reads `relay.counters().queuedItems` three times per round); each subscriber's sink is the repo's `nodeWritableFrameWriter` (`adapters/wt.ts:2279`) on a server-opened uni stream of the real native WT server (`packages/webtransport/src/index.ts` `createServer`, `cohortWtListenerAdmission` shape), opened at admission, with the writer's drain callback pumping exactly as `server.ts:1044-1046`; the sink routes by first byte (`0xC1` → uni stream, else in-process control) — the amendment's D2 discipline with no `JSON.parse` on the delivery path; the relay's codec is overridden for `data`/`warmup-data` relay→subscriber frames with the amendment's compact template + clone (`compact-codec.ts` from the R1 scratch, 24 B header + payload); publishers are registered in-process with counting sinks; ingress is offered at a paced rate from pre-encoded publisher frames so the loop is charged only with what the rig relay child pays (`handleInboundBytes` decode + admission, bookkeeping, compact encode, stream write); production warmup (10 per publisher) runs first over the real streams. Per second the harness records accepted/s, writes/s, the `timed` busy fraction (the same spans `onRelayWork` charges at `server.ts:993-1000`), the main thread's own CPU from `/proc/self/task/<pid>/stat` (fields 14+15) and the process's from `/proc/self/stat`, queued items, would-blocks and faults. On the Mac, 4 processes × K/4 sessions (`wt-count-client.ts`, the workspace package's native `connect()`) count fixed-size frames. Conservation on the Mac was exact in every run (warmup + measured writes = frames received, remainder 0). The external `thread-sampler.sh` attached to the ssh wrapper shell by mistake and is not evidence; the in-process `/proc` reads are.

| Cell (K subscribers, P publishers, payload) | Offered ingress/s | Deliveries/s achieved | Timed busy (`onRelayWork` spans) | **Main thread, core** | Process cores | Faults / would-block | Raw file |
|---|--:|--:|--:|--:|--:|---|---|
| 100/1/100 B, 10 windows — **1× rate-250** | 250 | 24,992 | 0.30 | **0.45** | 1.75 | 0 / 0 | `combined-k100-p1-n100-r250-row1x-server.json` |
| same — **2× (D4 shape)** | 500 | 49,987 | 0.49 | **0.74** | 3.11 | 0 / 0 | `…-r500-d4v2-server.json` (repeat of `…-r500-smoke`: 0.47 / 0.77) |
| same — 4× | 1,000 | 95,336 / 94,493 / 84,864 / 90,139 for the first 5 s | 0.49–0.54 | 0.98–0.99 | 3.1–3.3 | collapse at t≈6 s: queue-full on 7 subscribers → admission closed, 4,397 faults | `…-r1000-ceil1-server.json` |
| same — 6× | 1,500 | 120,771 in the first second, then collapse | 0.71 | 0.99 | 2.6 | 12,288 faults | `…-r1500-ceil2-server.json` |
| 2,500/10/128 B, 30 windows — **1× chat 2.5k** | 10 | 24,943 | 0.32 | **0.74** | 2.36 | 0 / 0 | `combined-k2500-p10-n128-r10-row1x-server.json` |
| same — **2× = the chat warmup rate (D4 shape)** | 20 | 49,770 | 0.42 | **0.98** | 3.56 | 0 / 0; queue peak 7,380 items (≤ 3 per subscriber) | `…-r20-d4v2-server.json` (repeat of `…-r20-d4`: 0.40 / 0.98) |
| same — 3× | 30 | 73,244 | 0.45 | 0.98 | 3.93 | 0 / 0; one ≈0.7 s loop stall (`maxBurst` 22); queue peak 13,480 | `…-r30-ceil1-server.json` |
| same — 4× | 40 | 74,709 mean; 87–118k transients | 0.43–0.66 | 0.99 | 3.2 | 2,244 write-timeouts, 4,259 faults, 37 ingress refusals | `…-r40-ceil2-server.json` |
| 2,500 idle sessions (1 ingress/s) | 1 | 2,481 | 0.04 | 0.12 | 0.29 | 0 | `…-r1-idle-server.json` |
| 100 idle sessions (1 ingress/s) | 1 | 100 | 0.003 | 0.03 | 0.06 | 0 | `combined-k100-…-r1-idle-server.json` |

Per-cell cost constants measured in-process after each run (200 calls): `relay.counters()` **664 µs** at K=2,500 (40–50 µs at K=100); an empty `relay.pump()` **360–466 µs** at K=2,500 (28 µs at K=100).

**Reading.**

1. *Ceilings*: the additive model holds or is conservative. K=100: model 1/(4.08+7.0) = 90,253/s; measured 90–95k sustained (the 1,000/s run sat at the ceiling with the thread at 0.98–0.99 for 5 s and then collapsed as the backlog crossed 64 items per subscriber; the 120k of the 1,500/s run is a one-second transient). K=2,500: model 1/(7.75+7.0) = 67,797/s; measured 73–75k (30/s sustained 73k with a stall; 40/s collapsed). So D3's margin column, defined as ceiling ÷ declared, is right or slightly pessimistic: rate-250 WT ≈ 3.6–3.8×, chat 2.5k WT ≈ 2.9–3.0×.
2. *Utilisation*: the model under-states the main thread below the ceiling. At the D4 rate the thread reads 0.74 (K=100; model 50,000 × 11.08 µs = 0.55) and 0.98 (K=2,500; model 50,000 × 14.75 µs = 0.74); at the row rates 0.45 (model 0.28) and 0.74 (model 0.37). Two components explain the gap and neither is in D1 nor in `onRelayWork`: (i) the settler's `relay.counters()` — `settle()` calls it once and `round()` twice (`server.ts:643-651`), 1 + 2·⌈K/256⌉ = 21 calls per ingress at K=2,500, each rebuilding `registeredPublisherIds()`/`registeredSubscriberIds()` with a `localeCompare` sort of every session (`fanout-relay.ts:1721-1754`): 14 ms per ingress, 0.14 core at 10/s, 0.28 at 20/s, all outside `timed`; (ii) the WT writer's asynchronous main-thread completion work after `writable.write()` returns: main − timed − counters ≈ 0.18 core / 50k = 3.6 µs per write at K=100 and ≈ 0.28 / 50k = 5.6 µs at K=2,500 (larger per write at low rates — 83 ms per 2,500-write expansion in the idle run — because completions batch less). The R1 link harness's 7 µs was the whole main-thread cost of a saturated writer with trivial JS around it; in the relay it splits into a synchronous part inside the spans and an asynchronous part outside them.
3. *Is 2.5× enough slack?* For the measured window, yes: at 1× the top rows leave 0.55 (ticker) and 0.26 (chat 2.5k) of a core idle, and the ceilings are ≥2.9× the row. For the chat warmup epoch, no — finding 3.
4. *The rig side is not the only cost*: the 4 dumb Mac counting clients burned 123–134 cpu-s per 45 s run at K=2,500 (≈3 M1 cores for 50–75k frames/s, ≈40–60 µs per frame across threads; the production worker will decode as well). Not a limiter for 8 workers on a 10-core M1 Max, but it belongs in D1's Mac row.

Commands: `run-combined.sh 100 1 100 10 500 10000 d4v2`, `run-combined.sh 2500 10 128 30 20 30000 d4v2` (and the other rows above); summary via the python block in `run-combined.sh`; medians over steady-state seconds with `python3` over `series[]` (`writesPerS > 1000 && faults == 0`).

## B. Architect R2 findings re-verified by execution

| # | Claim | Verified by | Result |
|---|---|---|---|
| 1 | No carrier for `DELIVERY_CONTEXT_MISMATCH`; no cross-language test | `grep -n 'schema: "role-' tools/compare/bin/fanout-role.ts` → `role-ready/v1 :1281`, `role-warmup-complete/v1 :1381`, `role-measure-start-ack/v1 :1485`, `role-partial/v1 :1655`, `role-exited/v1 :1677`; `sed -n 2338,2346p` → `console.error(\`[fanout-role] ${outcome.code}: …\`); process.exit(1)`; `sed -n 8736,8742p tools/compare/remote-supervisor.ts` → `poison("UNEXPECTED_EOF", …)`; `sed -n 9030,9033p` → stderr forwarded to `config.onChildStderr`; `sed -n 6654,6656p tools/compare/bin/compare-controller.ts` → `process.stderr.write(\`[${childId}] ${text}\`)` and nothing else; `sed -n 1431,1460p` → 18-entry substring list, default `TRUST_PROTOCOL`; pins 18 / 21 / 21 as cited | **Confirmed.** The controller echoes the child's line to its own stderr; no hop parses a code from it. |
| 2a | WT routing byte wrong; Mac uni reader unstated | `sed -n 1073,1083p fanout-wire.ts` (u32be prefix); `sed -n 865,890p server.ts` (prefix read first); `grep -n 'class LengthPrefixedFrameReader' -A 45 adapters/wt.ts` (throws `RangeError` on a length > cap, so a `0xC1…` first byte would declare ≥3.2 GB and throw inside the `data` handler); `sed -n 2247,2256p fanout-role.ts` (uni stream read through that reader) | **Confirmed.** Also: `nodeWritableFrameWriter` never inspects the prefix (`adapters/wt.ts:2302-2321`), so writing a compact frame as-is is fine on the server side. |
| 2b | Uni stream does not exist at warmup-context time; K²/2 parses | `sed -n 1038,1069p server.ts`; `sed -n 1791,1810p, 1827,1838p fanout-relay.ts`; `sed -n 1339,1342p` (`drainControl` for every active session per pump); runtime bind order `sed -n 3036,3043p` (`bindWarmupEpoch` then `closeRegistration`); `grep -n 'WARMUP_MESSAGES_PER_PUBLISHER\|WARMUP_INTERVAL_MS' cohort-protocol.ts` → 10 / 500 | **Confirmed; magnitude re-sized** (§D): ≈11 s at chat 2.5k, ≈1.8 s at chat 1k. |
| 3–9 | Non-blocking items | D5 bounds `sed -n 6740,6748p, 7425,7431p compare-controller.ts`; lease gate `sed -n 480,482p frozen-run-wrapper.fragment.sh`; interpolation 4.48 µs at K=500; `sed -n 371,383p fanout-wire.ts` (`kind.startsWith("warmup-")`); mirror grep §E; `sed -n 1852,1858p, 1435,1462p fanout-role.ts` | **All confirmed.** |

## C. Question (d): chat 2.5k on WT, with arithmetic

- Readiness: 2,510 sessions ÷ 500/s = 5.02 s nominal, 5.23 s at the 480/s floor; measured this pass 3.2–3.4 s for 2,500 real WT sessions from 4 Mac processes (`connectMs` in the `k2500` server JSONs; `handshakesBurst = sessions` lets the burst exceed the refill). Deadline 180,000 ms → ≥34× time headroom. **Pass.**
- Memory: 2,510 × 104 KB = 261 MB rig RSS; queues ≤ 2,500 × 64 × 152 B = 24 MB; `free -m` today MemAvailable 6,769 MB. Mac: 8 workers × 314 sessions × ~300 KB ≈ 0.75 GB; ports 2,510 / 16,384 = 15 %. **Pass.**
- Loop budget at the measured rate (25,000/s): measured main thread 0.74 core, ceiling 73–75k → 2.9–3.0× ≥ 2.5×. **Pass.**
- Loop budget at the warmup rate: `WARMUP_MESSAGES_PER_PUBLISHER 10` at `WARMUP_INTERVAL_MS 500` (`cohort-protocol.ts:113-114`) → each of 10 publishers offers 2/s → 20 ingress/s × 2,500 = **50,000 deliveries/s for 5 s**, at 500 ms boundaries in bursts of 10 ingress (25,000 queued items at once, 10 per subscriber). Measured at exactly that shape (20/s, 30 s): 49,770/s, thread 0.98, zero faults, per-subscriber queue ≤ 3. Ceiling 73–75k → **1.5×, below the 2.5× rule**; the model's own ceiling gives 1.36×. **Fail** under the amendment's rule; passes physically with no headroom, before the finding-1 context storm that lands in the same 5 s.
- The "2.7×" printed in D3 is the model ceiling over the measured rate; it is neither the utilisation at 1× (0.74) nor the warmup margin (1.5×).

## D. Finding 2(b) re-sized with measured constants

Per the code path (control backlog → `drainControl` → `trySend` → `relayFrameRoutingFields` → `JSON.parse` → `delivery === null` → `would-block`), at the warmup bind with K contexts emitted at once and K uni-stream open callbacks each running `relay.pump()` + `settler.settle()`:

- parses: K²/2 = 3,123,750 at K=2,500 × 2.6–3.0 µs (measured: `bun -e` on the rig, 727 B context, DataView + `TextDecoder` + `JSON.parse`, 3 × 200,000 iterations → 2.99 / 2.63 / 2.62 µs) ≈ **8.4 s**;
- K empty pumps × 360 µs ≈ **0.9 s**; K `settle()` → `counters()` × 664 µs ≈ **1.7 s**;
- total ≈ **11 s** of relay thread (chat 1k: 500,000 parses ≈ 1.35 s + ≈0.4 s ≈ 1.8 s; chat 500 ≈ 0.5 s), against 3.2 s of per-subscriber warmup queue (64 ÷ 20/s), the 5,000 ms write deadline (`fanout-relay.ts:24`) and the worker's 15 s warmup budget (`fanout-role.ts:1361-1362`). The Architect's ≈25 s used ~8 µs per parse; the conclusion is unchanged.

Corollary at HEAD, independent of the amendment: the lazy open already costs the first warmup step K open callbacks × (empty pump + `counters()`) ≈ 2,500 × 1.02 ms ≈ 2.6 s plus ≤256 head parses per callback ≈ 1.7 s at chat 2.5k on WT — a new row the base plan never ran. Opening the uni stream at admission (the Architect's fix) removes both; D4 with the warmup in scope is what would have caught it.

## E. Mirror enumeration and literals (execution)

- `git grep -nE 'ticker-fanout/rate-(10000|50000|100000)([^0-9]|$)|chat-fanout/subscribers-(5000|10000)([^0-9]|$)' -- tools crates ':!*.test.ts' ':!*fixtures*' ':!crates/native/tests'` → `secure_fs.rs:19574,19587,19600,19626,19639`; `compare-controller.ts:277`, **`:4096`**; `frozen-run-section-9.{6,7}.fragment.sh:4`; `render-phase4-report.ts:23`; `cross-supervisor-protocol.ts:195,199,203,211,215`; `evidence.ts:154-159`. All in D3 except `:4096`. Test files naming an old id: 26. `ls tools/compare/*conformance*` → only `phase-a-admission-conformance.test.ts`; `cohort-cells.conformance.test.ts` is new. `stage-live-campaign.ts abandon` exists (`:76`, `:3476`).
- D5: per-execution bounds `readinessDeadlineMs + (5,000 + 6,000) + (measuredDurationMs + 10,000 + 5,000) + 10,000` (`compare-controller.ts:6742-6748`, `:7425-7431`) → 76 / 156 / 246 s; B6 worst case 3×12×76 + 2×12×156 + 12×246 = **9,432 s = 2.62 h** (D5 prints 136 / 226 / 8,712); with `captureMs 15,000 + roleReceiveMs 5,000` per execution 10,872 s = 3.02 h. `14,400,000` holds. Wrapper admission needs `RUN_TIMEOUT_MS + 5,400,000` = 21,600,000 ms of lease at launch (`frozen-run-wrapper.fragment.sh:480-482`).
- D3 rows: every margin reproduces from D1's rule (rate-250 WT 90,253/25,000 = 3.61×; chat 2.5k WT 67,797/25,000 = 2.71×; chat 500 at the interpolated 4.48 µs would print 182,482 / 87,108).

## F. Questions (b), (c), (g): the fail-closed path, the tag, and §4.2

**(b) Frames that can still be dropped before `onMalformed`, or reach a counter with a wrong tag** (read hop by hop: connectors `fanout-role.ts:2196-2197`, `:2247-2256`; inbox `:1200-1210`, `createFrameQueue :1818-1849` — unbounded, never drops; warmup loop `:1361-1370`; `consumeWorkerFrame :1435-1462`; `WorkerWindowBook :925-1016`; child exit `:2340-2345`):

1. The warmup loop's callback (`:1365`) counts `kind === "warmup-data"` and **ignores everything else**; under D2 it must refuse (not ignore) a frame whose tag is not the warmup context's, or a JSON data frame — the text says the tag "replaces" the kind test but does not say the loop fails closed on the other branch (finding 7).
2. A mixed-mode uni reader that frames a compact unit by the header's own `payloadBytes` cannot refuse a wrong `payloadBytes` — it waits for bytes that never come, the stream desynchronises silently and the worker sees fewer frames: `D < L`, a non-promotable PASS rather than a refusal. The reader must frame by the cell constant `24 + messageBytes` and refuse at once when `payloadBytes ≠ messageBytes` (finding 6).
3. A length-prefixed unit whose prefix exceeds the cap makes `LengthPrefixedFrameReader.push` throw inside the `data` listener (`adapters/wt.ts:2228-2231`): the child dies uncaught → `UNEXPECTED_EOF`/`CHILD_LIFECYCLE`, FAIL but uncounted. Acceptable as fail-closed; say so in D2 so the mixed-version test does not expect `malformedCount`.
4. `consumeWorkerFrame` discards `recordDelivery`'s failure result (`:1449-1456`), so at HEAD a wrong-subscriber frame increments `malformed` and continues; with D2 fail-closed 3 the verifier refuses on the counter — closed once slice 4 lands. Nothing else drops between the connector and the book.

**(c) Stale-session / cross-cohort collision with `contextTag` + `subscriberIndex` + a roleId-keyed book.** One server child per execution (`compare-controller.ts:2028, 4838`) writes on per-execution sockets; a closed subscriber cannot re-register (`REGISTRATION_CLOSED`, `fanout-relay.ts:691`); the context digest preimage carries `subscriberId` and `subscriberIndex`, so tags are per session and per epoch; a frame of session X written on Y's socket fails `subscriberIndex` and (2⁻³²) the tag; a context of X delivered on Y fails the worker's recomputation; a frame of a previous execution cannot reach a socket that did not exist. **No counted scenario found.** Two unspecified branches to pin: a warmup-tagged compact frame arriving after the measured context (today `kind !== "data"` is ignored at `:1446`; under D2 it has "a context for its tag" and would be counted as warmup after warmup completion — refuse it), and equal warmup/measured tags (refuse; Architect 6).

**(g) §4.2 promises the verifier or seals depend on.** `git grep` shows no seal, partial, equation or verifier reads a relay→subscriber frame's bytes, prefix or digest: `relayWriteBytes` is `writesCompleted × config.messageBytes` (`fanout-relay.ts:1376-1379`), `deliveredBytes` is `D × this.messageBytes` (`fanout-role.ts:1000-1003`), `verify-artifact.ts` compares counts only (`:4124-4149`), Rust answers `fanout-wire/v1` as `TRUST_CHILD_FRAME_INVALID` for any cohort record (`comparison-supervisor.rs:4171-4175`) and never parses a data frame. The compact frame is under the 1 KiB data cap; the context (727 B measured) is under the 4 KiB control cap the uni reader is built with. The `end` marker's cross-stream exposure (control bidi vs uni) is HEAD behaviour, not introduced here. **Nothing the verifier or seals depend on breaks**; the §4.2 sentence "WT prefixes each logical frame" is overridden explicitly.

## G. Question (e): can D4 as written pass while B5/B6 would fail?

Yes, three ways, in decreasing weight:

1. The warmup epoch is out of scope, and for every chat row the warmup rate is 2× the measured rate (20K/s vs 10K/s) — the binding load; at chat 2.5k on WT it saturates the thread (§C) and is where finding-1's context storm lands. Architect finding 3, promoted.
2. The receipt records "the relay's `onRelayWork` busy fraction"; at chat 2.5k/20 ingress/s that reads 0.42 while the thread is at 0.98. A lead reading the receipt sees 58 % headroom. The receipt must record the child's main-thread CPU (`/proc/<pid>/task/<pid>/stat`, utime+stime over the window) and the predicate must bound it (finding 3).
3. The pass predicate binds `D = A × K`, zero queue-full, zero write-timeout — not `O = A`, not `A ≥ 2 × row ingress` per window. A relay that refuses ingress (`sendClosedAck`, `fanout-relay.ts:1178-1215`) or a publisher that slips its pace passes with `A` below 2× (finding 5). In my 4× runs the relay's own admission-close made `A < O` (5,616 of 10,000 accepted) — a D4 with the D2 predicate alone would have recorded conservation and no queue-full after the first fault.

## H. Question (f): the claim boundary and the render clause

- D5 says a promoted arm's `series.samples` equals the declared rate by construction and that B6 claims completion plus "the relay's attested loop-busy time was X (WS) vs Y (WT) — a CPU-accounting comparison at equal work". Measured (§A): on the WT arm the attested spans are 0.49 of a 0.74 main thread at the ticker D4 rate (66 %), 0.30 of 0.45 at rate-250 (67 %), 0.32 of 0.74 at chat 2.5k (43 %), 0.42 of 0.98 at its warmup rate (43 %); the WT process runs 1.75–3.9 cores in total (quinn workers). On WS `session.send` is synchronous and has no completion work, so the omitted share differs by transport. That is a comparison of timed relay spans, biased in WT's favour, not a CPU-accounting comparison. No sealed field carries process or thread CPU (`git grep -nE 'cpuUsage|rusage' -- tools/compare` non-test → none), so the render cannot print the correction from evidence. Finding 4: state the metric's scope in D5 and D6, or add the CPU fields (a §4.4 shape change the amendment currently forswears).
- Stop-gate visibility: D6's `EXPECTED_TOTALS_MISMATCH` in `verify-campaign-index.ts` plus the per-arm topology/totals print make the original B5 failure mode (PASS entries two orders below the gate) visible and refused. A FAIL entry with `failureCode` `CHILD_LIFECYCLE` instead of `DELIVERY_CONTEXT_MISMATCH` remains the one invisible miss (finding 2).

## Blocking findings

### 1. BLOCKING — the WT delivery channel as specified does not exist when the warmup context is sent; wrong routing byte; uni reader unstated (Architect R2 finding 2, confirmed and re-sized)

`server.ts:1038-1069` opens the uni stream lazily on the first delivery-routed `trySend` and answers `would-block`; `fanout-relay.ts:1791-1810` backlogs the context; `:1341` + `:1827-1838` retry it through `relayFrameRoutingFields` (`server.ts:865-890`, a `JSON.parse`) for every active session on every pump. With K contexts at the warmup bind and K open callbacks each pumping (`server.ts:1048-1052`), that is K²/2 parses + K empty pumps + K `counters()` calls ≈ **11 s** of relay thread at chat 2.5k (§D) against 3.2 s of per-subscriber queue and a 5 s write deadline; chat 1k ≈ 1.8 s. On WT the bytes at the routing site begin with the u32 prefix, never `{` (`fanout-wire.ts:1073-1083`), and the Mac reads the uni stream through `LengthPrefixedFrameReader` (`fanout-role.ts:2247-2256`), which an unprefixed `0xC1` unit makes throw. Required text (as the Architect states, plus one line): open the uni stream at WT session admission before `accept` is written; the relay tells the sink the channel when it encodes (`trySend(bytes, "control" | "delivery")` or a pre-routed byte) so no outbound frame is parsed for routing; the Mac uni reader is mixed-mode, framing compact units by the cell constant `24 + messageBytes` and length-prefixed JSON otherwise, the control reader unchanged; D4 runs the production warmup on both transports at both topologies. Commands: `sed -n 1038,1069p tools/compare/server.ts; sed -n 1791,1840p tools/compare/scenarios/fanout-relay.ts; sed -n 1339,1342p tools/compare/scenarios/fanout-relay.ts; ssh … bun -e '<JSON.parse probe>'` (2.6–3.0 µs).

### 2. BLOCKING — `DELIVERY_CONTEXT_MISMATCH` has no carrier to the controller and "the existing cross-language vocabulary test" does not exist (Architect R2 finding 1, confirmed)

`fanout-role.ts:2340-2345` prints `[fanout-role] CODE: message` and exits 1; the child's only frames are the five at `:1281/:1381/:1485/:1655/:1677`; `remote-supervisor.ts:8739-8742` poisons `UNEXPECTED_EOF`; `:9030-9033` forwards stderr to `onChildStderr`, which the controller implements as `process.stderr.write` only (`compare-controller.ts:6654-6656`); the terminal `failureCode` is chosen by substring over a hardcoded 18-entry list defaulting to `TRUST_PROTOCOL` (`:1431-1460`); pins are 18 / 21 / 21 and nothing compares them. The arm fails, but the sealed record never names the refusal, and a mixed-version test written to "→ child FAIL" proves nothing about the code. Required text: (i) a `role-failed/v1` control frame `{schema, sequence, executionSha256, childId, code: CampaignFailureCode, message}` sent before exit and mapped by the supervisor to its terminal (its `isCampaignFailureCode` pass-through at `remote-supervisor.ts:7206` already admits any member); (ii) slice 1 owns `compare-controller.ts:1431-1455` and the three pinned lengths (18→19, 21→22 ×2); (iii) the mixed-version test asserts `failureCode === "DELIVERY_CONTEXT_MISMATCH"` on the sealed terminal; (iv) name the cross-language vocabulary test as new. Commands in §B row 1.

### 3. BLOCKING — the physical budget omits two main-thread costs, and the chat warmup epoch (2× the measured rate) is the binding load: chat 2.5k on WT runs the relay thread at 0.98 core for 5 s and clears the 2.5× rule by 1.5×

Measured this pass with the production relay, the verbatim production settler and the real WT writer (§A): at chat 2.5k the main thread is **0.74 core at the row rate** (model 0.37) and **0.98 at the warmup rate of 50,000 deliveries/s** (model 0.74); the measured ceiling is 73–75k, so the warmup margin is 1.5× (model: 1.36×) against the rule's 2.5×. The two unmodelled costs are the settler's `relay.counters()` calls (`server.ts:643-651`; 1 + 2·⌈K/256⌉ per ingress; 664 µs each at K=2,500 because `counters()` rebuilds `registeredPublisherIds()` and `registeredSubscriberIds()` with a full `localeCompare` sort, `fanout-relay.ts:1721-1754`) and the WT writer's asynchronous completion work on the same thread (≈4–6 µs per write at the operating points, more when writes are sparse). Both are outside `onRelayWork`, so the D4 receipt "busy fraction" would show 0.42 for a saturated thread. Required: (i) D1 gains both components as measured (or the settler stops calling `counters()` — a one-line `queuedItems` getter — and the relay µs are re-measured before the rows are frozen; that is an engine change the exact-stage reviews must see); (ii) the budget rule is applied to every epoch's rate: `ceiling(K, t) ≥ 2.5 × max(measured rate, warmup rate)`, which at K=2,500 on WT is 2.5 × 50,000 = 125,000 > 75,000 — the row is out unless re-measured after (i), and the top WT-bounded chat row becomes `chat-fanout/subscribers-1000` (warmup 20,000/s vs model 83,472 → 4.2×; measured ceiling at K=1,000 not taken this pass) or a K the re-measured budget admits; (iii) D4's receipt records the relay child's main-thread core (`/proc/<pid>/task/<pid>/stat` utime+stime over the window) beside the busy fraction, and the predicate bounds it at the 2× offer (≤ 0.8 is the number my runs separate: 0.74 passes at K=100, 0.98 fails at K=2,500). Commands: `run-combined.sh 2500 10 128 30 20 30000 d4v2` (raw `combined-k2500-p10-n128-r20-d4v2-server.json`, `series[].mainThreadCore` 0.97–0.99, `busyFrac` 0.41–0.43, `countersUs` 664); `sed -n 629,656p tools/compare/server.ts; sed -n 1721,1754p tools/compare/scenarios/fanout-relay.ts; grep -n 'WARMUP_MESSAGES_PER_PUBLISHER\|WARMUP_INTERVAL_MS' tools/compare/cohort-protocol.ts`.

### 4. BLOCKING — D5's claim boundary calls `busyMs` "a CPU-accounting comparison at equal work"; on the WT arm the attested spans are 43–66 % of the main thread and exclude 1.7–3.9 native cores

§H. The sentence is the amendment's own addition and the report will print it (D6). Required text: "`busyMs` is the relay's timed spans on the server child's JS thread (`onRelayWork`); it excludes the settler's bookkeeping between spans, the transport's asynchronous completion work on the same thread, and the native transport threads; measured at D1 it covers N % of the WT thread and M % of the WS thread at the top rows" — with N/M from D4's receipt — or the seals gain the child's thread/process CPU so the render prints both (a §4.4 shape change). Command: `python3` over `combined-k2500-p10-n128-r10-row1x-server.json` (`busyFrac` 0.32 vs `mainThreadCore` 0.74, `processCores` 2.36).

## Non-blocking findings

### 5. D4's pass predicate must bind offered = accepted and accepted ≥ 2× per window

§G item 3. Add: `O_origin[w] = A_origin[w]` and `A_origin[w] ≥ 2 × (row ingress ÷ windows)` for every window, on both transports; "achieved rate" in the receipt is the accepted ingress, not the conserved expansion. `sed -n 1178,1215p tools/compare/scenarios/fanout-relay.ts` shows the closed-ack refusals that make `A < O`.

### 6. The mixed-mode uni reader must frame compact units by the cell constant

§F(b) items 2–3. State that the reader frames a `0xC1` unit as exactly `24 + messageBytes` bytes and refuses immediately when the header's `payloadBytes ≠ messageBytes` (so a wrong length is an `onMalformed`, not a silent stall), and that a prefix over the cap still throws and ends the child (FAIL via lifecycle, no counter) — the mixed-version test should expect that, not `malformedCount`.

### 7. The warmup loop and the post-measured warmup tag

§F(b) item 1 and §F(c). The warmup consumption at `fanout-role.ts:1361-1370` must refuse, not ignore, any frame that is not a warmup-tagged compact frame; a warmup-tagged frame after the measured context is refused; equal warmup/measured tags refuse the session.

### 8. Architect R2 items 3–9, all confirmed

D4 must name the warmup epoch (now finding 3); D5 arithmetic 156 / 246 s, 9,432 s = 2.62 h, and the lease must leave ≥ 21,600,000 ms; chat-500 row prints the K=1,000 bound where D1 says interpolate (4.48 µs → 182k / 87k); the delivery-context parser keys the forbidden set on `epoch` (`rejectEpochMixing` keys on `kind.startsWith("warmup-")`, `fanout-wire.ts:371-372`); mirror residue `compare-controller.ts:4096`, the conformance test is new, `READINESS_DEADLINE_MS_CHAT_5K` serves chat 2.5k; `drainOnce` must carry the inbox roleId (`:1852-1858`, book keyed by `frame.subscriberId` at `:1455` today); D1's 25.6/31.2 Mbit/s are frame-plus-header rates.

### 9. The scratch codec's tag preimage is not D2's

`compact-codec.ts:52-57` hashes `{grant, barrier, messageBytes, subscriberId, windowCount}`; D2 hashes the whole context frame minus `deliveryContextSha256` (with `epoch`, `subscriberIndex`, `publisherIds`, the warmup nonce). D2 governs; slice 1's exact-byte vectors must not be lifted from the prototype.

### 10. D1's Mac row

The dumb counting client cost ≈3 M1 cores at 50–75k frames/s across 4 processes (§A item 4); add the Mac-side per-frame cost at the top rows to D1 so the 8-worker budget is stated, not assumed.

## Verified without finding

- D1 relay-per-K, send-cost, cable, frame sizes: reproduce from the raw files (closure table, Critic 1–2); rig link still 1000 Mbit/s (`cat /sys/class/net/eno1/speed`), bun 1.3.14 at `/home/hermes-admin/.bun/bin/bun`, MemAvailable 6,769 MB, load 0.03–0.07 before my runs.
- D3 rows against D1's rule on both transports: reproduce (§E); measured WT ceilings meet or exceed the model at K=100 and K=2,500 (§A).
- Compact frame vs consumers and §4.5 equations: complete and bound (Architect R2 §C re-read: `windowIndex` u8 < 30, `publisherIndex` ↔ `publisher-N`, `subscriberIndex` ↔ `subscriber-NNNNNN`, `payloadBytes` = cell constant, tag per session and epoch).
- `nodeWritableFrameWriter` writes any bytes as-is (`adapters/wt.ts:2302-2321`); no seal, equation or Rust record reads a delivery frame (§F(g)).
- Mirror enumeration complete to `compare-controller.ts:4096`; 26 test files; retired ids none in the rows (§E).
- D4 under `check-official-io`: `cliEntryTs` entry is specifiable (Architect R2 §G, not re-executed here); receipt path outside the fresh-root triple.
- 9.6 literals (`CAMPAIGN_TIMEOUT_MS=1800000`, pilot `timeoutMs 2_100_000` = +300,000) and B5 totals at rate-250 (2,500 / 250,000 per arm); 9.7 `EXPECTED_*` = 6 × 2 × 5.
- Rig scratch deleted; no process of mine left on either host (`pgrep` on both → none); tracked tree untouched.

## Two-perspective note

- Perfectionist: the amendment's promise is rows derived from a measured budget with 2.5× slack, and the one epoch that is 2× hotter than the measured window on every chat row was never priced; the metric the report will compare misses half of one arm's thread. Both are exactly the class of defect the user's ruling targets, and both would be frozen into every grant's `approvedPlanSha256`.
- Pragmatist: the ceilings, the frame, the mirror list and the fail-closed design are right and now measured end to end on the real hardware; the fixes are one row decision (or a one-line settler change plus a re-measure), two receipt fields, one predicate line, and honest wording — one more revision.
