CHANGES REQUIRED

# Critic review R4 — physical-budget amendment (compact delivery frames, hardware-derived cells)

Reviewer: Claude (Critic pass, revision R4), 2026-09-08. Git read-only (`git status --porcelain | grep -v '^??' | wc -l` → 0 before and after; only this file written). No campaign, stage or controller started; nothing under `.release-evidence/`, `/var/db/webtransport-bun`, `/usr/local/libexec/webtransport-bun`, the rig stage dirs or keys touched. Rig access: one read-only ssh (`cat /proc/loadavg` → 0.11 0.08 0.08; `ls -d /tmp/amendment-review-*` → none; `pgrep -af 'amendment-review|combined-relay|fanout-physical'` → nothing but the pgrep shell; `eno1/speed` → 1000; governor `performance`, `boost` 1). No scratch created on the rig, so nothing to delete. No new measurement was taken: every closure claim was re-checked against HEAD by execution, and the utilisation figures were recomputed from the R2/R3 raw files, which live in this session's scratchpad (`scratchpad/r2/raw/`, `scratchpad/r3/raw/`, the R3 Critic's own runs).

## Bindings

- Amendment SHA-256: `b8a4bcfe61b420923875c3a8456eedd3ecfa43b06a39cdd4d89a07fee24f7ef8` (`shasum -a 256 docs/superpowers/plans/2026-09-08-physical-budget-amendment.md`, 160 lines, revision R4; matches the dispatch SHA)
- Base plan SHA-256: `9374b7470223655bff5d118c1a8812f62e62515aa8ac56e38ff61eb4f4b961d9` (re-hashed: match)
- Candidate HEAD: `efaed8bb0af82a499ca58b2627ea251efced41c5` (`git rev-parse HEAD`; tracked tree clean)
- Architect R4 artifact SHA-256: `cfa50091b173b3ed244f2ff3310c5e09335356bcdd5f17f7f3c8c4dc5976d22e` (read, then every hop re-executed; not trusted)
- Cohort completion amendment `90d469cb…` and integration design `62ea8c4a…` re-hashed: match.
- R3 pair read first, then the R2 and R1 pairs for the reopened items.

## Verdict in one paragraph

Every R3 finding closes as the Architect says: I re-executed each hop (`PHASE_B_ROLE_CHILD_SCHEMAS` at `child-pipe-protocol.ts:1571-1587` with `roleChildFrameBoundForSchema :1596-1602`, `encodeRoleChildFrame :1625-1637` refusing an unregistered schema, `decodeRoleChildFrame` `STATE_INVALID` at `:1673-1679`, `PHASE_B_ROLE_CHILD_ORIGINATED_SCHEMAS` at `supervisor-protocol.ts:1872-1880`, `MacRoleChildControlChannel.receive :8676-8745` draining `this.ready` before it tests the deadline and `poison :8626` with `??=`, the five driver awaits, `closedCohortFailureCode :6368-6370`, the seal at `:6454`, the substring terminal at `:1431-1460`, `server.ts:1172-1175` opening the uni stream at accepted subscriber registration with the lazy fallback at `:1058-1066`, `bindWarmupEpoch` refusing outside `registration` at `fanout-relay.ts:762` and `warmup-drained` refused while `queuedItems !== 0` at `:996`, the rig receive sites `comparison-supervisor.rs:2308/:2407`, the Rust snapshot-receipt key set `secure_fs.rs:15308-15337` and mint `:17595-17633`, the evidence vector members `:23425-23470` without the receipt, the vocabulary union `3 + 18 = 21`, `check-official-io` `failure-count=0`), and the physics still holds on every row from the raw files. What blocks is one number in the gate the whole amendment rests on: D1's rule says the D4 preflight offers **2× the binding rate** for the full window, and D1 defines the binding rate of every chat row as its **warmup** rate (20,000/s for chat 1k); D4's 2× pass then offers "chat 20/s for 30 s" — 20 ingress/s × 1,000 = 20,000 deliveries/s, which is **1×** the binding rate, and its count predicate `A_origin[w] ≥ 2 × (row ingress ÷ windows)` = 20 per window binds the same 1× load. So for the three chat rows the pass that reads the ≤ 0.80 bound never offers the load the rule names, the "≥ 2× headroom" of every chat row is demonstrated only by the R3 Critic's one-off 40/s run (0.54) and never by the per-stage gate, and the stop condition "a D4 preflight below 2× stops staging" cannot fire for chat. The fix is one number and one clause (chat 40/s for 30 s; the 2× predicate stated against the binding ingress rate), and the R3 raw file shows the fix retires no row (chat 1k at 40,000/s: mean 0.551, max 0.68, exact conservation). Everything else is non-blocking: D3 labels per-second medians as "whole-window means" (the means are 0.43/0.45, 0.75/0.76/0.78, 0.33/0.55 — inside the bounds, one of them 0.024 from the 2× bar), one warmup-loop sentence read literally refuses the warmup context it needs, the Architect's two line-citation residues are confirmed, and there are three informational offsets.

## R3 findings closure

Architect R3 (1–9):

| # | R3 finding | Status | Evidence (executed this pass) |
|---|---|---|---|
| 1 | Pre-check "≥ 2.5×" refuses rate-250 | **CLOSED** | D1 now "≥ 2× the binding rate … rate-250 on WT pre-checks at 62,189/s = 2.49×". Recomputed with D1's interpolation and `send_us` 1.0 / 12.0: WT 12.44 / 6.22 / **2.488** / 12.32 / 6.07 / 2.945×, WS 39.4 / 19.7 / 7.87 / 38.2 / 18.2 / 8.36× — every row ≥ 2×; every D3 ceiling/margin print reproduces. |
| 2 | Carrier at hops that do not exist | **CLOSED** | D2 fail-closed 2 names the registry + bound (`:1571-1587`, `:1596-1602`), the originated set (`supervisor-protocol.ts:1871-1880`), `receive` at every await, `poison` keeping the code, `closedCohortFailureCode` (`:6368-6370`) → `:6454`, and the campaign terminal `:1431-1460` derived from `CAMPAIGN_FAILURE_CODES`. Executed: all as cited (the registry has 15 entries; the terminal is an 18-entry `as const satisfies` list with `reason.includes`). `grep -n 'remote-supervisor.ts:7206\|onOpen' amendment.md` → 0. Deadline race: `warmupDeadlineMs: WARMUP_DURATION_MS + inputs.deadlines.warmupDrainMs` (`:6743`), `warmupDrainMs: 6_000` (`:7427`), `ROLE_WARMUP_DEADLINE_CODE` (`:7462`); worker budget `Date.now() + config.warmupDurationMs + COHORT_DRAIN_DEADLINE_MS` at `fanout-role.ts:1361-1362` (D2's citation is right; the Architect's ":1363-1364" is the offset that is off). Non-overwrite: `receive` shifts `this.ready` before `until - Date.now()` (`:8691-8720`). §B below on the remaining loss paths. |
| 3 | WT "at admission" pinned to `onOpen` / wrong order / HEAD mis-cited | **CLOSED** | D2 "Delivery channel" describes HEAD (`server.ts:1173-1175`, b7a93bbe — `git log -S'routing.kind === "register"'` → b7a93bbe; `git merge-base --is-ancestor b7a93bbe HEAD` → ancestor), the role-less `handle.onSession` (`:1030`; D2 says `:1032`), the accept inside `handleRegister` (`fanout-relay.ts:676`), keeps the register-time open and removes the lazy fallback (`:1058-1066`: `if (delivery === null) { openDeliveryStream(); return "would-block"; }`); "before the accept frame is answered" gone; context-as-queue-head specified. §B on its soundness. |
| 4 | chat-250 not interpolated | **CLOSED** | 4.08 + 0.90 × 150/900 = 4.23 µs; WS 191,205 / 38.2×; WT 61,614 / 12.3× — the row prints 4.23 / 191k / 38× / 61.6k / 12.3×. |
| 5 | `serverChildCpu` record placement | **CLOSED** (citation residue, non-blocking 5) | D6 names the rig-authored `rig-server-snapshot-receipt/v1`, the two read instants (`comparison-supervisor.rs:2310`/`:2407` — `pipe.receive("server-measure-start-ack/v1")` at `:2308`, `pipe.receive("server-capture-ack/v1")` at `:2407`), the pid check (`:2157-2161` confirmed), and "not into `rig-measure-start-ack/v1`, whose digest the start barrier binds". Rust key set `SERVER_SNAPSHOT_RECEIPT` (27 keys, `secure_fs.rs:15308-15337`, exact-key admission via `:15362`), mint `:17595-17633`, carried at `:17685-17686`; the Mac evidence vector (`:23425-23470`) does not embed it. |
| 6 | Vocabulary test asserts the union | **CLOSED** | D2: "asserts `SECTION_7_CODES == REFUSAL ∪ FAILURE` (22 = 3 + 19 after the addition)". Executed python over both files: `3 18 21 True`, both differences empty, intersection empty. |
| 7 | Pinned evidence vectors are a mirror | **CLOSED** (one more pin, Architect R4 1, confirmed) | D3 names `mac_cohort_runtime.rs:4846/:4943` (`S3_VECTOR_TICKER_10K`, `assert_manifest_vector(…, "ticker-fanout/rate-10000", 1, 100)` — executed), `:5514-5545` (`the_ticker_10k_evidence_vector_is_reproducible_and_pinned`, size 134_555 — executed), the fixture files (`ls fixtures/cohort-evidence-vectors/` → `*.ticker-fanout_rate-10000.{hex,json}` ×3) pinned at `cross-supervisor-protocol.test.ts:2417-2437` (executed: `cellId: "ticker-fanout/rate-10000", size: 134_555`). `:5073-5085` is the CHAT_1K manifest test (the ticker `cohort_cell` call is `:5162`) — citation residue. |
| 8 | Bit-rate label | **CLOSED** | 25,000 × 128 × 8 = 25.6 M; × 156 = 31.2 M (frame + 4 B WS header); × 100 = 20.0 M; × 128 = 25.6 M (payload) — D1 prints exactly this. |
| 9 | `comparison-supervisor.rs` in the mirror list | **CLOSED** | `grep -n 'comparison-supervisor.rs' amendment.md` → lines 141 (D6) and 152 (slice 4) only. |

Critic R3 (1–10):

| # | R3 finding | Status | Evidence |
|---|---|---|---|
| 1 | Pre-check refuses the pilot cell | **CLOSED** | as Architect 1. |
| 2 | Carrier hops; deadline race not admitted | **CLOSED** | D2: "on the first wrong unit (never by shortfall: the driver's warmup deadline fires at 11,000 ms … before the worker's 15,000 ms budget … and would seal `COHORT_PROTOCOL`)"; "a driver deadline that fires after a `role-failed/v1` was received does not overwrite it"; "the mixed-version test produces the mismatch on the first unit and asserts the sealed `failureCode`". `grep -c WARMUP_DEADLINE_EXCEEDED cross-supervisor-protocol.ts` → 0 still, so the first-unit rule is load-bearing and stated. §B traces the orchestration order and finds the first-unit rule sufficient. |
| 3 | WT channel | **CLOSED** | as Architect 3. |
| 4 | "≤ 0.80 over each window" fails the pilot row | **CLOSED** | D4: "≤ 0.50 core as the mean over the whole measured window (10 s / 30 s) and over the whole 5 s warmup epoch in the 1× pass, and ≤ 0.80 core as the mean over the whole measured window in the 2× pass; the per-origin-window maximum is recorded, not gated". Recomputed from the raw series (§A): every 2× ticker run's mean is ≤ 0.78. |
| 5 | 1× pass has no rate predicate; warmup has no interval | **CLOSED** for the 1× pass and the warmup epoch; the **2× pass's predicate is the new blocking finding** | D4: "`A_origin[w] = row ingress ÷ windows` in the 1× pass" (250 / 10 integers on every row: 2,500/10, 300/30) and "`D_warmup = 10 × publisherCount × K` counted on the Mac over the epoch's own 5,000 ms interval" (1,000 / 25,000 / 50,000 / 100,000). The 2× predicate `A_origin[w] ≥ 2 × (row ingress ÷ windows)` binds 2× the *measured* rate, which for chat rows is 1× the *binding* rate (finding 1). |
| 6 | Rig CPU frequency unpinned/unrecorded | **CLOSED** | D1 "Rig clock" row; D4 receipt "per-core MHz of the rig at each interval boundary"; rig today: `acpi-cpufreq`, governor `performance`, `boost` 1 (executed). |
| 7 | `decoded.value.length !== 1` dropped silently | **CLOSED** | D2 fail-closed 1 names `fanout-role.ts:2253`; `sed -n 2253p` → `if (decoded.ok && decoded.value.length === 1) {`. |
| 8 | Mac host state | **CLOSED** | D4 precondition (Mac load < 4, rig load < 0.5, both recorded) and receipt fields (Mac load average, per-worker CPU). |
| 9 | Architect 4/5/6/8 confirmed | **CLOSED** | as above. |
| 10 | Mirror residue (`:4846/:4943`; `comparison-supervisor.rs`) | **CLOSED** | as Architect 7 and 9. |

R2 items the R3 pair reopened (architect r2 1, 2b, 5, 9; critic r2 1, 2, 5, 8) close with the rows above. R1 Critic 3 (ii) closes with Architect 2. No R1/R2 residue remains open.

## A. Utilisation figures recomputed from the raw files

Every run D1/D3 cite, `series[].mainThreadCore` per 1 s sample (9 samples over the 10 s ticker window, 29 over the 30 s chat window; no sample excluded by the `writesPerS > 1000 && faults == 0` filter in any of these runs):

| Run (raw file) | Load | Median | **Mean** | Max |
|---|---|--:|--:|--:|
| R3 `r3/raw/combined-k100-p1-n100-r250-r3row1x-b-server.json` | ticker 1×, 25,000/s | 0.420 | **0.429** | 0.47 |
| R2 `r2/raw/combined-k100-p1-n100-r250-row1x-server.json` | ticker 1× | 0.449 | **0.449** | 0.48 |
| R3 `r3/raw/combined-k100-p1-n100-r500-r3d4v2-b-server.json` | ticker 2×, 50,000/s | 0.751 | **0.762** | 0.87 |
| R2 `r2/raw/combined-k100-p1-n100-r500-d4v2-server.json` | ticker 2× | 0.740 | **0.750** | 0.83 |
| R2 `r2/raw/combined-k100-p1-n100-r500-smoke-server.json` | ticker 2× | 0.770 | **0.776** | 0.85 |
| R3 `r3/raw/combined-k1000-p10-n128-r20-r3warm1x-server.json` | chat 1k, bursts of 10 / 500 ms, 20,000/s | 0.320 | **0.330** | 0.47 |
| R3 `r3/raw/combined-k1000-p10-n128-r40-r3warm2x-server.json` | chat 1k, bursts of 20 / 500 ms, 40,000/s | 0.540 | **0.551** | 0.68 |

Both bounds hold on every row under the R4 aggregation (mean ≤ 0.50 at 1×, ≤ 0.80 at 2×). The amendment's figures ("0.42 … 0.75", "0.42–0.45 … 0.74–0.76 (three runs)", "0.32 … 0.54") are the medians, which D3 now labels "whole-window means"; the means are 0.43 / 0.45, 0.75 / 0.76 / 0.78 and 0.33 / 0.55 (non-blocking 2). The tightest reading on record is the R2 smoke run's 0.776 against the 0.80 bar at 2× — 0.024 core, inside the ±0.03 run-to-run spread — which is exactly why D3's row-retirement rule exists and why D4 must offer the load the rule names (finding 1).

## B. Attacks on the new contracts

**The context at the head of the subscriber queue.** Both enqueue points see an empty queue: `bindWarmupEpoch` refuses unless `phaseValue === "registration"` (`fanout-relay.ts:762`), when nothing has been queued for anyone; `openMeasuredWindow` refuses unless `warmup-drained` (`:1080`), and that transition is refused while `this.queuedItems !== 0` (`:996-1000`, before `phaseValue = "warmup-drained"` at `:1025`). So a context can never be placed ahead of a pending frame and never reorders a delivery. Starvation: `pump` breaks on the head's `would-block` (`:1370`); a context on a WT stream that has not opened holds the head until the stream opens, and the deliveries behind it are bounded by the 64-item cap (`:1292-1308` → `SUBSCRIBER_QUEUE_FULL`, session closed) and the 5,000 ms deadline (`:1355-1367` → `RELAY_WRITE_TIMEOUT`, session closed) — fail-closed in both directions, never silent. At the warmup bind the stream was requested at registration seconds earlier, so the steady case writes at once. Counting: `writesCompleted`/`writeBytes`/`postStopRelayWrites` are charged per non-warmup head (`:1377-1383`) and D2 excludes a `kind: "context"` item; the close-time `disconnect-undelivered` fault (`:565-578`) would count a pending context as a queued delivery, which is the right fail-closed outcome. One dead clause: "a subscriber registered late or re-registered receives the context of the epoch it joins" — registration is refused outside the `registration` phase (`REGISTRATION_CLOSED`, `:689`), so no subscriber can join an epoch late; harmless (non-blocking 4).

**Can `role-failed/v1` be lost between the child and the seal?** Orchestration order (executed): the rig accepts the warmup epoch first (`compare-controller.ts:5743-5751`, the relay binds it at `fanout-relay.ts:3036` and, under D2, enqueues the warmup context), then `runWarmupWire` (`:7759`) sends every `role-warmup-start/v1` before reading any completion, and reads one child at a time in publisher-then-worker order with a shared 11,000 ms deadline; there is no rig-side check between the epoch acceptance and the Mac reads, so a worker that refuses the warmup context at its first unit writes `role-failed/v1` into its pipe, the publishers complete at ~5 s, and the driver's next `receive` on that worker's pipe shifts the buffered frame before testing the deadline (`remote-supervisor.ts:8691`) — the code propagates through `if (!complete.ok) return complete` (`:7778`) to `driveCohortArm` → `:6454`. For the measured epoch: `runMeasuredWindow` (`:7811`) sends `role-measure-start/v1`, reads the ack, and queues `role-stop/v1` **immediately** (`:7863-7868`), so no `send` precedes `collectPartials`' `receive("role-partial/v1")` (`:7915`); a measured-context mismatch after the ack writes `role-failed/v1` into a pipe nobody writes to again, and the partial read returns it. The EPIPE-before-read race I went looking for (a stop frame written to a child that has already exited) does not exist at HEAD. Residual losses, all fail-closed and all today's behaviour: a channel already poisoned by the supervisor's own earlier fault keeps that first code (`poison` uses `??=`, `:8627`; later `receive`s return `protocolFail` = `COHORT_PROTOCOL`, `:3024-3026`); a child killed before writing seals `UNEXPECTED_EOF` → `COHORT_PROTOCOL` (not a member) or `CHILD_LIFECYCLE` (a member) — the Architect's informational 3 is right; and a second failing child is never read because the driver returns on the first. None of these lose a *first-unit* refusal's code.

**Is the whole-window mean a sound gate?** Yes, because it is not alone: `O_origin[w] = A_origin[w]` fails a relay that refuses ingress; `A_origin[w] = row ingress ÷ windows` at 1× fails a publisher that under-offers; `D = A × K` counted by the production workers on the Mac fails a relay that drops; zero `SUBSCRIBER_QUEUE_FULL` / write-timeout / refusals / malformed fails any stall the 64-item cap (256 ms at rate-250) or the 5 s deadline catches; and the per-window maximum is recorded. A 1 s saturation hidden under a 0.80 mean at the ticker rows would blow the 64-item cap (250 items/s per subscriber) before the window ended — the fault predicates back the mean. `/proc` accounting is precise and steal-free on this rig (R3 §D). The 2× pass, however, only demonstrates 2× headroom where it offers 2× the *binding* load — which it does for the ticker rows and does not for the chat rows (finding 1).

**Snapshot-receipt placement.** `cohort-start-barrier/v1` binds the digest of `rig-measure-start-ack/v1` (`server-observation-artifact.ts:110-113`), minted before the window; `rig-server-snapshot-receipt/v1` is minted after capture (`secure_fs.rs:17595-17633`), signed, carried in `rig-capture-complete-ack/v1` (`:17685-17686`) and admitted by exact key set on both sides (`:15308-15337`; TS interface `:160-190`, verified at `remote-supervisor.ts:8226-8236`). Adding `serverChildCpu` there moves no barrier digest; the two `/proc` reads sit at the receive sites within the pipe latency of the child's own `binding.busyMs()` instants (`server.ts:2532`, `:2574`), and `mainThreadMs ≤ processMs` holds by construction (`/proc/<pid>/stat` sums every thread). `busyMs` keeps its meaning: it stays the child-authored `timed()` spans (`server.ts:640`, `:1161-1172`), in the child-authored `server-loop-utilization/v1` whose key set (`secure_fs.rs:15462-15484`) D6 leaves untouched. A5's sealed A-phase artifacts predate the field; the amendment says they are not re-verified, and the fixture graph is minted at test time (`cohort-fixture-signing.ts:440-472`).

**Discriminator, tag, stale sessions.** Unchanged from R3 §C: a legal JSON unit begins `0x00` on WT (`fanout-wire.ts:1073-1083` `u32be` prefix; caps 4,096 / 1,024 B) and `{` on WS; `LengthPrefixedFrameReader` throws on an over-cap prefix (`adapters/wt.ts:2225-2231`); a stale session cannot reach a new session's inbox (inboxes are per-roleId per child process, `fanout-role.ts:1200-1201`), the full digest is recomputed at consumption, and equal warmup/measured tags refuse the session. `rejectEpochMixing` keys on `kind.startsWith("warmup-")` (`fanout-wire.ts:371-372`) as cited.

## C. What still does not respect "measurement has to always respect physical constraints"

Only finding 1: for the chat rows the gate never offers the 2× load the rule defines, so the ≥ 2× headroom of chat 250 / 500 / 1k is a review-time measurement (R3 §A, 0.54 at 40,000/s) rather than a per-stage one. The physics is not in doubt; the gate's text is.

## Blocking findings

### 1. BLOCKING — D4's 2× pass offers 1× the binding rate on every chat row

D1 (line 45): "**Binding rate of a row** = max(measured-window deliveries/s, warmup deliveries/s) = the warmup rate for every chat row (2×) and the measured rate for ticker rows." D1 (line 51): "the D4 preflight, **offering 2× the binding rate for the full window**, must sustain it with the main thread ≤ 0.80 core". D4 (line 124): "(2) **2× — the publishers offer twice the row's ingress rate for the full measured window** (ticker 500/s for 10 s; **chat 20/s for 30 s**), the warmup epoch unchanged"; D4 (line 125): "`A_origin[w] ≥ 2 × (row ingress ÷ windows)` in the 2× pass".

For chat 1k: binding rate = warmup rate = 10 publishers × 2/s × 1,000 = **20,000 deliveries/s**; 2× binding = **40,000/s** = 40 ingress/s; D4's 2× pass offers 20 ingress/s × 1,000 = **20,000/s** = 1× binding, and its predicate binds `A_origin[w] ≥ 20`, the same 1× load. The same holds for chat 250 and chat 500 (2× the measured rate = 1× the warmup rate on every chat row, by D1's own "chat 20/s × K = 2× the measured rate"). So the pass that reads the ≤ 0.80 bound never offers the load the rule names on half the ladder, D3's "at 2× 0.54 — Both bounds hold on every row; D4 re-measures them before each stage" is not re-measurable by D4 as written, and the stop condition "a D4 preflight below 2× stops staging" cannot fire for a chat row. The ticker rows are consistent (binding = measured; 2× = 500/s).

Required text: D4 pass (2) offers **2× the row's binding ingress rate** for the full measured window — ticker 500/s for 10 s, **chat 40/s for 30 s** — with the predicate `A_origin[w] ≥ 2 × (binding ingress ÷ windows)` (ticker 500 per window, chat 40 per window), and D3's 2× column stated against the same number. The fix retires no row: the R3 raw file at chat 1k, 40 ingress/s (bursts of 20 per 500 ms, a harsher shape than D4's paced 40/s) reads mean 0.551, max 0.68, queue peak 2,464, conservation exact, zero faults.

Commands: `sed -n 45p;51p;124p;125p docs/superpowers/plans/2026-09-08-physical-budget-amendment.md | grep -o 'the warmup rate for every chat row[^.]*\|offering 2× the binding rate[^,]*\|chat 20/s for 30 s\|A_origin\[w\] ≥[^;]*'`; `python3 -c "print(20*1000, 2*20*1000)"` → `20000 40000`; `python3` over `scratchpad/r3/raw/combined-k1000-p10-n128-r40-r3warm2x-server.json` → mean 0.551, max 0.68.

## Non-blocking findings

### 2. D3 labels per-second medians as whole-window means; one 2× run's mean lies outside the stated range

§A. D1 "Combined at the R3 rows" prints 0.42 / 0.75 / 0.32 / 0.54 and D3 prints "whole-window means: ticker rate-250 WT 0.42–0.45 core at 1×, 0.74–0.76 at 2× (three runs); chat 1k … 0.32, at 2× 0.54". Those are the medians of the 1 s samples. The sample means are 0.43 / 0.45 (1×), **0.75 / 0.76 / 0.78** (2×; R2 `smoke` = 0.776), 0.33 / 0.55. Every mean is inside its bound; state the means as means (and the 2× range as 0.75–0.78) so the document's numbers are the statistic the rule gates. Command: python over the seven raw files listed in §A.

### 3. The warmup-loop sentence, read literally, refuses the warmup context it needs

D2: "the warmup loop (`:1361-1370`) refuses, not ignores, any unit that is not a warmup-tagged compact frame". The warmup delivery context is a `0x00`-prefixed JSON unit on the same channel, consumed by the same loop before any compact frame ("verifies at consumption, in inbox order"). Write: "any unit that is neither the epoch's delivery context nor a warmup-tagged compact frame".

### 4. "A subscriber registered late or re-registered receives the context of the epoch it joins" describes no reachable case

`registrationRefusal` returns `REGISTRATION_CLOSED` unless `phaseValue === "registration"` (`fanout-relay.ts:689`), and `bindWarmupEpoch` leaves that phase (`:857`). Every subscriber that exists at the warmup bind receives the warmup context then; nobody joins later. Delete or reword; nothing depends on it.

### 5. Architect R4 findings 1 and 2 — confirmed; one offset the Architect added is itself off

- Pinned grant hex: `RUST_PINNED_TICKER10K_GRANT_HEX` at `cohort-protocol.test.ts:3709` and `crates/native/tests/cohort_protocol.rs:970` (executed; the hex embeds `"cellId":"ticker-fanout/rate-10000"`); `cohort_cell(cell_id)?` at `secure_fs.rs:19695` (`fn cohort_cell` at `:19658`) makes the pinned grant unparseable once the id is refused. Add to D3's pinned list and slice 3.
- Citations: `cohort-fixture-signing.ts:363-415` mints `rig-measure-start-ack/v1` (`:364`) and `server-loop-utilization/v1` (`:393`); the snapshot-receipt fixture is `:440-472`. `server-observation-artifact.test.ts:568` pins `RUST_PINNED_MEASURE_START_ACK_HEX` (`:526`, `:563`), which D6 does not move; the snapshot receipt has no hex pin. `mac_cohort_runtime.rs:5073-5085` is the CHAT_1K manifest test; the ticker call is `:5162`.
- Offsets: `handle.onSession` is `server.ts:1030`; the Rust vocabulary is `secure_fs.rs:12171-12193`; the worker warmup budget **is** `fanout-role.ts:1361-1362` as D2 says (the Architect's ":1363-1364" is wrong); the snapshot frame starts at `server.ts:2596`; `frame.subscriberId ?? ""` is at `fanout-role.ts:1451` (D2 says `:1455`).
- D3's "(89 tracked files)" for `ws-wt-r0`: `git ls-files … | wc -l` → 855 tracked, of which 80 name a retired id (`git grep -lE … | wc -l`). Informational.

### 6. Informational

- `secure_fs.rs:12119-12121` `CHAT_10K_MAX_WORKER_SUBSCRIBERS = 1_250` / `CHAT_10K_MERKLE_SIBLINGS = 14`, used as a token-bundle bound at `:13389`: constants named for a retired cell that remain valid as bounds. Not a mirror; slice 3 should leave them or rename them, not shrink them.
- Test files naming a retired id or label: 21 by my pattern (Architect 22, R3 Critic 24, D3 "24–26") — pattern differences.
- D2's "a child that exits without the frame still maps to `UNEXPECTED_EOF`/`CHILD_LIFECYCLE`": `UNEXPECTED_EOF` is a channel code and seals as `COHORT_PROTOCOL` (Architect 3); today's behaviour, kept.
- The `series` the R2/R3 harness recorded has 9 samples per 10 s window; D4's definitive reading is the `(utime+stime)` delta over the whole interval, as D4 says, so the preflight's number will not be a mean of samples. Fine — the receipt should say which instrument produced each figure (R3 §D last item; D4 records both).

## Verified without finding

- Every D1 number cited in D3/D4 traces to a raw file (§A) or a REPORT.md (`.scratch/2026-09-08-physical-budget/{link-and-transport,relay-cpu-and-encoding,sessions-fds-memory}/REPORT.md` exist; the scratch codec's five-field preimage is at `relay-cpu-and-encoding/tree/bench/compact-codec.ts:52-57` and is disclaimed correctly).
- D3 arithmetic: `windowCount: 10 | 30` (`cohort-protocol.ts:1805`); `A_origin[w]` at 1× = 250 / 10; `D_warmup` = 1,000 / 25,000 / 50,000 / 100,000; `publisherRatePerSecond = measuredIngress / publisherCount / seconds` (`compare-controller.ts:4631-4634`) integers on every row; readiness closed set enforced at `cohort-protocol.ts:834` and `:3869`, removed members `:103-104`, Rust rows `secure_fs.rs:19636`/`:19649` (no Rust closed set beyond the rows, as D3 says); `cohortReadinessDeadlineMs` switch `:6874-6889` in D3's list; `PHASE4_GATE_CELLS :275-278` names the retired ticker id and is in D3's list.
- Mirror grep outside tests (`git grep -nE … -- tools crates ':!*.test.ts' ':!*fixtures*' ':!crates/native/tests'`): `secure_fs.rs:19574,19587,19600,19626,19639`, `compare-controller.ts:277,:4096`, both fragments `:4`, `render-phase4-report.ts:23`, `cross-supervisor-protocol.ts:195-215`, `evidence.ts:154-159` — all in D3; `types.ts:30`, `scenario-registry.ts:228/:256-258/:514-519`, `client.ts:1442/:1470`, `cohort-protocol.ts:5596-5602/:5713-5721` as listed.
- 9.6 at HEAD: `CAMPAIGN_ID=fanout-pilot-r1`, `pilot`, `REPS=1`, `CAMPAIGN_TIMEOUT_MS=1800000`, `EXPECTED_PASS=2`, `EXPECTED_PROMOTABLE=0`; `stage-live-campaign.ts:1089` pilot `2_100_000` (+300,000). 9.7: `43200000` / `:1091` `45_000_000` (+1,800,000); D5's 14,400,000 / 16,200,000 keeps the pairing; `EXPECTED_PASS=60`, `EXPECTED_FLATS=12`, `EXPECTED_PAIRED_PROMOTIONS=6`. Worst case 36 × 76 + 36 × 156 = 8,352 s = 2.32 h; lease `RUN_TIMEOUT_MS + 5400000` = 21,600,000 (`frozen-run-wrapper.fragment.sh:480-481`). `abandon` at `stage-live-campaign.ts:76`, `:3476`.
- `bun tools/compare/check-official-io.ts` → `failure-count=0` (resolved-graph `1ba7d8b3…`, inventory `4f53cda1…`); `cliEntryTs` has 10 entries; the fresh-root check tests `<candidate>/<campaignId>`, `macRoot`, `rigRoot` only (`:2799-2808`), so the preflight directory is exempt.
- `verify-artifact.ts` reads no worker counter (`grep -c 'malformedCount\|perSubscriberDelivered'` → 0; `:4124-4149` Linux-only); `WorkerPartialV1` carries `perSubscriberDelivered`, `duplicateCount`, `reorderCount`, `malformedCount` (`cohort-protocol.ts:4092-4095`); `recordMalformed` at `fanout-role.ts:937`; `estimateDeliveryBytes` probes canonical JSON (`fanout-relay.ts:1711-1719`); the WS connector drops a refused decode silently (`:2196-2197`) and the WT connector at `:2253` — D2 names both.
- `verify-campaign-index.ts:220-245` requires null codes on PASS and a non-null `failureCode` on FAIL; `cohortWtListenerAdmission` at `server.ts:940`; `FANOUT_MAX_WINDOW_COUNT = 30` (`fanout-wire.ts:71`).
- The armed stage's reviews exist (`.trust-staging/efaed8bb…/fanout-pilot-r1/reviews/{architect,critic}.md`) and the A5 evidence directory D1 cites exists (`efaed8bb…/busyms-attested-focused-r1/`), both untracked and untouched.
- Deviation file `2026-09-08-a5-admission-gates-before-traps.md` exists; the other three are to be written by slice 4, as D6 says.
- Rig idle, no scratch, no process of mine on either host; tracked tree untouched.

## Cleanup

Nothing was created on the rig (`ls -d /tmp/amendment-review-*` → none, before and after). Local: only this file written; `git status --porcelain | grep -v '^??' | wc -l` → 0.

## Two-perspective note

- Perfectionist: after four revisions the document's own rule and its own gate still disagree on the one number that decides whether half the ladder has the headroom the user's ruling demands — and the disagreement sits in the pass whose bound (0.80) the tightest run on record misses by 0.024. A gate that offers 1× and calls it 2× is the class of defect this review exists to stop before a grant hashes it. The means-labelled-medians slip is the same reflex: a statistic named without being computed.
- Pragmatist: the physics is measured at the true 2× point already (0.55 at 40,000/s, exact conservation), every hop of the fail-closed path exists at HEAD and was traced end to end, the record placement is right, and the fix is one number, one predicate clause and a relabelled row — no row, frame, record or mechanism changes. One more revision, and it should be the last.
