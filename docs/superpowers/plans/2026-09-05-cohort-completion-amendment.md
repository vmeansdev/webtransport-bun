# Cohort integration completion amendment

**Status:** APPROVED

The status is frozen before the approving review, as required by the base plan. This file alone grants nothing: implementation starts only after actual unconditional Architect then Critic verdicts on these exact bytes and the bound baseline. Both passes are performed by the lead Codex agent (GPT-6) at the user's request; they are separate passes, not independent agents or external CLI reviews.

## Identity and precedence

- Base plan: `docs/superpowers/plans/2026-08-30-busyMs-attested-fanout.md`, SHA-256 `9374b7470223655bff5d118c1a8812f62e62515aa8ac56e38ff61eb4f4b961d9`.
- Existing integration design: `docs/superpowers/plans/deviations/2026-09-02-cohort-runtime-integration-design.md`, SHA-256 `62ea8c4a87ac646e4be4f98dff0948e71f8ff04a2ecc3f660068c243e4f4feab`.
- Source baseline: HEAD `35c4cec7e56a3ebba72a780443f505784a8e728c`, plus tracked source changes recorded by `.scratch/2026-09-05-cohort-completion/baseline.json` (patch SHA-256 `e29f531ce1292dfd58384e42627d306db8b79fc5f7f54f06aa0800582b2b65ba`). The patch preserves the pre-existing wave-3.5 work and integration design edits. No existing work is discarded.
- Worktree: `/Users/vmeansdev/Developer/Codex/Apps/webtransport-bun/.claude/worktrees/ws-scenario-comparison`.
- The user authorizes plan correction followed by end-to-end execution, and explicitly overrides the old Sol High CLI instruction. Bun 1.3.14 is installed. Use Bun for JS/TS commands; no Node execution is needed.
- Original plan/design bytes remain unchanged. This amendment overrides only the specific missing contracts and intermediate gate/ownership conflicts described below. Its review identity replaces the historical source-review HEAD for this resumed implementation. Fresh staged campaign reviews still bind the final implementation HEAD, exact archive, binaries, roots, keys, approval identity and run command.
- Final scope remains new A5 focused proof, B5 ticker-10k pilot, and B6 six canonical fanout cells (60 measured seals, six pairs, twelve flats). Local chat-1k e2e is an additional integration test, not a substitute for B5 or B6. No claim of completing the unrelated 112-arm registry campaign is made by this six-cell plan.
- Final review artifacts are `reviews/2026-09-05-cohort-completion-architect.md` then `reviews/2026-09-05-cohort-completion-critic.md`; canonical approval record is `approvals/2026-09-05-cohort-completion-amendment.md`. New staging uses this amendment as `approvedPlanSha256` and that finalized record as `approvalRecordSha256`; the amendment binds the unchanged base/design digests above. Stage tooling must accept the explicitly supplied approved-plan/record paths and must not silently reuse the old approval record. Per-campaign exact staged reviews remain separate from this implementation approval.

## Findings driving the amendment

The completed initial Architect and Critic reviews are `reviews/2026-09-05-cohort-existing-architect.md` and `reviews/2026-09-05-cohort-existing-critic.md`. Source confirms missing grant topology and authority digests; unimplemented Phase-A/binary bridge; undefined export-signature transcript; old export consumers (16 fresh Bun type errors); live controller fixture attestation; a lease requiring future evidence before execution; and legacy SSH server startup before cohort dispatch. The retained wave-3.5 gate also identifies missing vectors and fixture ownership. These are implementation blockers, never environmental REFUSED rows.

The original full gate requires zero failures. Historical intermediate normalized failure baselines do not waive it. Historical A5 artifacts have not been independently re-established under the intended binary-owned signing graph and cannot be carried forward as completion evidence.

## C1: grant topology and bounded carrier

Retain the existing six-field `token-commitment-leaf-manifest/v1` and its Merkle leaf format. Add `publishersBase64` and `subscriberShardsBase64` to `mac-open-cohort-request/v1`: canonical JSON arrays of the already defined `PublisherRoleGrantV1` and `SubscriberShardV1` records. These are non-secret arrays produced by the existing TypeScript topology builder, alongside the leaf manifest. Each decoded array is capped at `COHORT_GRANT_MAX_BYTES` (256 KiB); both count against the execution evidence budget. Do not send a full unsigned grant or raw tokens.

Rust exact-parses both arrays and validates publisher IDs/order/commitment indices/token hashes against the presented leaves; exactly eight ordered shards; workerIndex == residue == array index; modulus 8; exact membership/union and ordered-ID digest; commitment index ranges; and registered cell counts. It embeds the verified presented arrays in its own signed grant and independently recomputes the Merkle root and leaf-manifest digest. TypeScript no longer constructs any production CohortGrantV1. It retains only token material, non-secret topology and manifest for presentation/FD5 delivery, then accepts the exact signed binary grant.

Measured at the baseline with the existing builder and a 41-character production cohort ID: chat-10k manifest is 2,713,015 decoded bytes / 3,617,356 Base64 characters, exceeding the design's 1 MiB request cap. Keep the existing 4 MiB leaf-manifest decoded cap. Give only `mac-open-cohort-request/v1` a 7 MiB encoded total cap, sufficient for 4 MiB manifest plus two 256 KiB arrays, Base64 expansion and the separately bounded role plan; cap the role plan at 256 KiB on this request. Outer frame and per-execution budgets remain 14 MiB and 20 MiB respectively. Charge manifest, topology and role plan decoded lengths before allocation; do not retain duplicate byte copies merely because they reappear in another frame. Test largest registered topology, exact caps and cap+1 in both languages. This is a bounded control-frame correction, not an increase to relay/product buffering budgets.

## C2: one execution authority and complete binary mints

Atomically extend the campaign-authority approval object with required `approvedPlanSha256` and `approvalRecordSha256` (real staged artifact digests). Update every authority producer, exact parser, retained type, fixture and digest dependency together. Retain both fields and pass the validated authority into the campaign Mac runtime. Never alias another approval digest. Historical artifact files are immutable; regenerate only current test fixtures and current staged artifacts.

`ResidentLoop` remains the sole allocator/consumer of legacy measurement grants and execution ordinals. Add execution-scoped immutable state holding the original grant/key, full cross-supervisor execution, draft, Mac execution receipt/signature, accepted series and admission receipt/acceptance timestamp. `present_artifact_payload` may consume the open grant but must transfer the verified facts into that retained execution state before clearing the open slot. All later Mac mints read this state, never a replacement controller value. A second ordinal allocator in `MacCohortRuntime` must be removed or delegate to this one source.

Route the registered `mac-open-execution-request/v1` and observation/admission frames in the actual binary dispatcher, using the same bounded sequence tracking as cohort frames. The open request validates the canonical draft against authority, campaign, candidate, source/plan/approval identity and the original measurement grant, constructs CrossSupervisorExecutionV1 and signs the exact MacExecutionGrantReceiptV1. Return the registered opened ack with retained receipt/signature bytes. Execution/grant declaration and run/repetition identity must be equal to the actual opened measurement, including warmup 0/1. No fixture execution is accepted as a shortcut.

All eight cohort transitions must have a positive production implementation. Open verifies C1 plus retained execution; warmup epoch uses supervisor nonce/clock; completion verifies every child and totals before signing its manifest; barrier verifies retained drained/start records and schedules at least 250 ms ahead; barrier acceptance binds the binary's own barrier; observation verifies five presented plus two retained rig records, admitted series and derived records; admission signs the two required records; export checks retained partial/derived evidence against admitted facts and returns C3. Preserve one-shot minting, expiry, replay, durable signing sequences, counter/accounting semantics and every existing independent forgery guard.

One campaign process must complete multiple executions without leaking retention or reusing a grant. Terminal cleanup releases execution-owned state; durable replay/sequence evidence persists. Missing execution, cross-run state and restart-with-five-of-seven remain failures.

## C3: terminal export signature and retained reconstruction

Keep the current eight-field `MacCohortEvidenceExportedAckV1`. Define `cohortObservationEvidenceSignatureBase64` as canonical Base64 of exactly 64 raw Ed25519 signature bytes. The binary signs canonical JSON of the seven other fields, in the normal canonical ordering:

```ts
{ schema: "mac-cohort-evidence-exported-ack/v1", responseSeq, ackRequestSeq,
  executionSha256, cohortObservationEvidenceSha256,
  cohortObservationEvidenceSize, terminalExport: true }
```

This explicitly resolves the design's undefined "signature of the digest" into a domain-, execution-, sequence-, size- and digest-bound transcript. The staged Mac public key is the sole verification key; the ack cannot supply a key. Do not expand `MacReceiptSignatureV1` or invent a recursive self-signature. The existing ack cap remains 8 KiB.

`CohortEvidenceExportReceipt` retains the complete ack including signature. `cohortEvidenceFromExportAck` receives the exact retained observation plus ack and staged verification key, validates all required components, canonicalizes the complete 33-field observation, checks the 9 MiB decoded cap, size and digest, and verifies the unsigned-ack transcript signature before constructing artifact evidence. It must not read the deleted payload field. Raw child evidence request retains its 9 MiB decoded / 14 MiB encoded caps and charges the execution budget before decoding. Null cohort admission records refuse, never shorten the assembled graph.

The writer preserves the full ack. The offline verifier requires its exact keys/signature, recomputes the same transcript and observation bytes, and verifies with the external staged trust key while retaining all existing issuer-graph/partial/ledger checks. A digest match without an authentic terminal ack is insufficient. Fixtures use test-only keys and explicit test producers; production uses only binary-issued bytes.

Pin identical unsigned-ack transcript bytes in Rust and TS, and complete the request/frame conformance vectors identified by the wave-3.5 gate. Pin the already-required chat-1k and ticker-10k full observation encodings independently in both production encoders. Every negative signature test has an honest positive sibling; deleting the verification call must break the forgery test.

## C4: channels, acquisition and deferred finalization

Implement `MacCohortChannel` as the bounded public-key-only client for the actual supervisor process. `MacFanoutSupervisor` becomes orchestration/state tracking over that channel, not a signer. Preserve exact receipt bytes from binary acknowledgments; remove private-key configuration and production `macSign` calls. Delete the TypeScript grant constructor and its returned/onMinted grant fields while retaining C1 topology/manifest delivery. Remove production reachability of `mintPhaseAAttestationFixture`; move fixture-only signing into fixture/test modules where needed. Both ordinary and cohort arms assemble Phase-A evidence from actual Mac/rig channel results.

Split runtime material into acquisition and finalization. Acquisition accepts verified stage handles/keys/digests, registered arm/repetition identity, grant/draft, role plan, endpoint/TLS data, token material and launch/clock/deadline configuration. It returns the binary-bound execution/cohort, child host, rig channel, token delivery, lifecycle driver, cleanup and a finalization function. It does not require server snapshot, admitted series or attestation before they exist.

After measured child partials, rig stop/capture and series admission, finalization obtains immutable authenticated snapshot bytes, admission counters, ArmMeasureSupervisorContext, recorder identity and complete ArmAttestationEvidenceV2. Only then assemble, verify and seal the artifact. No mutable casts, zero placeholders, fabricated recorders, fixture attestations or time-of-check substitutions. Retain accepted execution facts across legacy grant consumption as C2 requires.

The actual `realRunBody` wires this production lease factory. Branch on registered cohort routing before legacy server startup: cohort arms use rig-supervisor spawn after signed acceptance, never the SSH/nohup server path. Ordinary A5 traffic must also follow the base plan's signed server lifecycle. Staging binds real launch argv, local/remote binaries/addon/Bun, TLS and immutable roots. Keys remain inaccessible to the controller. Use the existing two campaign descriptors and uid boundary, apply all enumerated crossing modes, set Mac receipt validity environment, validate the twelve uid access preconditions and consume the reaped verdict on every teardown path.

Preserve named terminal semantics. Missing executors keep implementation blocked without writing REFUSED index rows. Before traffic environmental failures may be REFUSED; after launch protocol/product/lifecycle/resource failures are FAIL. Every wait and cleanup is bounded.

## C5: final validation and runtime feasibility

Fix the benchmark-local WT listener options to admit the registered session/ramp topology without changing native or package defaults, per-session/global byte budgets, or relay limits. Derive configured maxSessions/per-IP/per-prefix handshake/session limits from the registered cohort (including chat-10k 10,010 sessions), not an unbounded setting. Cover both per-IP and per-prefix enforcement. Correct the WS role connector TLS option translation to the existing Bun-compatible options; live runs use staged CA/SNI verification, not a verification bypass.

The final official-I/O audit must exit zero on a clean archived candidate and on the source scope it promises to inspect. Staged historical copies must not accidentally become fresh production roots; do not delete evidence to hide violations. The lead owns a focused audit policy/implementation slice: repair stale/missing/duplicate inventory and doubled path prefixes; define narrowly capability-aware operations for reviewed controller, supervisor, role-child and package-loader buckets; remove actual ambient/path authority reachable from official children; and preserve adversarial oracles for fixture reachability, arbitrary filesystem/exec/network operations and unvalidated addon loads. No blanket module permission or baseline-count subtraction. Source changes in package loader/stream modules are restricted to routing official-child operations through existing validated descriptor/configuration boundaries; ordinary API behavior must remain covered by its focused tests.

Use `bun node_modules/typescript/bin/tsc -p tsconfig.json` and `bun` for scripts/tests. Required final commands: comparison suite; typecheck; official-I/O audit; cargo workspace tests; cargo fmt; the base plan's native lib/supervisor Clippy gate with warnings denied; git diff check; current fixture/document hash validation. Baseline broader all-target Clippy findings are reported separately, but the required base-plan Clippy gate must be zero. Do not regenerate fixture hashes before source identities have settled for the slice; use existing convergence/check scripts.

Real-process acceptance must drive production channels, provider, executors, both supervisor binaries and actual role/server children. Local chat-1k runs warmup + measured for WS and WT (four executions), yields two verified non-promotable seals, and proves exact process/session/delivery cardinalities and both issuer graphs. Delete obsolete negative tests only when their positive production replacement passes. Add failure/timeout/signature/substitution mutation checks, largest-cell codec tests and key/PGID cleanup proof. Local loopback evidence remains local evidence, not the physical pilot.

## Execution slices and ownership

The lead performs Architect then Critic review of this bundle before product edits. Native inherited-model subagents may implement bounded owned slices; no model or external CLI override. They are not alone in the worktree, must preserve pre-existing edits and may not commit other slices. The lead owns integration, scoped staging/commits and review gates. Existing wave-3.5 changes are incorporated with their related reviewed contract slice, not discarded or committed as unrelated bulk work.

1. **Protocol contracts (C1/C3):** `tools/compare/cohort-protocol.ts`, `cross-supervisor-protocol.ts`, their test files; initial ownership of `evidence.ts`, `artifact-builder.ts`, `verify-artifact.ts` and related evidence/export fixtures/tests, including `fanout-artifact`, `cohort-artifact-budget`, `cohort-seal`. Introduce exact carrier/signature helpers and the complete reassembly API. Rust counterpart is implemented sequentially by slice 3. Pass focused protocol tests; typecheck fallout is tracked only until corresponding callers land, never accepted at the final gate.
2. **Atomic authority contract (C2):** `tools/compare/secure-fs.ts`, `bin/compare-stage.ts`, `bin/stage-live-campaign.ts`, `r1-fixtures.ts`, all authority/staging fixture tests, plus `crates/native/src/secure_fs.rs` authority region and `crates/native/tests/secure_fs_trust.rs`. No concurrent writer to Rust secure_fs. Migrate actual staged inputs and full exact fixture graph, verify missing/mismatched digests fail, then converge current fixture hashes.
3. **Rust Mac runtime (C2/C3):** `crates/native/src/secure_fs.rs` cohort/mac/measurement/trust bridge regions; `bin/comparison-supervisor.rs`; `tests/mac_cohort_runtime.rs`, `rig_cohort_runtime.rs`, `fanout_supervisor.rs`, `cross_supervisor_protocol.rs` and binary unit tests. Implement full Phase-A bridge and every mint, vectors, retention and terminal sequencing. Preserve rig behavior and stable wire refusal mappings. Focused Rust tests and native review gate before next integration work.
4. **Production TS channels (C4):** `remote-supervisor.ts`, `server-observation-artifact.ts`, `server-observation-artifact.test.ts` if needed, `fanout-executor.test.ts`, `fanout-supervisor-integration.test.ts`, `remote-supervisor.test.ts`, `mac-supervisor-spawn.test.ts`, `r1-fixtures.ts` only after slice 2 hands it back. Remove production fixture/signing/constructor paths and integrate exact-byte channels. Coordinate evidence API with slice 1; no concurrent edits to shared files.
5. **Controller/staging and transport integration (C4/C5):** `bin/compare-controller.ts`, its tests, `bin/stage-live-campaign.ts` and tests after slice 2; `server.ts`, `bin/fanout-role.ts`, server/role and relay integration tests. Wire acquire/finalize factory, preflight/modes, real ordinary and cohort lifecycle, TLS/admission configuration and cleanup. The earlier assertion that no slice may edit fanout-role.ts is superseded for the evidenced TLS defect and any proven lifecycle requirement, with focused tests.
6. **Lead gate/audit and acceptance (C5):** `check-official-io.ts`, `official-io-allowlist.json`, entrypoint/graph tests; relevant validated package-loader/official-child boundaries if actually reached; `fanout-production-e2e.test.ts`, campaign verifier/render tests and execution wrappers; fixture regeneration scripts only if source evidence proves necessary. Every newly discovered cross-file correction is recorded with its owner and proof; semantic changes to this approved contract require a new amendment/review, not silent expansion. Run spec-compliance then code-quality review, repair findings, repeat focused validation, and complete the full required gate.
7. **Fresh live acceptance:** preserve successful source snapshot with scoped commits, build/archive/stage fresh A5, review exact staged artifacts sequentially as lead Architect then Critic, run frozen A5 command, recursively verify two focused seals and zero flats. Only then stage/review/run B5 ticker-10k: 1+8 processes, 101 sessions, 100,000 ingress and 10,000,000 deliveries for each arm; exactly two non-promotable seals. Only after B5 passes stage/review/run B6: six cells, five measured reps per arm, warmups unsealed, 60 fresh measured primary PASS seals, twelve flats, six paired promotions, twelve attested arm summaries. Run immutable commands and cleanup exactly as the base plan's wrapper specifies. New campaign IDs/root suffixes are required where old roots exist, recorded in the exact staged review; never overwrite or resume old evidence.

Each logical behavior is one scoped commit, reviewed against its tests. Planning/review records remain local unless the user explicitly asks to commit them. No publish/merge occurs as part of this execution request. Final report distinguishes measured acceptance, code checks and remaining real environment/campaign failures with exact artifact references.

## Review criteria, risks and stop conditions

Architect must verify every changed record has an input source, canonical output, carrier, accounting and consumer; that acquisition/finalization ordering is causal; and that each signer is separate from the controller. Critic must challenge complete positive reachability, signature mutation sensitivity, highest-cardinality bounds, exact final gates and live evidence scope. Both verify the unchanged base/design digests and current baseline patch and bind this amendment's exact path/SHA/HEAD before recording approval.

Risks: (1) honest maximum-size evidence exhausts a control budget; detect with largest-cell preflight tests and fail explicitly, never drop evidence or silently increase limits. (2) controller retains fake or early evidence; detect through real-process positive flow plus mutation of retained signed bytes, forbid fixture reachability. (3) local/physical runtime cannot sustain prescribed ticker rate or host permissions/network prevent execution; distinguish pre-traffic environment from post-launch failure, collect bounded diagnostics and teardown, do not replace the topology or promote partial evidence. Re-prove honest failures only under a fresh correctly staged grant and review; source changes invalidate old stage approval.

Do not pause for per-slice confirmation: the user authorized review, corrections and execution. Stop dependent live work on actual missing external authority/environment, failed base-plan stop gate or invalidated identity; complete independent local work while investigating. Never label a partial implementation, green component suite, historical report, failed pilot or integrity-only report as end-to-end success.
